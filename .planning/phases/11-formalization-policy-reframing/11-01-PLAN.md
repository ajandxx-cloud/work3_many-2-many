---
phase: 11-formalization-policy-reframing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - paper/sections/model.tex
  - paper/sections/algorithm.tex
autonomous: true
requirements:
  - FORM-01
  - FORM-02

must_haves:
  truths:
    - "tab:timing-diagram table exists in model.tex with 5 event rows including the Bernoulli sampling row for Layer 2"
    - "eq:alns-objective equation exists in algorithm.tex with three enumerated design-choice bullets"
    - "No existing text in either file is deleted"
  artifacts:
    - path: "paper/sections/model.tex"
      provides: "Timing/decision table (tab:timing-diagram) showing Layer 1-3 event sequence with Bernoulli sampling point"
    - path: "paper/sections/algorithm.tex"
      provides: "Online Objective paragraph with ALNS surrogate objective equation and three design-choice bullets"
  key_links:
    - from: "paper/sections/model.tex"
      to: "paper/sections/algorithm.tex"
      via: "Both documents formalize the same coupled decision problem from different angles"
---

<objective>
Add two formalization elements to the paper that the Codex reviewer flagged as
implicit: (1) a timing/decision table in model.tex showing the Layer 1-3 event
sequence with explicit Bernoulli sampling timing; (2) an ALNS online objective
paragraph in algorithm.tex stating the surrogate expected-cost objective and
clarifying the role of Bernoulli sampling and rejection penalty Γ.

Both changes are pure LaTeX text insertions with no deletions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-formalization-policy-reframing/11-CONTEXT.md

@paper/sections/model.tex
@paper/sections/algorithm.tex
</context>

<tasks>

<task type="auto">
  <name>Task 1 (FORM-01): Insert timing/decision table into model.tex</name>
  <files>paper/sections/model.tex</files>
  <action>
    Read paper/sections/model.tex. Find the closing `\end{table}` of the
    `tab:layer-coupling` table (the table with label tab:layer-coupling showing
    Layer / Input / Output / Coupling). Insert the following content AFTER that
    `\end{table}` line and BEFORE the sentence "The three-layer architecture
    captures the essential feedback structure..." (insert between these two).

    Content to insert:

    ```latex
    \begin{table}[h]
    \centering
    \caption{Timing and decision sequence in the rolling horizon three-layer model.
             Each row represents one event within a single planning cycle.
             Re-optimization triggers fire at $t^{\text{now}} = 0, \Delta, 2\Delta, \ldots$
             (Equation~\ref{eq:rh-trigger}).}
    \label{tab:timing-diagram}
    \footnotesize
    \begin{tabular}{p{3.0cm}lp{3.5cm}p{3.5cm}l}
    \toprule
    \textbf{Event} & \textbf{Layer} & \textbf{Decision Variables} & \textbf{Information Flow} & \textbf{Bernoulli?} \\
    \midrule
    Request $r$ arrives at $t_r^{\text{req}}$ &
      1 &
      $m_r^P,\; m_r^D,\; v_r,\; \pi_r^P,\; \pi_r^D$ &
      Routes $\{\sigma_v\}$ in; bundle $b^*$ out &
      No \\[4pt]
    Bundle $b^*$ presented to passenger &
      2 &
      $z_r$ &
      $b^*$ in; acceptance $z_r$ out &
      Yes: $z_r \!\sim\! \mathrm{Bern}(P_{\text{acc}})$ \\[4pt]
    If $z_r=1$: route $\sigma_{v_r}$ updated &
      3 &
      (route update) &
      $z_r, b^*$ in; updated $\sigma_{v_r}$ out &
      No \\[4pt]
    Re-opt trigger at $t = k\Delta$ &
      3 &
      $\{\sigma_v\}$ (all vehicles) &
      $\mathcal{R}^{\text{act}}$ in; optimised $\{\sigma_v\}$ out &
      No \\[4pt]
    Updated routes fed back for next request &
      1 &
      --- &
      Updated $\{\sigma_v\}$ available to Layer~1 &
      No \\
    \bottomrule
    \end{tabular}
    \end{table}

    Table~\ref{tab:timing-diagram} details the event sequence and information
    flows within a single planning cycle; in particular, it shows that Bernoulli
    sampling ($z_r \sim \mathrm{Bernoulli}(P_{\text{accept}}(b^*))$) occurs
    exclusively in Layer~2, after bundle $b^*$ has been selected by Layer~1.
    ```

    Then the original sentence "The three-layer architecture captures..." follows
    unchanged.
  </action>
  <verify>
    <automated>python -c "
with open('paper/sections/model.tex', encoding='utf-8') as f:
    tex = f.read()
assert 'tab:timing-diagram' in tex, 'tab:timing-diagram label not found'
assert tex.count('Bernoulli') >= 2, f'Expected >=2 Bernoulli hits, got {tex.count(\"Bernoulli\")}'
assert 'tab:timing-diagram' in tex, 'cross-reference to tab:timing-diagram not found'
# Make sure original tab:layer-coupling still present
assert 'tab:layer-coupling' in tex, 'tab:layer-coupling was accidentally deleted'
print('model.tex FORM-01: all assertions passed')
"
    </automated>
  </verify>
  <done>tab:timing-diagram table exists in model.tex with Bernoulli row, and tab:layer-coupling is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2 (FORM-02): Insert ALNS Online Objective paragraph into algorithm.tex</name>
  <files>paper/sections/algorithm.tex</files>
  <action>
    Read paper/sections/algorithm.tex. Find the line ending with:
    "...rolling horizon ALNS heuristic that processes arriving requests online and
    periodically re-optimizes committed routes. The algorithm follows the
    three-layer architecture of Section~\ref{sec:three-layer}."
    (This is in subsection "Heuristic Algorithm: Rolling Horizon ALNS", around lines 77-79.)

    Insert the following content AFTER that sentence and BEFORE the
    `\subsubsection{Candidate Generation}` heading:

    ```latex
    \paragraph{Online Objective.}
    The ALNS heuristic operates online under uncertainty about future passenger responses.
    At each request arrival, the system selects bundle $b^*$ to minimize the
    \emph{surrogate expected routing cost}:
    \begin{equation}\label{eq:alns-objective}
      \mathbb{E}[\Delta C \mid b,\, \mathbf{x}_r]
        = P_{\text{accept}}(b^*) \times \Delta C_{\text{routing}}(b^*, \{\sigma_v\}),
    \end{equation}
    where $\Delta C_{\text{routing}}(b^*, \{\sigma_v\})$ is the incremental routing cost
    of inserting bundle $b^*$ into the current vehicle schedules
    (evaluated via constraint checks described below), and
    $P_{\text{accept}}(b^*) = \exp(U_{rb^*}^k) / (1 + \exp(U_{rb^*}^k))$
    is the binary logit acceptance probability (Equation~\ref{eq:binary-logit}).

    Three design choices follow from this formulation:
    \begin{enumerate}
      \item \textbf{Bernoulli sampling occurs after bundle selection.}
            The system selects $b^* = \arg\min_b \mathbb{E}[\Delta C \mid b, \mathbf{x}_r]$
            and \emph{then} samples $z_r \sim \mathrm{Bernoulli}(P_{\text{accept}}(b^*))$.
            ALNS uses the expected cost, not the realized cost, to rank bundles.
      \item \textbf{The rejection penalty $\Gamma$ does not enter bundle selection.}
            $\Gamma$ enters only the social welfare metric
            $W = \sum_r [z_r U_{rb^*} - (1-z_r)\Gamma]$ (Section~\ref{sec:pareto}),
            which is a post-hoc evaluation tool, not an input to the routing or
            binary logit model. The acceptance probability $P_{\text{accept}}(b^*)$
            is invariant to $\Gamma$.
      \item \textbf{Rolling horizon re-optimization minimizes the same routing cost.}
            At each trigger $t = 0, \Delta, 2\Delta, \ldots$, ALNS applies
            destroy--repair operators to minimise
            $\alpha_1 C^{op} + \alpha_2 C^{wait} + \alpha_3 C^{walk} + \alpha_4 C^{IVT}$
            over the active accepted set $\mathcal{R}^{\text{act}}$
            (Equation~\ref{eq:objective} with $z_r = 1$ for all active requests).
    \end{enumerate}
    ```

    Then the `\subsubsection{Candidate Generation}` heading follows unchanged.
  </action>
  <verify>
    <automated>python -c "
with open('paper/sections/algorithm.tex', encoding='utf-8') as f:
    tex = f.read()
assert 'eq:alns-objective' in tex, 'eq:alns-objective label not found'
assert 'Online Objective' in tex, 'Online Objective paragraph not found'
assert 'Bernoulli sampling occurs after bundle selection' in tex, 'Design choice 1 not found'
assert 'does not enter bundle selection' in tex or 'does not enter' in tex, 'Design choice 2 not found'
assert 'Candidate Generation' in tex, 'Candidate Generation subsubsection accidentally deleted'
print('algorithm.tex FORM-02: all assertions passed')
"
    </automated>
  </verify>
  <done>eq:alns-objective equation and three enumerated design-choice bullets exist in algorithm.tex; Candidate Generation subsubsection is unchanged.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | model.tex table insertion | mitigate | Pure append between two known anchors; verify with grep checks |
| T-11-02 | Tampering | algorithm.tex paragraph insertion | mitigate | Pure insertion; existing subsubsection structure unchanged |
</threat_model>

<verification>
1. `grep "tab:timing-diagram" paper/sections/model.tex` returns label and cross-reference
2. `grep "Bernoulli" paper/sections/model.tex` returns ≥2 hits
3. `grep "eq:alns-objective" paper/sections/algorithm.tex` returns the equation label
4. `grep "Bernoulli sampling occurs after" paper/sections/algorithm.tex` returns enumerated item
5. `grep "Online Objective" paper/sections/algorithm.tex` returns paragraph header
</verification>

<success_criteria>
- tab:timing-diagram table in model.tex has 5 rows including Bernoulli sampling row for Layer 2
- Cross-reference sentence "Table~\ref{tab:timing-diagram} details..." present in model.tex
- eq:alns-objective equation in algorithm.tex with E[ΔC|b,x_r] = P_accept × ΔC_routing
- Three enumerated design choices: Bernoulli after selection, Γ excluded from bundle selection, rolling horizon minimizes routing cost
- No existing text deleted from either file
</success_criteria>

<output>
After completion, create `.planning/phases/11-formalization-policy-reframing/11-01-SUMMARY.md`
</output>
