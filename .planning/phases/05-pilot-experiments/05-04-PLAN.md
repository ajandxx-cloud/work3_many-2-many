---
phase: 05-pilot-experiments
plan: 05-04
type: execute
wave: 4
depends_on: [05-03]
files_modified:
  - experiments/phase05_coverage_smoke.py
  - tests/test_phase05_pilot.py
  - .planning/phases/05-pilot-experiments/05_BUG_LEDGER.csv
  - .planning/phases/05-pilot-experiments/05_PILOT_RESULTS.md
  - .planning/STATE.md
  - results/pilot/phase05/matched_coverage_pilot.csv
  - results/pilot/phase05/fixed_accepted_set_smoke.json
autonomous: true
gap_closure: true
requirements: [REP-03]
must_haves:
  truths:
    - "BUG-05-001 and BUG-05-002 are closed by matched-coverage target logic that passes tolerance at scale=20 seeds 42, 43, and 44 without loosening tolerance=0.03."
    - "BUG-05-003 is closed by a fixed accepted-set smoke that retains at least one request for scale=20 seed=42 and runs a routing diagnostic."
    - "05_BUG_LEDGER.csv records fixed rerun results and has no unresolved blocks_phase6=true rows after rerun."
    - "05_PILOT_RESULTS.md reports the gap-closure rerun as readiness-only and keeps the no-formal-claims boundary."
  artifacts:
    - path: "experiments/phase05_coverage_smoke.py"
      provides: "Corrected matched-coverage and fixed accepted-set diagnostic logic"
      contains: "run_matched_coverage_pilot"
    - path: "tests/test_phase05_pilot.py"
      provides: "Regression coverage for BUG-05-001 through BUG-05-003"
      contains: "matched coverage and fixed accepted-set tests"
    - path: ".planning/phases/05-pilot-experiments/05_BUG_LEDGER.csv"
      provides: "Closed blocker ledger"
      contains: "BUG-05-001"
    - path: ".planning/phases/05-pilot-experiments/05_PILOT_RESULTS.md"
      provides: "Updated pilot gate report"
      contains: "Gap Closure Rerun"
  key_links:
    - from: "experiments/phase05_coverage_smoke.py"
      to: "results/pilot/phase05/matched_coverage_pilot.csv"
      via: "run_matched_coverage_pilot output writer"
      pattern: "matched_coverage_pilot.csv"
    - from: "experiments/phase05_coverage_smoke.py"
      to: "results/pilot/phase05/fixed_accepted_set_smoke.json"
      via: "run_fixed_accepted_set_smoke output writer"
      pattern: "fixed_accepted_set_smoke.json"
    - from: ".planning/phases/05-pilot-experiments/05_BUG_LEDGER.csv"
      to: ".planning/phases/05-pilot-experiments/05_PILOT_RESULTS.md"
      via: "same blocker IDs and rerun outcomes"
      pattern: "BUG-05-00[123]"
---

<objective>
Close the Phase 5 verification blockers from `05-VERIFICATION.md` by repairing the pilot coverage-control diagnostics, rerunning the smoke checks, and updating the readiness ledger/report.

Purpose: Phase 6 must not start while matched-coverage tolerance and fixed accepted-set diagnostics remain unresolved.
Output: Corrected diagnostic code/tests, regenerated pilot diagnostic artifacts, closed bug ledger rows, and an updated no-claims gate report.
</objective>

<execution_context>
@C:/Users/39583/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/39583/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-pilot-experiments/05-CONTEXT.md
@.planning/phases/05-pilot-experiments/05-RESEARCH.md
@.planning/phases/05-pilot-experiments/05-PATTERNS.md
@.planning/phases/05-pilot-experiments/05-VERIFICATION.md
@.planning/phases/05-pilot-experiments/05_BUG_LEDGER.csv
@.planning/phases/05-pilot-experiments/05_PILOT_RESULTS.md
@.planning/phases/05-pilot-experiments/05-01-SUMMARY.md
@.planning/phases/05-pilot-experiments/05-02-SUMMARY.md
@.planning/phases/05-pilot-experiments/05-03-SUMMARY.md
@.planning/phases/02-experimental-contract-and-metric-standardization/02_COVERAGE_CONFOUNDING_PLAN.md
@experiments/phase05_coverage_smoke.py
@experiments/endogenous_matched_coverage.py
@experiments/milp_gap.py
@experiments/phase05_pilot.py
@experiments/pilot_validation.py
@tests/test_phase05_pilot.py

<interfaces>
Current callable contracts the executor must preserve:

- `run_matched_coverage_pilot(results_dir=PILOT_RESULTS_DIR, scale=PILOT_SCALE, seeds=PILOT_SEEDS, tolerance=0.03) -> dict`
- `run_fixed_accepted_set_smoke(results_dir=PILOT_RESULTS_DIR, scale=PILOT_SCALE, seed=42, include_milp=False) -> dict`
- `MAIN_BEHAVIORAL_METHOD_LABELS` contains exactly the four Phase 5 behavioral-main method labels.
- `validate_phase05_outputs(results_dir, expected_seeds, expected_method_labels, expected_scale) -> dict`
</interfaces>
</context>

<source_audit>
| Source | ID | Requirement or Decision | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | Phase 5 | Run small experiments to detect bugs and sanity-check results without creating final claims | 05-04 | COVERED | Closes only pilot readiness blockers; no formal claims |
| REQ | REP-03 | Failed, timeout, and infeasible runs are durable rows with status/error/runtime/config/seed/method/scenario | 05-04 | COVERED | Ledger/report rerun keeps durable blocker records |
| RESEARCH | Matched coverage | Pilot matched-coverage tolerance failures block Phase 6 | 05-04 Task 1 | COVERED | Fix target/cap semantics; keep tolerance=0.03 |
| RESEARCH | Fixed accepted set | Fixed accepted-set smoke must produce a retained set and routing diagnostic | 05-04 Task 2 | COVERED | Uses common served intersection first, then common serviceable intersection per Phase 2 contract |
| RESEARCH | Testing | Extend `tests/test_phase05_pilot.py` for diagnostics | 05-04 Tasks 1-2 | COVERED | Adds regression tests for all three bug IDs |
| CONTEXT | D-01 to D-14 | Main pilot matrix, schemas, no-claims report, bug ledger | Existing 05-01 and 05-03; 05-04 Task 3 preserves | COVERED | Existing plans are preserved; report/ledger update keeps no-claims boundary |
| CONTEXT | D-15 | Matched-coverage runs as small-sample readiness diagnostic | 05-04 Task 1 | COVERED | Uses scale=20 and seeds 42/43/44 |
| CONTEXT | D-16 | Matched-coverage tolerance failure is a Phase 5 blocker | 05-04 Task 1 and Task 3 | COVERED | Does not loosen tolerance; ledger remains blocking if rerun still fails |
| CONTEXT | D-17 | Fixed accepted-set smoke builds common accepted/serviceable request intersection and runs routing diagnostic | 05-04 Task 2 | COVERED | Target state is non-empty retained set for seed 42 |
| CONTEXT | D-18 | Optional MILP/Gurobi diagnostics are non-blocking | 05-04 Task 2 | COVERED | Preserves no-Gurobi non-blocking behavior |
</source_audit>

<tasks>
  <task type="auto" tdd="true">
    <name>Task 1: Close BUG-05-001 and BUG-05-002 with corrected matched-coverage target semantics</name>
    <files>experiments/phase05_coverage_smoke.py, tests/test_phase05_pilot.py</files>
    <read_first>
      - ".planning/phases/05-pilot-experiments/05-VERIFICATION.md"
      - ".planning/phases/05-pilot-experiments/05_BUG_LEDGER.csv"
      - ".planning/phases/05-pilot-experiments/05-CONTEXT.md"
      - ".planning/phases/02-experimental-contract-and-metric-standardization/02_COVERAGE_CONFOUNDING_PLAN.md"
      - "experiments/phase05_coverage_smoke.py"
      - "experiments/endogenous_matched_coverage.py"
      - "experiments/phase05_pilot.py"
      - "tests/test_phase05_pilot.py"
    </read_first>
    <behavior>
      - Test 1: Running `run_matched_coverage_pilot(tmp_path, scale=20, seeds=[42, 43, 44], tolerance=0.03)` returns `passed is True`.
      - Test 2: The generated capped-control rows for seeds 42, 43, and 44 each have `abs_gap <= tolerance`.
      - Test 3: A monkeypatched unreachable target still returns `passed is False` and writes a durable failed diagnostic row instead of hiding the failure.
    </behavior>
    <action>
      Replace the current mean-target-only cap in `run_matched_coverage_pilot` with per-seed integer target-count semantics. For each seed, run `FullModel` and record the original FullModel served request count/share. Compute the common attainable target count as the minimum of the FullModel served count and the uncapped deterministic DoorToDoorCapped serviceable count for the same seed. Use `cap_share = target_count / scale` for that seed, not the current cross-seed mean `target_share`. Persist both the original FullModel target and the adjusted target in `matched_coverage_pilot.csv` with columns named `original_fullmodel_served_share`, `target_count`, `target_basis`, and `target_adjusted`. Keep `tolerance=0.03`; do not loosen it and do not drop seed 42 or seed 44. A capped row passes only when `abs(achieved_served_share - target_served_share) <= tolerance`; otherwise it remains a blocking failed row with `detailed_reason` explaining the target and achieved counts.
    </action>
    <verify>
      <automated>$env:PYTHONPATH='src'; pytest tests/test_phase05_pilot.py -q</automated>
      <automated>$env:PYTHONPATH='src'; python -m experiments.phase05_coverage_smoke</automated>
    </verify>
    <acceptance_criteria>
      - "`matched_coverage_pilot.csv` contains capped-control rows for seeds 42, 43, and 44."
      - "No capped-control row has `status == 'failed'` for the default scale=20 seeds 42, 43, and 44 rerun."
      - "Every capped-control row has `abs_gap <= tolerance` with `tolerance == 0.03`."
      - "The CSV includes `original_fullmodel_served_share`, `target_count`, `target_basis`, and `target_adjusted`."
      - "Tests cover the prior seed 42 and seed 44 failures by asserting they pass under the corrected target semantics."
    </acceptance_criteria>
    <done>BUG-05-001 and BUG-05-002 no longer reproduce under the default Phase 5 coverage-smoke command, and the tolerance contract remains at 0.03.</done>
  </task>

  <task type="auto" tdd="true">
    <name>Task 2: Close BUG-05-003 with serviceable fixed-set fallback and routing diagnostic</name>
    <files>experiments/phase05_coverage_smoke.py, tests/test_phase05_pilot.py</files>
    <read_first>
      - ".planning/phases/05-pilot-experiments/05-VERIFICATION.md"
      - ".planning/phases/05-pilot-experiments/05_BUG_LEDGER.csv"
      - ".planning/phases/05-pilot-experiments/05-CONTEXT.md"
      - ".planning/phases/02-experimental-contract-and-metric-standardization/02_COVERAGE_CONFOUNDING_PLAN.md"
      - "experiments/phase05_coverage_smoke.py"
      - "experiments/milp_gap.py"
      - "experiments/phase05_pilot.py"
      - "experiments/pilot_validation.py"
      - "tests/test_phase05_pilot.py"
    </read_first>
    <behavior>
      - Test 1: If the common served intersection is non-empty, the fixed-set smoke uses it and runs `GreedyInsertionBaseline`.
      - Test 2: If the common served intersection is empty but the common serviceable intersection is non-empty, the smoke uses the serviceable set and runs `GreedyInsertionBaseline`.
      - Test 3: The default `run_fixed_accepted_set_smoke(scale=20, seed=42)` writes JSON with `retained_request_count > 0`, `status == 'passed'`, and `routing_status == 'completed'`.
      - Test 4: Optional MILP `no_gurobi` remains non-blocking and does not change fixed-set pass/fail status by itself.
    </behavior>
    <action>
      Keep `_common_served_request_ids` as the first construction rule. Add a second construction rule that builds the common serviceable request-id intersection from `utility_components.csv`: for each of the four main behavioral methods, include request IDs whose utility/status rows show an actual service offer, meaning `status` is `served` or `choice_rejected`; exclude feasibility-rejected rows. In `run_fixed_accepted_set_smoke`, use the served intersection when non-empty; otherwise use the serviceable intersection. Persist `served_intersection_count`, `serviceable_intersection_count`, `construction_rule`, `retained_request_count`, and `retained_share` in `fixed_accepted_set_smoke.json`. For the default scale=20 seed=42 case, the target state is a non-empty retained set and a completed `GreedyInsertionBaseline` routing diagnostic. Preserve `include_milp` behavior: `no_gurobi` stays non-blocking and must not turn a completed greedy fixed-set smoke into a blocker.
    </action>
    <verify>
      <automated>$env:PYTHONPATH='src'; pytest tests/test_phase05_pilot.py -q</automated>
      <automated>$env:PYTHONPATH='src'; python -m experiments.phase05_coverage_smoke</automated>
    </verify>
    <acceptance_criteria>
      - "`fixed_accepted_set_smoke.json` includes `served_intersection_count`, `serviceable_intersection_count`, `construction_rule`, `retained_request_count`, and `retained_share`."
      - "For default scale=20 seed=42, `retained_request_count > 0`."
      - "For default scale=20 seed=42, `status == 'passed'` and `routing_status == 'completed'`."
      - "The JSON keeps `evidence_family == 'algorithm_diagnostic'` and `diagnostic_role == 'fixed_accepted_set_smoke'`."
      - "Tests assert an empty served intersection can still complete through the serviceable intersection when serviceable request IDs exist."
    </acceptance_criteria>
    <done>BUG-05-003 no longer reproduces: the fixed accepted-set smoke does not skip routing for scale=20 seed=42.</done>
  </task>

  <task type="auto">
    <name>Task 3: Rerun diagnostics and close the Phase 5 blocker ledger/report</name>
    <files>.planning/phases/05-pilot-experiments/05_BUG_LEDGER.csv, .planning/phases/05-pilot-experiments/05_PILOT_RESULTS.md, .planning/STATE.md, results/pilot/phase05/matched_coverage_pilot.csv, results/pilot/phase05/fixed_accepted_set_smoke.json</files>
    <read_first>
      - ".planning/phases/05-pilot-experiments/05-VERIFICATION.md"
      - ".planning/phases/05-pilot-experiments/05_BUG_LEDGER.csv"
      - ".planning/phases/05-pilot-experiments/05_PILOT_RESULTS.md"
      - ".planning/STATE.md"
      - "results/pilot/phase05/matched_coverage_pilot.csv"
      - "results/pilot/phase05/fixed_accepted_set_smoke.json"
      - "experiments/phase05_coverage_smoke.py"
      - "experiments/pilot_validation.py"
    </read_first>
    <action>
      Run the focused tests and the Phase 5 coverage-smoke module after Tasks 1 and 2. Update `.planning/phases/05-pilot-experiments/05_BUG_LEDGER.csv` in place for `BUG-05-001`, `BUG-05-002`, and `BUG-05-003`: set `fix_status=fixed`, set `rerun_result` to the concrete rerun outcome, and set `blocks_phase6=false` only when the regenerated matched-coverage CSV has no default capped-control failures and the regenerated fixed-set JSON has a non-empty retained set with completed routing. If a new issue appears, append a new `BUG-05-00X` row instead of deleting evidence. Update `05_PILOT_RESULTS.md` with a `Gap Closure Rerun` section that lists commands, artifact paths, corrected matched-coverage target semantics, fixed-set construction rule, and the closed blocker IDs. Keep the report's no-formal-claims language. Update `.planning/STATE.md` so it no longer says Phase 6 is blocked by BUG-05-001 through BUG-05-003 when the ledger has no unresolved blockers; do not mark formal experiments as started.
    </action>
    <verify>
      <automated>$env:PYTHONPATH='src'; pytest tests/test_phase05_pilot.py tests/test_runner.py tests/test_metrics.py tests/test_variants.py -q</automated>
      <automated>$env:PYTHONPATH='src'; python -m experiments.phase05_coverage_smoke</automated>
      <automated>$env:PYTHONPATH='src'; python -c "import csv; rows=list(csv.DictReader(open('.planning/phases/05-pilot-experiments/05_BUG_LEDGER.csv', newline=''))); unresolved=[r for r in rows if r.get('blocks_phase6','').lower()=='true' and r.get('fix_status') not in {'fixed','not_applicable','redesigned_non_blocking'}]; assert not unresolved, unresolved"</automated>
    </verify>
    <acceptance_criteria>
      - "`05_BUG_LEDGER.csv` retains rows for `BUG-05-001`, `BUG-05-002`, and `BUG-05-003` with `fix_status=fixed` and `blocks_phase6=false` after passing rerun."
      - "`05_PILOT_RESULTS.md` contains a `Gap Closure Rerun` section and references all three bug IDs."
      - "`05_PILOT_RESULTS.md` still states that Phase 5 pilot outputs make no formal claims."
      - "`.planning/STATE.md` references Phase 5 gap closure and does not list BUG-05-001 through BUG-05-003 as active blockers when the ledger is closed."
      - "`pytest tests/test_phase05_pilot.py tests/test_runner.py tests/test_metrics.py tests/test_variants.py -q` passes with `PYTHONPATH=src`."
    </acceptance_criteria>
    <done>The Phase 5 gap closure artifacts show no unresolved Phase 6 blockers from BUG-05-001, BUG-05-002, or BUG-05-003, and verification can be rerun cleanly.</done>
  </task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Pilot diagnostics -> Phase 6 planning | Readiness artifacts decide whether formal experiment planning may proceed. |
| Generated CSV/JSON -> planning reports | Persisted outputs become project-state evidence and must not hide failed diagnostics. |
| Optional solver dependency -> diagnostic status | Gurobi availability is external and must not block Phase 5 by itself. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-05-04-01 | Tampering | `matched_coverage_pilot.csv` | mitigate | Keep tolerance at 0.03 and assert regenerated rows pass by corrected target semantics, not by threshold loosening. |
| T-05-04-02 | Repudiation | `05_BUG_LEDGER.csv` | mitigate | Preserve existing bug IDs and append new rows for new failures instead of deleting prior blocker evidence. |
| T-05-04-03 | Information Integrity | `fixed_accepted_set_smoke.json` | mitigate | Persist construction rule and both served/serviceable intersection counts so retained-set provenance is auditable. |
| T-05-04-04 | Denial of Service | Optional MILP path | accept/mitigate | Preserve `no_gurobi` as non-blocking and use GreedyInsertionBaseline as the required fixed-set routing diagnostic. |
| T-05-04-SC | Tampering | package installs | mitigate | This plan requires no package-manager installs; executor must not add dependencies. |
</threat_model>

<verification>
Overall checks:

1. `$env:PYTHONPATH='src'; pytest tests/test_phase05_pilot.py tests/test_runner.py tests/test_metrics.py tests/test_variants.py -q`
2. `$env:PYTHONPATH='src'; python -m experiments.phase05_coverage_smoke`
3. `$env:PYTHONPATH='src'; python -c "import pandas as pd; df=pd.read_csv('results/pilot/phase05/matched_coverage_pilot.csv'); capped=df[df['method_label'].eq('DoorToDoor_Capped_MatchedCoverage')]; assert not capped.empty; assert (capped['abs_gap'].astype(float) <= capped['tolerance'].astype(float)).all(); print(capped[['seed','target_served_share','achieved_served_share','abs_gap','status']].to_string(index=False))"`
4. `$env:PYTHONPATH='src'; python -c "import json; p=json.load(open('results/pilot/phase05/fixed_accepted_set_smoke.json')); assert p['retained_request_count'] > 0, p; assert p['routing_status'] == 'completed', p; print(p)"`
5. `$env:PYTHONPATH='src'; python -c "import csv; rows=list(csv.DictReader(open('.planning/phases/05-pilot-experiments/05_BUG_LEDGER.csv', newline=''))); unresolved=[r for r in rows if r.get('blocks_phase6','').lower()=='true' and r.get('fix_status') not in {'fixed','not_applicable','redesigned_non_blocking'}]; assert not unresolved, unresolved; print('unresolved blockers:', len(unresolved))"`
</verification>

<success_criteria>
- BUG-05-001 and BUG-05-002 are closed without changing the Phase 5 tolerance from 0.03.
- BUG-05-003 is closed with a non-empty retained request set and completed routing diagnostic for scale=20 seed=42.
- `05_BUG_LEDGER.csv` preserves the bug history and records fixed rerun results.
- `05_PILOT_RESULTS.md` reports readiness status without formal or method-superiority claims.
- Phase 6 readiness is no longer blocked by the three Phase 5 verification gaps.
</success_criteria>

<output>
Create `.planning/phases/05-pilot-experiments/05-04-SUMMARY.md` when done.
</output>
