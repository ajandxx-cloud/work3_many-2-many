---
phase: 02
plan: 02-02
title: Build occurrence-level claim ledger with provenance and denominator fields
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - .planning/milestones/tr_e_claim_ready/03_CLAIM_LEDGER.md
autonomous: true
requirements: [CLAI-02, CLAI-03, CLAI-04]
user_setup: []
must_haves:
  truths:
    - "Every current and planned manuscript claim occurrence in `manuscript/main.tex` and all `manuscript/sections/*.tex` has a traceable ledger row."
    - "Every target manuscript file is represented in a coverage inventory with reviewed line ranges and claim IDs or an explicit no-claim note."
    - "Every ledger row has mandatory provenance fields and distinguishes source, script, command, formula, numerator, denominator, evidence role, allowed sentence, and prohibited sentence."
    - "Allowed replacement sentences use Phase 4 placeholders for unverified numerical content."
  artifacts:
    - path: ".planning/milestones/tr_e_claim_ready/03_CLAIM_LEDGER.md"
      provides: "Occurrence-level claim ledger and target-file coverage inventory for Phase 3 prose edits and Phase 4 numerical provenance"
      contains: "claim_id"
  key_links:
    - from: ".planning/milestones/tr_e_claim_ready/03_CLAIM_LEDGER.md"
      to: "manuscript/main.tex and manuscript/sections/*.tex"
      via: "source_path and manuscript_location per claim occurrence"
      pattern: "manuscript/sections/"
    - from: ".planning/milestones/tr_e_claim_ready/03_CLAIM_LEDGER.md"
      to: "results/formal/phase06/"
      via: "source_path for formal evidence claims"
      pattern: "results/formal/phase06/"
    - from: ".planning/milestones/tr_e_claim_ready/03_CLAIM_LEDGER.md"
      to: "experiments/formal_validation.py and experiments/metrics.py"
      via: "metric_formula, numerator, and denominator fields"
      pattern: "vkm_per_served_trip|served_share|choice_rejection_rate"
---

<objective>
Create the occurrence-level claim ledger that downstream manuscript and numerical
provenance phases will execute against.

Purpose: make every current and planned manuscript claim traceable, classify its
evidence role, and prevent final numerical injection before Phase 4.
Output: `.planning/milestones/tr_e_claim_ready/03_CLAIM_LEDGER.md`.
</objective>

<execution_context>
@C:/Users/39583/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/39583/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-tr-e-positioning-lock-and-claim-ledger/02-CONTEXT.md
@.planning/phases/02-tr-e-positioning-lock-and-claim-ledger/02-RESEARCH.md
@.planning/phases/02-tr-e-positioning-lock-and-claim-ledger/02-PATTERNS.md
@.planning/milestones/tr_e_claim_ready/00_MILESTONE_PLAN.md
@.planning/milestones/tr_e_claim_ready/01_REPO_AND_EVIDENCE_AUDIT.md
@.planning/milestones/tr_e_claim_ready/02_TR_E_POSITIONING_LOCK.md
@.planning/milestones/tr_e_claim_ready/04_MANUSCRIPT_ACTION_PLAN.md
@experiments/formal_validation.py
@experiments/metrics.py
@experiments/formal_statistics.py
@results/formal/phase06/phase06_result_manifest.json
@results/formal/phase06/phase06_verification_report.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the ledger schema, enums, and evidence-role map</name>
  <files>.planning/milestones/tr_e_claim_ready/03_CLAIM_LEDGER.md</files>
  <read_first>
  - `AGENTS.md`
  - `.planning/PROJECT.md`
  - `.planning/REQUIREMENTS.md`
  - `.planning/ROADMAP.md`
  - `.planning/STATE.md`
  - `.planning/phases/02-tr-e-positioning-lock-and-claim-ledger/02-CONTEXT.md`
  - `.planning/phases/02-tr-e-positioning-lock-and-claim-ledger/02-RESEARCH.md`
  - `.planning/phases/02-tr-e-positioning-lock-and-claim-ledger/02-PATTERNS.md`
  - `.planning/milestones/tr_e_claim_ready/00_MILESTONE_PLAN.md`
  - `.planning/milestones/tr_e_claim_ready/01_REPO_AND_EVIDENCE_AUDIT.md`
  - `.planning/milestones/tr_e_claim_ready/02_TR_E_POSITIONING_LOCK.md`
  - `.planning/milestones/tr_e_claim_ready/04_MANUSCRIPT_ACTION_PLAN.md`
  - `.planning/milestones/tr_e_claim_ready/03_CLAIM_LEDGER.md` if it already exists
  </read_first>
  <action>
  Create `.planning/milestones/tr_e_claim_ready/03_CLAIM_LEDGER.md` with these sections: `Purpose`, `Scope`, `Ledger Rules`, `Evidence Role Enum`, `Action Enum`, `Phase 4 Placeholder Rules`, `Metric Formula Reference`, `Manuscript Coverage Inventory`, and `Claim Ledger`. Follow the `02-PATTERNS.md` claim-ledger analog assignment: use the canonical manuscript-source inventory and evidence-role map shape from `.planning/milestones/tr_e_claim_ready/01_REPO_AND_EVIDENCE_AUDIT.md`; use the stable-ID/status-field style from `experiments/formal_validation.py`; use the formal table generation, manifest, and closeout-command patterns from `experiments/formal_statistics.py`; use denominator formulas from `experiments/formal_validation.py` and `experiments/metrics.py`. Implement D-01 through D-07 by using one row per manuscript claim occurrence, grouping related rows with `claim_family_id`, covering every current manuscript claim plus planned safe replacement claim, and using non-numeric safe wording or placeholders. Add a `Manuscript Coverage Inventory` table with columns `reviewed_path`, `reviewed_line_range`, and `claim_ids_or_no_claim_note`; it must contain one row for `manuscript/main.tex` and one row for every current `manuscript/sections/*.tex` file. The `Claim Ledger` table header must be exactly: `claim_id`, `claim_family_id`, `manuscript_location`, `current_sentence`, `claim_type`, `comparison`, `metric`, `reported_value`, `source_path`, `supporting_source_path`, `script_path`, `generation_command`, `metric_formula`, `numerator`, `denominator`, `evidence_role`, `phase4_status`, `action`, `allowed_sentence`, `prohibited_sentence`, `notes`. Implement D-38 through D-41 by using `not_applicable` for non-numeric `metric_formula`, `numerator`, and `denominator`; using `not_applicable_manual_text` for manual positioning/scope text in `script_path` and `generation_command`; and defining the evidence-role enum as `positioning`, `mechanism_scope`, `primary_behavioral`, `diagnostic_matched_coverage`, `diagnostic_fixed_accepted_set`, `robustness_sensitivity`, `equity_type_heterogeneity`, `algorithm_diagnostic`, `limitation`, and `package_consistency`.
  </action>
  <verify>
    <automated>$content = Get-Content -Raw ".planning\milestones\tr_e_claim_ready\03_CLAIM_LEDGER.md"; $required = @("source_path","script_path","generation_command","metric_formula","numerator","denominator","evidence_role","allowed_sentence","prohibited_sentence","claim_id","claim_family_id","manuscript_location","phase4_status","action"); $missing = $required | Where-Object { $content -notmatch "\|\s*$($_)\s*\|" }; if ($missing) { throw "Missing ledger columns: $($missing -join ', ')" }; rg -n "positioning|mechanism_scope|primary_behavioral|diagnostic_matched_coverage|diagnostic_fixed_accepted_set|robustness_sensitivity|equity_type_heterogeneity|algorithm_diagnostic|limitation|package_consistency" .planning\milestones\tr_e_claim_ready\03_CLAIM_LEDGER.md</automated>
  </verify>
  <acceptance_criteria>
  - The ledger file exists with the exact table header listed in the action.
  - The mandatory provenance columns `source_path`, `script_path`, `generation_command`, `metric_formula`, `numerator`, `denominator`, `evidence_role`, `allowed_sentence`, and `prohibited_sentence` are present.
  - The execution fields `claim_id`, `claim_family_id`, `manuscript_location`, `current_sentence`, `claim_type`, `comparison`, `metric`, `reported_value`, `phase4_status`, and `action` are present.
  - The file defines all evidence-role enum values and action values `retain_with_verification`, `replace_non_numeric`, `delete`, `downgrade_to_diagnostic`, `move_to_limitation`, and `phase4_verify_number`.
  - The file states that `[PHASE4_VERIFIED_VALUE]`, `[PHASE4_VERIFIED_CI]`, `[PHASE4_VERIFIED_TABLE]`, and `[PHASE4_VERIFIED_FIGURE]` must be used for unverified numerical content.
  </acceptance_criteria>
  <done>The claim ledger schema and enum contract are established before occurrence rows are populated.</done>
</task>

<task type="auto">
  <name>Task 2: Populate manuscript occurrence rows from current claims</name>
  <files>.planning/milestones/tr_e_claim_ready/03_CLAIM_LEDGER.md</files>
  <read_first>
  - `.planning/milestones/tr_e_claim_ready/03_CLAIM_LEDGER.md`
  - `.planning/milestones/tr_e_claim_ready/02_TR_E_POSITIONING_LOCK.md`
  - `.planning/milestones/tr_e_claim_ready/01_REPO_AND_EVIDENCE_AUDIT.md`
  - `.planning/milestones/tr_e_claim_ready/04_MANUSCRIPT_ACTION_PLAN.md`
  - `manuscript/main.tex`
  - `manuscript/sections/abstract.tex`
  - `manuscript/sections/intro.tex`
  - `manuscript/sections/literature.tex`
  - `manuscript/sections/model.tex`
  - `manuscript/sections/algorithm.tex`
  - `manuscript/sections/experiments.tex`
  - `manuscript/sections/policy.tex`
  - `manuscript/sections/conclusion.tex`
  </read_first>
  <action>
  Populate `03_CLAIM_LEDGER.md` with occurrence-level rows for every current manuscript claim and every planned safe replacement claim per D-01 through D-05 and D-34. Full ledger scope is `manuscript/main.tex` and all current `manuscript/sections/*.tex`; do not include README, CLAUDE, cover letter, response-to-reviewers, or figure-script comment risks here unless the wording is reused in the manuscript. Follow the `02-PATTERNS.md` claim-source inventory analog from `.planning/milestones/tr_e_claim_ready/01_REPO_AND_EVIDENCE_AUDIT.md`: record locations as normalized forward-slash file paths with line ranges, and convert the manuscript source inventory into occurrence rows rather than only regex hits. Use stable IDs such as `C-001`, `C-002`, and family IDs such as `F-routing-intensity`, `F-coverage-tradeoff`, `F-policy-framing`, `F-gamma`, `F-beijing`, `F-milp`, and `F-diagnostic-evidence`. Use these scan commands to seed the rows, not to limit coverage: `rg -n "18\.3|29\.1|35\.0|0\.1216|%|vkm|Gini|percentage points|confidence|Table~|Figure~" manuscript/main.tex manuscript/sections/*.tex`; `rg -n "co-optimization|policy|Policy Implications|decision tool|dominates|dominance|outperform|outperforms|superior|Beijing|real-world|validation|Gamma|gamma|Pareto|MILP|exact|near-optimal|benchmark" manuscript/main.tex manuscript/sections/*.tex`. Manually review every target file line range. Fill the `Manuscript Coverage Inventory` so each reviewed file has line ranges and either the claim IDs found in those ranges or `no_claims_after_review`; absence of a regex hit is not sufficient. Treat each claim-bearing sentence, bullet, caption-like manuscript reference, or claim-bearing clause as one occurrence; split a sentence into multiple rows when different metrics, denominators, evidence roles, or actions apply. Implement D-42 through D-45 by giving every row a complete `allowed_sentence` and a concrete `prohibited_sentence`; for final numerical claims use placeholders rather than final values.
  </action>
  <verify>
    <automated>$ledger = (Get-Content -Raw ".planning\milestones\tr_e_claim_ready\03_CLAIM_LEDGER.md"); $targets = @("manuscript/main.tex") + (Get-ChildItem manuscript\sections\*.tex | Sort-Object Name | ForEach-Object { "manuscript/sections/$($_.Name)" }); $missingTargets = $targets | Where-Object { $ledger -notmatch [regex]::Escape($_) }; if ($missingTargets) { throw "Coverage inventory missing target files: $($missingTargets -join '; ')" }; $requiredInventory = @("reviewed_path","reviewed_line_range","claim_ids_or_no_claim_note"); $missingColumns = $requiredInventory | Where-Object { $ledger -notmatch "\|\s*$($_)\s*\|" }; if ($missingColumns) { throw "Missing coverage inventory columns: $($missingColumns -join ', ')" }; $hits = rg -n "18\.3|29\.1|35\.0|0\.1216|%|vkm|Gini|percentage points|confidence|Table~|Figure~" manuscript/main.tex manuscript/sections/*.tex | ForEach-Object { $parts = $_ -split ":", 3; (($parts[0] -replace "\\","/") + ":" + $parts[1]) }; $missingHits = $hits | Where-Object { $ledger -notmatch [regex]::Escape($_) }; if ($missingHits) { throw "Missing claim ledger locations: $($missingHits -join '; ')" }; rg -n "C-[0-9][0-9][0-9]|F-routing-intensity|F-coverage-tradeoff|F-gamma|F-beijing|F-milp|\[PHASE4_VERIFIED_VALUE\]|no_claims_after_review" .planning\milestones\tr_e_claim_ready\03_CLAIM_LEDGER.md</automated>
  </verify>
  <acceptance_criteria>
  - Every file in `manuscript/main.tex` plus current `manuscript/sections/*.tex` appears in the `Manuscript Coverage Inventory` with reviewed line ranges.
  - Each coverage inventory row has claim IDs or the explicit note `no_claims_after_review`; absence from regex scans is not treated as proof of no claims.
  - Each numerical hit returned by the first scan command appears as a `manuscript_location` in the ledger, using normalized forward-slash paths such as `manuscript/sections/abstract.tex:15`.
  - Ledger rows include both current unsafe sentences and planned safe replacement sentences.
  - Current old or risky values `18.3%`, `29.1%`, `35.0%`, and `0.1216` are recorded as current/reported/prohibited values with `phase4_status=phase4_verify_number`, not as final replacement values.
  - Diagnostic claims carry diagnostic evidence roles and diagnostic qualifiers in `allowed_sentence`.
  - Package-facing files are not over-ledgered; they are left for `05_BLOCKERS_AND_SAFE_CLAIMS.md` unless reused in manuscript text.
  </acceptance_criteria>
  <done>The manuscript claim occurrences and planned safe replacements are mapped at row level.</done>
</task>

<task type="auto">
  <name>Task 3: Complete provenance, formulas, denominators, and Phase 4 handoff fields</name>
  <files>.planning/milestones/tr_e_claim_ready/03_CLAIM_LEDGER.md</files>
  <read_first>
  - `.planning/milestones/tr_e_claim_ready/03_CLAIM_LEDGER.md`
  - `.planning/milestones/tr_e_claim_ready/01_REPO_AND_EVIDENCE_AUDIT.md`
  - `.planning/milestones/tr_e_claim_ready/02_TR_E_POSITIONING_LOCK.md`
  - `.planning/phases/02-tr-e-positioning-lock-and-claim-ledger/02-PATTERNS.md`
  - `experiments/formal_validation.py`
  - `experiments/metrics.py`
  - `experiments/formal_statistics.py`
  - `experiments/phase06_formal.py`
  - `experiments/phase06_coverage_controls.py`
  - `experiments/phase06_robustness.py`
  - `results/formal/phase06/phase06_result_manifest.json`
  - `results/formal/phase06/phase06_verification_report.json`
  - `results/formal/phase06/main_behavioral/validation_report.json`
  - `results/formal/phase06/tables/main_behavioral_table.csv`
  - `results/formal/phase06/tables/paired_differences.csv`
  - `results/formal/phase06/tables/paired_bootstrap_ci.csv`
  - `results/formal/phase06/tables/equity_type_summary.csv`
  - `results/formal/phase06/tables/matched_coverage_paired_differences.csv`
  - `results/formal/phase06/tables/fixed_accepted_set_paired_differences.csv`
  - `results/formal/phase06/tables/robustness_setting_summary.csv`
  - `results/formal/phase06/tables/supplementary_summary.csv`
  </read_first>
  <action>
  Fill every ledger row with hard provenance per D-06, D-38, D-39, D-40, and D-41. Follow the `02-PATTERNS.md` formal provenance analogs: cite table sources using the `experiments/formal_statistics.py` paired-difference, bootstrap-CI, manifest, and synthesis-validation patterns; use `experiments/formal_validation.py` and `experiments/metrics.py` denominator formulas; map manifest code-side roles to Phase 2 ledger roles exactly as documented in `02-PATTERNS.md`. For formal numerical claims, `source_path` must point under `results/formal/phase06/`, `script_path` must name the generating or validating script such as `experiments/formal_statistics.py`, `experiments/phase06_formal.py`, `experiments/phase06_coverage_controls.py`, or `experiments/phase06_robustness.py`, and `generation_command` must use a concrete command such as `$env:PYTHONPATH='src'; python -m experiments.formal_statistics --closeout --results-dir results/formal/phase06` or `$env:PYTHONPATH='src'; python -m experiments.formal_statistics --validate --results-dir results/formal/phase06`. For manual positioning, mechanism-scope, or limitation rows, use `script_path=not_applicable_manual_text` and `generation_command=not_applicable_manual_text`. For metric formulas, use the formal contract: `served_share = n_served / n_requests`; `vkm_per_original_request = vehicle_km / n_requests`; `vkm_per_served_trip = vehicle_km / n_served`; `behavioral_acceptance_rate = 1.0 - choice_rejection_rate`; `choice_rejection_rate = choice_rejected / n_requests`; `feasibility_rejection_rate = feasibility_rejected / n_requests`; `rejection_partition = served_share + choice_rejection_rate + feasibility_rejection_rate`. For non-numeric rows use `not_applicable` for formula, numerator, and denominator. Preserve evidence-role separation required by CLAI-04.
  </action>
  <verify>
    <automated>$content = Get-Content -Raw ".planning\milestones\tr_e_claim_ready\03_CLAIM_LEDGER.md"; if ($content -match "\|\s*\|\s*\|") { throw "Potential blank ledger cells remain" }; rg -n "results/formal/phase06/|not_applicable_manual_text|served_share = n_served / n_requests|vkm_per_served_trip = vehicle_km / n_served|behavioral_acceptance_rate = 1.0 - choice_rejection_rate|primary_behavioral|diagnostic_matched_coverage|diagnostic_fixed_accepted_set|robustness_sensitivity|equity_type_heterogeneity|algorithm_diagnostic|limitation" .planning\milestones\tr_e_claim_ready\03_CLAIM_LEDGER.md</automated>
  </verify>
  <acceptance_criteria>
  - Formal claim rows use `source_path` values under `results/formal/phase06/`; non-canonical root legacy, smoke, archive, and ad hoc paths are not used as formal evidence sources.
  - Manual/non-numeric rows use `not_applicable_manual_text` for `script_path` and `generation_command`, and `not_applicable` for `metric_formula`, `numerator`, and `denominator`.
  - Numerical rows use formulas and denominators from `experiments/formal_validation.py` and `experiments/metrics.py`, not free-text denominator guesses.
  - All evidence roles needed by CLAI-04 appear where relevant: `primary_behavioral`, `diagnostic_matched_coverage`, `diagnostic_fixed_accepted_set`, `robustness_sensitivity`, `equity_type_heterogeneity`, `algorithm_diagnostic`, and `limitation`.
  - The ledger functions as a Phase 3 and Phase 4 handoff: each row has `phase4_status`, `action`, `allowed_sentence`, and `prohibited_sentence`.
  </acceptance_criteria>
  <done>The claim ledger is complete enough for Phase 3 non-numeric manuscript edits and Phase 4 numerical provenance verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Manuscript source to ledger | Existing manuscript claims are transformed into execution rows without changing manuscript files. |
| Formal evidence tree to ledger | Formal and diagnostic evidence paths are cited as provenance without manually editing results. |
| Formula code to Markdown ledger | Metric denominator formulas are transcribed into governance text. |
| Phase 2 ledger to Phase 3/4 execution | Later phases rely on ledger rows to decide replacements and final numerical injection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | `source_path` fields | mitigate | Require formal claim `source_path` values under `results/formal/phase06/`; root legacy, smoke, archive, and ad hoc outputs are non-canonical unless explicitly labeled outside formal claims. |
| T-02-06 | Tampering | `allowed_sentence` fields | mitigate | Use `[PHASE4_VERIFIED_VALUE]`, `[PHASE4_VERIFIED_CI]`, `[PHASE4_VERIFIED_TABLE]`, and `[PHASE4_VERIFIED_FIGURE]` for evidence-dependent values. |
| T-02-07 | Repudiation | Occurrence-level coverage | mitigate | Use stable `claim_id`, `claim_family_id`, and `manuscript_location` so every later edit can be traced to a row. |
| T-02-08 | Information Disclosure | Local validation commands | accept | Commands are repository-relative and do not require secrets or solver license details. |
| T-02-09 | Spoofing | Diagnostic evidence promoted as primary | mitigate | Fixed evidence-role enum distinguishes primary, matched-coverage diagnostic, fixed-accepted-set diagnostic, robustness, equity/type, algorithm diagnostic, and limitation rows. |
</threat_model>

<verification>
- `$content = Get-Content -Raw ".planning\milestones\tr_e_claim_ready\03_CLAIM_LEDGER.md"; $required = @("source_path","script_path","generation_command","metric_formula","numerator","denominator","evidence_role","allowed_sentence","prohibited_sentence"); $missing = $required | Where-Object { $content -notmatch "\|\s*$($_)\s*\|" }; if ($missing) { throw "Missing ledger columns: $($missing -join ', ')" }`
- `$ledger = (Get-Content -Raw ".planning\milestones\tr_e_claim_ready\03_CLAIM_LEDGER.md"); $targets = @("manuscript/main.tex") + (Get-ChildItem manuscript\sections\*.tex | Sort-Object Name | ForEach-Object { "manuscript/sections/$($_.Name)" }); $missingTargets = $targets | Where-Object { $ledger -notmatch [regex]::Escape($_) }; if ($missingTargets) { throw "Coverage inventory missing target files: $($missingTargets -join '; ')" }; $requiredInventory = @("reviewed_path","reviewed_line_range","claim_ids_or_no_claim_note"); $missingColumns = $requiredInventory | Where-Object { $ledger -notmatch "\|\s*$($_)\s*\|" }; if ($missingColumns) { throw "Missing coverage inventory columns: $($missingColumns -join ', ')" }; $hits = rg -n "18\.3|29\.1|35\.0|0\.1216|%|vkm|Gini|percentage points|confidence|Table~|Figure~" manuscript/main.tex manuscript/sections/*.tex | ForEach-Object { $parts = $_ -split ":", 3; (($parts[0] -replace "\\","/") + ":" + $parts[1]) }; $missingHits = $hits | Where-Object { $ledger -notmatch [regex]::Escape($_) }; if ($missingHits) { throw "Missing claim ledger locations: $($missingHits -join '; ')" }`
- `rg -n "results/formal/phase06/|not_applicable_manual_text|\[PHASE4_VERIFIED_VALUE\]|primary_behavioral|diagnostic_matched_coverage|diagnostic_fixed_accepted_set|robustness_sensitivity|equity_type_heterogeneity|algorithm_diagnostic|limitation" .planning/milestones/tr_e_claim_ready/03_CLAIM_LEDGER.md`
- `git diff --name-only -- manuscript results src experiments analysis README.md CLAUDE.md pyproject.toml`
</verification>

<success_criteria>
- CLAI-02 is satisfied by `03_CLAIM_LEDGER.md` with mandatory provenance columns and execution fields.
- CLAI-03 is satisfied when every current and planned manuscript claim in `manuscript/main.tex` and all `manuscript/sections/*.tex` is represented by a ledger row, and numerical claims in abstract, introduction, experiments, policy/implications, and conclusion appear with Phase 4 provenance status.
- CLAI-04 is satisfied by fixed evidence-role classification for primary evidence, diagnostics, robustness/sensitivity, equity/type heterogeneity, algorithm diagnostics, and limitations.
- Phase 2 scope is preserved: manuscript, results, source, experiment, analysis, README, CLAUDE, and dependency files are not edited.
</success_criteria>

<output>
Create `.planning/phases/02-tr-e-positioning-lock-and-claim-ledger/02-02-SUMMARY.md` when done.
</output>
