---
phase: 06-academic-figures-visualization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - figures/scripts/fig04_baseline_comparison.py
  - figures/scripts/fig05_sensitivity.py
  - figures/fig04_baseline_comparison.pdf
  - figures/fig04_baseline_comparison.png
  - figures/fig05_sensitivity.pdf
  - figures/fig05_sensitivity.png
autonomous: true
requirements:
  - FIG-04
  - FIG-05

must_haves:
  truths:
    - "figures/fig04_baseline_comparison.pdf exists and shows a two-panel grouped bar chart: left panel = acceptance_rate_mean for 6 variants, right panel = vehicle_km_mean for 6 variants, with FullModel in dark blue and DoorToDoor in gray, and a −34.9% annotation on the vkm panel"
    - "figures/fig05_sensitivity.pdf exists and shows a two-panel line plot: left panel = acceptance_rate vs rho (FullModel and DoorToDoor lines, vertical dashed line at rho=1000), right panel = acceptance_rate vs n_vehicles (FullModel and DoorToDoor lines, vertical dashed line at n=20)"
    - "Both PDFs are double-column width (180mm) at 300 dpi"
    - "Data is read directly from results/metrics_table.csv, results/sensitivity_walk.csv, results/sensitivity_fleet.csv — no hardcoded values"
    - "Variant labels in FIG-04 match the paper table exactly: DoorToDoor, SingleSidedPickup, BidirectionalNoChoice, FullModel, AblationNoChoice, AblationNoRollingHorizon"
  artifacts:
    - path: "figures/fig04_baseline_comparison.pdf"
      provides: "Baseline comparison bar chart for fig:baseline-comparison label"
    - path: "figures/fig05_sensitivity.pdf"
      provides: "Sensitivity line plots for fig:sensitivity label"
    - path: "figures/scripts/fig04_baseline_comparison.py"
      provides: "Reproducible script reading results/metrics_table.csv"
    - path: "figures/scripts/fig05_sensitivity.py"
      provides: "Reproducible script reading results/sensitivity_walk.csv and results/sensitivity_fleet.csv"
  key_links:
    - from: "results/metrics_table.csv"
      to: "figures/fig04_baseline_comparison.pdf"
      via: "pd.read_csv('results/metrics_table.csv') in fig04 script"
      pattern: "read_csv.*metrics_table"
    - from: "results/sensitivity_walk.csv"
      to: "figures/fig05_sensitivity.pdf"
      via: "pd.read_csv('results/sensitivity_walk.csv') in fig05 script"
      pattern: "read_csv.*sensitivity_walk"
    - from: "results/sensitivity_fleet.csv"
      to: "figures/fig05_sensitivity.pdf"
      via: "pd.read_csv('results/sensitivity_fleet.csv') in fig05 script"
      pattern: "read_csv.*sensitivity_fleet"
---

<objective>
Produce the two data-driven figures (FIG-04, FIG-05) by reading the Phase 3/4 CSV results and generating publication-quality charts with matplotlib. These figures directly support the paper's main results and sensitivity analysis sections.

Purpose: Visualize the baseline comparison (6 variants × acceptance rate + vkm) and sensitivity sweeps (walking tolerance ρ and fleet size) that the experiments.tex narrative references.
Output: Two PDF files + two PNG fallbacks in figures/, plus two reproducible Python scripts in figures/scripts/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-academic-figures-visualization/06-CONTEXT.md

<!-- Data files — read these to understand exact column names before writing scripts -->
@results/metrics_table.csv
@results/sensitivity_walk.csv
@results/sensitivity_fleet.csv

<!-- Paper section that references these figures -->
@paper/sections/experiments.tex
</context>

<interfaces>
<!-- Exact CSV column names the executor MUST use — extracted from the data files -->

From results/metrics_table.csv:
Columns: variant, acceptance_rate_mean, acceptance_rate_std, vehicle_km_mean, vehicle_km_std,
         avg_wait_mean, avg_wait_std, p95_wait_mean, p95_wait_std, avg_walk_mean, avg_walk_std,
         avg_ivt_mean, avg_ivt_std, detour_ratio_mean, detour_ratio_std,
         fairness_index_mean, fairness_index_std, cpu_time_mean, cpu_time_std

Variant values (exact strings): AblationNoChoice, AblationNoRollingHorizon,
BidirectionalNoChoice, DoorToDoor, FullModel, SingleSidedPickup

Key values for annotation:
- FullModel: acceptance_rate_mean=0.2082, vehicle_km_mean=628.51
- DoorToDoor: acceptance_rate_mean=0.6096, vehicle_km_mean=2603.71
- vkm per unit acceptance: FullModel=2383.85, DoorToDoor=3662.33 → −34.9% gain

From results/sensitivity_walk.csv:
Columns: rho, variant, acceptance_rate, vehicle_km, avg_walk, avg_wait, density_tier
Filter to density_tier='standard' for the main sweep.
rho values: 200, 300, 400, 500, 700, 1000
FullModel acceptance at rho=1000: 0.09; DoorToDoor: 0.58 (constant)

From results/sensitivity_fleet.csv:
Columns: n_vehicles, variant, acceptance_rate, vehicle_km, avg_wait
n_vehicles values: 5, 10, 15, 20, 25, 30
FullModel acceptance peaks at n_vehicles=30: 0.24; DoorToDoor at n_vehicles=25: 0.775
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: FIG-04 — Baseline comparison bar charts</name>
  <files>figures/scripts/fig04_baseline_comparison.py, figures/fig04_baseline_comparison.pdf, figures/fig04_baseline_comparison.png</files>

  <read_first>
    - results/metrics_table.csv — read all rows and columns; note exact variant name strings and mean/std column names
    - paper/sections/experiments.tex — read lines 63-105 (Main Results section) for the narrative context and the −34.9% annotation that must appear in the figure
    - .planning/phases/06-academic-figures-visualization/06-CONTEXT.md — read FIG-04 specifics block
  </read_first>

  <action>
Write figures/scripts/fig04_baseline_comparison.py.

Figure spec (double-column, 180mm × 80mm, 300 dpi):
- figsize=(7.09, 3.15) inches (180mm × 80mm), dpi=300
- Font: serif, axis labels 9pt, tick labels 8pt, annotation 7pt
- Use plt.rcParams['font.family'] = 'serif'

Data loading:
```python
import pandas as pd
import matplotlib.pyplot as plt
import matplotlib.patches as mpatches
import numpy as np

df = pd.read_csv('results/metrics_table.csv')
```

Variant display order (left to right in bars):
```python
order = ['DoorToDoor', 'SingleSidedPickup', 'BidirectionalNoChoice',
         'FullModel', 'AblationNoChoice', 'AblationNoRollingHorizon']
short_labels = ['D2D', 'Single\nPickup', 'Bidir\nNoChoice',
                'FullModel', 'Ablation\nNoChoice', 'Ablation\nNoRH']
```

Color mapping:
```python
colors = {
    'DoorToDoor': '#AAAAAA',           # gray
    'SingleSidedPickup': '#78A8D1',    # light blue
    'BidirectionalNoChoice': '#4878CF', # medium blue
    'FullModel': '#1A3A6B',            # dark blue (highlight)
    'AblationNoChoice': '#B47CC7',     # purple
    'AblationNoRollingHorizon': '#D65F5F',  # red
}
```

Layout — two subplots side by side (fig, (ax1, ax2) = plt.subplots(1, 2, figsize=(7.09, 3.15))):

Left panel (ax1) — Acceptance Rate:
- Grouped bars: x positions = np.arange(6), bar width = 0.6
- y values: df.set_index('variant').loc[order, 'acceptance_rate_mean'].values
- Error bars: yerr = df.set_index('variant').loc[order, 'acceptance_rate_std'].values, capsize=3, ecolor='#333333', elinewidth=0.8
- y-axis label: "Acceptance Rate", y-axis range [0, 0.75]
- x-tick labels: short_labels, rotation=0, fontsize=8
- Title: "(a) Acceptance Rate"
- Add horizontal dashed gray line at DoorToDoor acceptance rate (0.6096) with label "D2D baseline"

Right panel (ax2) — Vehicle-km:
- Same structure, y values from 'vehicle_km_mean', yerr from 'vehicle_km_std'
- y-axis label: "Vehicle-km (vkm)", y-axis range [0, 3200]
- Title: "(b) Vehicle-km"
- Add horizontal dashed gray line at DoorToDoor vkm (2603.71) with label "D2D baseline"
- Add annotation arrow pointing to FullModel bar: text "−34.9%\nvkm/acceptance\nvs D2D", arrowprops=dict(arrowstyle='->', color='black'), fontsize=7, color='#1A3A6B'
  - The annotation should point to the FullModel bar (index 3 in the order list)
  - Position the text above and to the right of the FullModel bar

Both panels:
- ax.spines['top'].set_visible(False); ax.spines['right'].set_visible(False)
- ax.tick_params(axis='both', labelsize=8)
- plt.tight_layout(pad=0.5)

Save:
```python
fig.savefig('figures/fig04_baseline_comparison.pdf', bbox_inches='tight', dpi=300)
fig.savefig('figures/fig04_baseline_comparison.png', bbox_inches='tight', dpi=300)
```

Run the script: `python figures/scripts/fig04_baseline_comparison.py`
  </action>

  <verify>
    <automated>python -c "
import os, subprocess
result = subprocess.run(['python', 'figures/scripts/fig04_baseline_comparison.py'], capture_output=True, text=True)
assert result.returncode == 0, f'Script failed: {result.stderr}'
assert os.path.exists('figures/fig04_baseline_comparison.pdf'), 'PDF missing'
assert os.path.getsize('figures/fig04_baseline_comparison.pdf') > 10000, 'PDF too small'
assert os.path.exists('figures/fig04_baseline_comparison.png'), 'PNG missing'
print('FIG-04 OK')
"</automated>
  </verify>

  <acceptance_criteria>
    - figures/fig04_baseline_comparison.pdf exists, size > 10 KB
    - figures/fig04_baseline_comparison.png exists, size > 30 KB
    - Script reads from 'results/metrics_table.csv' (grep: "read_csv.*metrics_table")
    - Script contains all 6 variant names as strings (grep: "FullModel", "DoorToDoor", "AblationNoRollingHorizon")
    - Script contains the "−34.9%" annotation string
    - Script uses figsize=(7.09, 3.15) for double-column width
    - Script runs without error from project root directory
  </acceptance_criteria>

  <done>figures/fig04_baseline_comparison.pdf and .png exist; script reads live CSV data and is reproducible</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: FIG-05 — Sensitivity analysis line plots</name>
  <files>figures/scripts/fig05_sensitivity.py, figures/fig05_sensitivity.pdf, figures/fig05_sensitivity.png</files>

  <read_first>
    - results/sensitivity_walk.csv — note the rho values (200,300,400,500,700,1000), variant column, density_tier column; filter to density_tier='standard' for the main sweep
    - results/sensitivity_fleet.csv — note n_vehicles values (5,10,15,20,25,30) and variant column
    - paper/sections/experiments.tex — read lines 140-165 (Sensitivity Analysis section) for the narrative: ρ=1000m threshold annotation, n=20 diminishing returns annotation
    - .planning/phases/06-academic-figures-visualization/06-CONTEXT.md — read FIG-05 specifics block
  </read_first>

  <action>
Write figures/scripts/fig05_sensitivity.py.

Figure spec (double-column, 180mm × 75mm, 300 dpi):
- figsize=(7.09, 2.95) inches, dpi=300
- Font: serif, axis labels 9pt, tick labels 8pt

Data loading:
```python
import pandas as pd
import matplotlib.pyplot as plt
import numpy as np

walk_df = pd.read_csv('results/sensitivity_walk.csv')
fleet_df = pd.read_csv('results/sensitivity_fleet.csv')

# Filter walk sweep to standard density tier only
walk_std = walk_df[walk_df['density_tier'] == 'standard']
walk_full = walk_std[walk_std['variant'] == 'FullModel'].sort_values('rho')
walk_d2d  = walk_std[walk_std['variant'] == 'DoorToDoor'].sort_values('rho').drop_duplicates('rho')

fleet_full = fleet_df[fleet_df['variant'] == 'FullModel'].sort_values('n_vehicles')
fleet_d2d  = fleet_df[fleet_df['variant'] == 'DoorToDoor'].sort_values('n_vehicles')
```

Layout — two subplots side by side (fig, (ax1, ax2) = plt.subplots(1, 2, figsize=(7.09, 2.95))):

Left panel (ax1) — Walking tolerance sweep:
- x-axis: rho values from walk_full['rho'] (200, 300, 400, 500, 700, 1000)
- Plot FullModel line: ax1.plot(walk_full['rho'], walk_full['acceptance_rate'], marker='o', color='#1A3A6B', label='FullModel', linewidth=1.5, markersize=4)
- Plot DoorToDoor line: ax1.plot(walk_d2d['rho'], walk_d2d['acceptance_rate'], marker='s', color='#AAAAAA', linestyle='--', label='DoorToDoor', linewidth=1.5, markersize=4)
- Add vertical dashed red line at x=1000: ax1.axvline(x=1000, color='#D65F5F', linestyle=':', linewidth=1.2, label='ρ=1000m threshold')
- Annotate: ax1.annotate('Min viable\nthreshold', xy=(1000, 0.05), xytext=(750, 0.15), fontsize=7, color='#D65F5F', arrowprops=dict(arrowstyle='->', color='#D65F5F', lw=0.8))
- x-axis label: "Walking tolerance ρ (m)", y-axis label: "Acceptance Rate"
- x-ticks: [200, 400, 600, 800, 1000], y-range: [0, 0.7]
- Title: "(a) Walking Tolerance Sweep"
- Legend: loc='upper left', fontsize=7

Right panel (ax2) — Fleet size sweep:
- x-axis: n_vehicles from fleet_full['n_vehicles'] (5, 10, 15, 20, 25, 30)
- Plot FullModel: ax2.plot(fleet_full['n_vehicles'], fleet_full['acceptance_rate'], marker='o', color='#1A3A6B', label='FullModel', linewidth=1.5, markersize=4)
- Plot DoorToDoor: ax2.plot(fleet_d2d['n_vehicles'], fleet_d2d['acceptance_rate'], marker='s', color='#AAAAAA', linestyle='--', label='DoorToDoor', linewidth=1.5, markersize=4)
- Add vertical dashed line at x=20: ax2.axvline(x=20, color='#B47CC7', linestyle=':', linewidth=1.2, label='Diminishing returns (n=20)')
- Annotate: ax2.annotate('Diminishing\nreturns', xy=(20, 0.22), xytext=(22, 0.35), fontsize=7, color='#B47CC7', arrowprops=dict(arrowstyle='->', color='#B47CC7', lw=0.8))
- x-axis label: "Fleet size (vehicles)", y-axis label: "Acceptance Rate"
- x-ticks: [5, 10, 15, 20, 25, 30], y-range: [0, 0.9]
- Title: "(b) Fleet Size Sweep"
- Legend: loc='upper left', fontsize=7

Both panels:
- ax.spines['top'].set_visible(False); ax.spines['right'].set_visible(False)
- ax.tick_params(axis='both', labelsize=8)
- plt.tight_layout(pad=0.5)

Save:
```python
fig.savefig('figures/fig05_sensitivity.pdf', bbox_inches='tight', dpi=300)
fig.savefig('figures/fig05_sensitivity.png', bbox_inches='tight', dpi=300)
```

Run the script: `python figures/scripts/fig05_sensitivity.py`
  </action>

  <verify>
    <automated>python -c "
import os, subprocess
result = subprocess.run(['python', 'figures/scripts/fig05_sensitivity.py'], capture_output=True, text=True)
assert result.returncode == 0, f'Script failed: {result.stderr}'
assert os.path.exists('figures/fig05_sensitivity.pdf'), 'PDF missing'
assert os.path.getsize('figures/fig05_sensitivity.pdf') > 10000, 'PDF too small'
assert os.path.exists('figures/fig05_sensitivity.png'), 'PNG missing'
print('FIG-05 OK')
"</automated>
  </verify>

  <acceptance_criteria>
    - figures/fig05_sensitivity.pdf exists, size > 10 KB
    - figures/fig05_sensitivity.png exists, size > 30 KB
    - Script reads from 'results/sensitivity_walk.csv' (grep: "read_csv.*sensitivity_walk")
    - Script reads from 'results/sensitivity_fleet.csv' (grep: "read_csv.*sensitivity_fleet")
    - Script filters walk data to density_tier == 'standard' (grep: "density_tier.*standard")
    - Script contains axvline at x=1000 (walking threshold annotation)
    - Script contains axvline at x=20 (diminishing returns annotation)
    - Script uses figsize=(7.09, 2.95) for double-column width
    - Script runs without error from project root directory
  </acceptance_criteria>

  <done>figures/fig05_sensitivity.pdf and .png exist; script reads live CSV data, filters correctly, and is reproducible</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| results/ CSV files → script | Scripts read CSV data from results/; data was produced by Phase 3/4 simulation code |
| script → figures/ | Scripts write PDF/PNG output to figures/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering | results/metrics_table.csv | accept | CSV is a Phase 3 artifact; any modification would be caught by re-running Phase 3 experiments |
| T-06-02-02 | Tampering | results/sensitivity_*.csv | accept | Same as above — Phase 4 artifact, reproducible |
| T-06-02-03 | Information Disclosure | figure content | accept | Figures contain only simulation results; no PII, no sensitive data |
| T-06-02-04 | Denial of Service | pandas/matplotlib import failure | mitigate | Scripts use only pandas, matplotlib, numpy — all present in pyproject.toml environment; verify with `python -c "import pandas, matplotlib, numpy"` before running |
| T-06-02-05 | Spoofing | hardcoded values instead of CSV read | mitigate | Acceptance criteria explicitly grep for read_csv calls; executor must not hardcode data values |
</threat_model>

<verification>
After both tasks complete:
1. `ls figures/fig04* figures/fig05*` — should list 4 files (2 PDF, 2 PNG)
2. `python figures/scripts/fig04_baseline_comparison.py` — runs without error
3. `python figures/scripts/fig05_sensitivity.py` — runs without error
4. `grep -l "read_csv" figures/scripts/fig04_baseline_comparison.py figures/scripts/fig05_sensitivity.py` — both files match
</verification>

<success_criteria>
- figures/fig04_baseline_comparison.pdf and .png exist, non-empty
- figures/fig05_sensitivity.pdf and .png exist, non-empty
- Both scripts read from CSV files (not hardcoded data)
- FIG-04 shows all 6 variants with FullModel highlighted in dark blue and −34.9% annotation
- FIG-05 shows ρ=1000m threshold line and n=20 diminishing returns line
- Both figures are 180mm wide (double-column TR Part A format)
</success_criteria>

<output>
After completion, create .planning/phases/06-academic-figures-visualization/06-02-SUMMARY.md
</output>
