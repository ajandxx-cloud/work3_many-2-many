---
phase: 01-problem-formulation-model-structure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - model/notation.tex
  - model/problem-definition.tex
autonomous: true
requirements:
  - PROB-01
  - PROB-02
  - PROB-03
  - PROB-05
must_haves:
  truths:
    - "The many-to-many DRT problem is defined with named sets for requests, vehicles, meeting points"
    - "Bidirectional meeting point sets M_r^P and M_r^D are formally introduced per request"
    - "The service offer bundle b = (m_r^P, m_r^D, τ_r, p_r) is given exact domains for each component"
    - "The system objective combining operational, waiting, walking, IVT, and rejection costs is written as a single expression"
    - "The online decision vector (z_r, m_r^P, m_r^D, v_r, π_r^P, π_r^D) is introduced with each component defined"
    - "A self-contained notation table covers every symbol used in plans 01–03"
  artifacts:
    - path: "model/notation.tex"
      provides: "Complete notation table/glossary for all symbols"
      contains: "\\begin{longtable}"
    - path: "model/problem-definition.tex"
      provides: "Network, sets, service offer bundle, objective, decision vector"
      contains: "M_r^P"
  key_links:
    - from: "model/notation.tex"
      to: "model/model.tex"
      via: "\\input{model/notation}"
      pattern: "\\\\input\\{model/notation\\}"
    - from: "model/problem-definition.tex"
      to: "model/model.tex"
      via: "\\input{model/problem-definition}"
      pattern: "\\\\input\\{model/problem-definition\\}"
---

<objective>
Produce two LaTeX source files that together define the many-to-many DRT problem with
bidirectional meeting points and establish the complete notation table used across all
subsequent plans and Phase 2.

Purpose: Notation consistency is the highest-risk cross-phase dependency. Fixing all symbols
now in a single authoritative file eliminates drift between the model formulation (Phase 1),
algorithm implementation (Phase 2), and paper writing (Phase 5).

Output:
- model/notation.tex — longtable covering every symbol, domain, and description
- model/problem-definition.tex — network definition, sets, service offer bundle,
  system objective, and online decision vector
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-problem-formulation-model-structure/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create notation table (model/notation.tex)</name>

  <read_first>
    .planning/REQUIREMENTS.md — source of truth for all notation already committed (M_r^P,
    M_r^D, b, decision vector components)
    .planning/phases/01-problem-formulation-model-structure/01-CONTEXT.md — confirms indexing
    conventions (r, v, P/D, [e_r, l_r])
  </read_first>

  <files>model/notation.tex</files>

  <action>
Create the directory model/ at the project root (alongside .planning/). Write model/notation.tex
as a self-contained LaTeX fragment (no \documentclass — it will be \input'd by model.tex).

The file must contain a longtable environment with three columns: Symbol | Domain/Type | Description.
Include every symbol listed below, in the order shown. Use \mathcal for set names, \mathbb for
number sets, superscripts P/D for pickup/dropoff.

SETS AND INDICES (section header row):
  Symbol              Domain          Description
  r                   \mathcal{R}     Index for passenger requests; \mathcal{R} = \{1,\ldots,|\mathcal{R}|\}
  v                   \mathcal{V}     Index for vehicles; \mathcal{V} = \{1,\ldots,|\mathcal{V}|\}
  \mathcal{R}         —               Set of all requests arriving in the planning horizon
  \mathcal{V}         —               Set of all vehicles in the fleet
  \mathcal{M}         —               Universe of pre-defined fixed meeting points (bus stops, landmarks)
  M_r^P               \subseteq \mathcal{M}   Candidate pickup meeting point set for request r (filtered by walking radius)
  M_r^D               \subseteq \mathcal{M}   Candidate dropoff meeting point set for request r (filtered by walking radius)
  \mathcal{B}_r       —               Set of feasible service offer bundles for request r; \mathcal{B}_r = M_r^P \times M_r^D

SPATIAL AND TEMPORAL (section header row):
  d(i,j)              \mathbb{R}_{\geq 0}     Euclidean distance between locations i and j
  t(i,j)              \mathbb{R}_{\geq 0}     Travel time from i to j; t(i,j) = d(i,j) / \bar{v} where \bar{v} is average speed
  o_r                 —               Origin location of request r (passenger's pickup address)
  \delta_r            —               Destination location of request r (passenger's dropoff address)
  e_r                 \mathbb{R}_{\geq 0}     Earliest acceptable pickup time for request r (time window lower bound)
  l_r                 \mathbb{R}_{\geq 0}     Latest acceptable pickup time for request r (time window upper bound)
  \rho^P              \mathbb{R}_{>0}         Maximum walking radius for pickup meeting point (meters)
  \rho^D              \mathbb{R}_{>0}         Maximum walking radius for dropoff meeting point (meters)

SERVICE OFFER BUNDLE (section header row):
  b                   \mathcal{B}_r   A feasible service offer bundle for request r
  m_r^P               M_r^P           Selected pickup meeting point for request r
  m_r^D               M_r^D           Selected dropoff meeting point for request r
  \tau_r              \mathbb{R}_{\geq 0}     Promised/scheduled pickup time at m_r^P
  p_r                 \mathbb{R}_{\geq 0}     Fare charged to request r (fixed per trip in this model)

DECISION VARIABLES (section header row):
  z_r                 \{0,1\}         Acceptance indicator: z_r = 1 if request r is accepted, 0 otherwise
  v_r                 \mathcal{V}     Vehicle assigned to serve request r (defined when z_r = 1)
  \pi_r^P             —               Insertion position of pickup node for r in vehicle v_r's route
  \pi_r^D             —               Insertion position of dropoff node for r in vehicle v_r's route

ROUTE AND SCHEDULE (section header row):
  \sigma_v            —               Ordered route (sequence of nodes) for vehicle v
  s_{v,i}            \mathbb{R}_{\geq 0}     Scheduled service time at node i in vehicle v's route
  Q_v                 \mathbb{Z}_{>0}         Passenger capacity of vehicle v
  q_v(i)             \mathbb{Z}_{\geq 0}     Vehicle v's passenger load when departing from node i
  L^{\max}            \mathbb{R}_{>0}         Maximum allowable in-vehicle ride time per passenger

COST COMPONENTS (section header row):
  C^{op}              \mathbb{R}_{\geq 0}     Total vehicle operational cost (proportional to total distance driven)
  C^{wait}            \mathbb{R}_{\geq 0}     Aggregate passenger waiting time cost
  C^{walk}            \mathbb{R}_{\geq 0}     Aggregate passenger walking distance cost
  C^{IVT}             \mathbb{R}_{\geq 0}     Aggregate in-vehicle travel time cost
  C^{rej}             \mathbb{R}_{\geq 0}     Cost/penalty for each rejected request
  \alpha_1,\ldots,\alpha_5   \mathbb{R}_{\geq 0}   Weights on cost components in objective function

PASSENGER CHOICE (section header row — populated fully in plan 03):
  k                   \mathcal{K}     Passenger type index; \mathcal{K} = \{price, time, walk\}
  U_{rb}              \mathbb{R}      Deterministic utility of bundle b for request r
  U_{r0}              \mathbb{R}      Utility of the outside option (reject all offers) for request r
  P_{rb}              [0,1]           MNL choice probability that request r selects bundle b
  \beta_1^k           \mathbb{R}      Walk-time coefficient for passenger type k
  \beta_2^k           \mathbb{R}      Waiting-time coefficient for passenger type k
  \beta_3^k           \mathbb{R}      In-vehicle travel time (IVT) coefficient for passenger type k
  \beta_4^k           \mathbb{R}      Price coefficient for passenger type k (negative for disutility)
  \text{Walk}_{rb}    \mathbb{R}_{\geq 0}     Walking time to/from meeting points for request r under bundle b
  \text{Wait}_{rb}    \mathbb{R}_{\geq 0}     Waiting time at pickup meeting point for request r under bundle b
  \text{IVT}_{rb}     \mathbb{R}_{\geq 0}     In-vehicle travel time for request r under bundle b

THREE-LAYER MODEL (section header row):
  H                   \mathbb{R}_{>0}         Rolling horizon window length (minutes)
  \Delta              \mathbb{R}_{>0}         Rolling horizon time step (re-optimization frequency, minutes)
  t^{\text{now}}      \mathbb{R}_{\geq 0}     Current system time in online operation
  \mathcal{R}^{\text{act}}(t)  —     Set of active (accepted, not yet completed) requests at time t
  k^{\text{top}}      \mathbb{Z}_{>0}         Number of top candidates generated per request per side (pickup and dropoff)

Use \toprule, \midrule, \bottomrule (booktabs) and \multicolumn for section header rows.
Label the table with \label{tab:notation} and caption "Summary of Notation".
Add a comment at the top: %% notation.tex — authoritative notation table for Work 3
  </action>

  <verify>
    grep -c "M_r\^P\|M_r\^{P}" model/notation.tex
    grep "longtable" model/notation.tex
    grep "label{tab:notation}" model/notation.tex
  </verify>

  <acceptance_criteria>
    - model/notation.tex exists and is non-empty
    - File contains \begin{longtable} environment
    - File contains at least 35 symbol rows (one per notation entry above)
    - The string M_r^P (or M_r^{P}) appears in the file
    - The string \mathcal{B}_r appears (service offer bundle set)
    - The string \beta_1^k appears (type-indexed MNL coefficient)
    - The string \label{tab:notation} appears
    - The comment "%% notation.tex" appears on line 1
    - No \documentclass or \begin{document} appears (fragment only)
  </acceptance_criteria>

  <done>model/notation.tex exists as a LaTeX fragment containing a complete longtable with every
  symbol defined in the action above, ready to be \input'd by model.tex.</done>
</task>

<task type="auto">
  <name>Task 2: Write network definition, service offer bundle, objective, and decision vector (model/problem-definition.tex)</name>

  <read_first>
    model/notation.tex — use exactly the symbol names defined there; do not introduce new symbols
    .planning/REQUIREMENTS.md — PROB-01 through PROB-05 specify the exact components required
    .planning/phases/01-problem-formulation-model-structure/01-CONTEXT.md — confirms Euclidean
    distance, directed graph, continuous time, and fixed meeting point set decisions
  </read_first>

  <files>model/problem-definition.tex</files>

  <action>
Create model/problem-definition.tex as a LaTeX fragment (no \documentclass). This file
formalizes the problem structure in four numbered subsections, each using \subsection*{}.

Add comment at top: %% problem-definition.tex — network, problem setup, objective, decision vector

--- SUBSECTION 1: Network and Input ---
Title: Network and Input

Write the following content in LaTeX, in this order:

Paragraph 1 — Network:
"We model the service area as a directed graph G = (N, A), where N is the set of nodes
(passenger origins, destinations, and meeting points) and A is the set of directed arcs.
Travel time between any two locations i, j \in N is given by t(i,j) = d(i,j) / \bar{v},
where d(i,j) denotes Euclidean distance and \bar{v} is the average vehicle speed."

Paragraph 2 — Meeting point universe:
"A finite set \mathcal{M} of pre-defined fixed meeting points (e.g., bus stops, commercial
landmarks) is given. For each request r, the candidate pickup set M_r^P \subseteq \mathcal{M}
is the subset of meeting points within walking radius \rho^P of the passenger's origin o_r,
and the candidate dropoff set M_r^D \subseteq \mathcal{M} is the subset within walking radius
\rho^D of the destination \delta_r. Formally:
  M_r^P = \{ m \in \mathcal{M} \mid d(o_r, m) \leq \rho^P \}
  M_r^D = \{ m \in \mathcal{M} \mid d(\delta_r, m) \leq \rho^D \}."

Paragraph 3 — Request:
"Each request r \in \mathcal{R} is characterized by a tuple (o_r, \delta_r, e_r, l_r, k_r),
where [e_r, l_r] is the pickup time window and k_r \in \mathcal{K} denotes the passenger
type. Requests arrive online over a continuous planning horizon."

--- SUBSECTION 2: Service Offer Bundle ---
Title: Service Offer Bundle

"For each request r, a \emph{service offer bundle} is a tuple
  b = (m_r^P,\; m_r^D,\; \tau_r,\; p_r)
where:
\begin{itemize}
  \item m_r^P \in M_r^P is the assigned pickup meeting point,
  \item m_r^D \in M_r^D is the assigned dropoff meeting point,
  \item \tau_r \in [e_r, l_r] is the scheduled pickup time at m_r^P,
  \item p_r \geq 0 is the fare charged to the passenger (fixed in this model).
\end{itemize}
The set of all feasible bundles for request r is \mathcal{B}_r = M_r^P \times M_r^D
(with \tau_r and p_r treated as service parameters determined by the system).
The service offer bundle b is the central decision object: the system selects one bundle
from \mathcal{B}_r or rejects the request."

--- SUBSECTION 3: System Objective ---
Title: System Objective

"The system minimizes a weighted sum of operational and passenger service costs over the
planning horizon:
\begin{equation}\label{eq:objective}
  \min \quad \alpha_1 C^{op} + \alpha_2 C^{wait} + \alpha_3 C^{walk}
             + \alpha_4 C^{IVT} + \alpha_5 C^{rej}
\end{equation}
where:
\begin{align}
  C^{op}   &= \sum_{v \in \mathcal{V}} c_v \cdot D_v, \label{eq:cop}\\
  C^{wait} &= \sum_{r \in \mathcal{R}} z_r \cdot (\tau_r - t_r^{\text{req}}), \label{eq:cwait}\\
  C^{walk} &= \sum_{r \in \mathcal{R}} z_r \cdot \bigl(d(o_r,m_r^P) + d(m_r^D,\delta_r)\bigr), \label{eq:cwalk}\\
  C^{IVT}  &= \sum_{r \in \mathcal{R}} z_r \cdot \text{IVT}_{rb}, \label{eq:civt}\\
  C^{rej}  &= \sum_{r \in \mathcal{R}} (1 - z_r) \cdot \Gamma, \label{eq:crej}
\end{align}
$D_v$ is the total distance driven by vehicle $v$, $c_v$ is cost per unit distance,
$t_r^{\text{req}}$ is the time request $r$ was submitted, and $\Gamma > 0$ is the rejection
penalty."

--- SUBSECTION 4: Online Decision Vector ---
Title: Online Decision Vector

"Upon arrival of request r, the system must immediately determine the \emph{online decision
vector}:
\begin{equation}\label{eq:decvec}
  \mathbf{x}_r = (z_r,\; m_r^P,\; m_r^D,\; v_r,\; \pi_r^P,\; \pi_r^D)
\end{equation}
where:
\begin{itemize}
  \item z_r \in \{0,1\} is the acceptance indicator (1 = accepted, 0 = rejected),
  \item m_r^P \in M_r^P is the pickup meeting point (defined when z_r = 1),
  \item m_r^D \in M_r^D is the dropoff meeting point (defined when z_r = 1),
  \item v_r \in \mathcal{V} is the vehicle assigned to serve request r (defined when z_r = 1),
  \item \pi_r^P is the insertion position of the pickup node in vehicle v_r's route,
  \item \pi_r^D is the insertion position of the dropoff node in vehicle v_r's route, with
        \pi_r^D > \pi_r^P (pickup precedes dropoff in route).
\end{itemize}
The decision must be made online — i.e., before future requests are known — and subject to
all feasibility constraints specified in Section~\ref{sec:constraints}."
  </action>

  <verify>
    grep "M_r\^P\|M_r\^{P}" model/problem-definition.tex
    grep "label{eq:objective}" model/problem-definition.tex
    grep "label{eq:decvec}" model/problem-definition.tex
    grep "mathcal{B}_r" model/problem-definition.tex
  </verify>

  <acceptance_criteria>
    - model/problem-definition.tex exists and is non-empty
    - The string M_r^P (or M_r^{P}) appears at least 3 times
    - The string M_r^D (or M_r^{D}) appears at least 3 times
    - The string \mathcal{B}_r appears (service offer bundle set defined)
    - The string \label{eq:objective} appears (objective function labeled)
    - The string \label{eq:decvec} appears (decision vector labeled)
    - The string z_r \in \{0,1\} appears (acceptance indicator domain specified)
    - The string \pi_r^D > \pi_r^P appears (precedence noted in decision vector definition)
    - All five cost components C^{op}, C^{wait}, C^{walk}, C^{IVT}, C^{rej} appear
    - No \documentclass or \begin{document} appears (fragment only)
    - Comment "%% problem-definition.tex" appears on line 1
  </acceptance_criteria>

  <done>model/problem-definition.tex exists as a LaTeX fragment with four subsections covering
  network setup (directed graph, M_r^P, M_r^D with distance filters), service offer bundle
  (all four components with domains), system objective (five cost terms with labels), and online
  decision vector (all six components with domains and precedence constraint noted).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Author → LaTeX files | Human-readable academic content; no external data, no code execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | notation.tex | accept | File is version-controlled in git; any unintended edit is traceable via diff |
| T-01-02 | Information Disclosure | model/*.tex | accept | Academic notation; no sensitive data, credentials, or PII in these files |
| T-01-03 | Repudiation | notation.tex | accept | Git history provides authorship trail; no additional logging needed |

Note: This is a documentation/mathematical writing phase. No code executes, no external APIs are called, no user input is processed. Threat surface is effectively zero.
</threat_model>

<verification>
After both tasks complete:
1. Run: ls -la model/ — expect notation.tex and problem-definition.tex both present
2. Run: grep -c "begin{" model/notation.tex — expect exactly 1 (the longtable)
3. Run: grep "label{eq:" model/problem-definition.tex — expect labels for objective, cop, cwait, cwalk, civt, crej, decvec
4. Manual review: open model/notation.tex and confirm all three columns (Symbol, Domain, Description) are populated for every row
5. Notation consistency check: grep for "M_r" across both files — all occurrences must use M_r^P / M_r^D (not M_r^p / M_r^d)
</verification>

<success_criteria>
- model/notation.tex: longtable with 35+ symbol entries, all four symbol categories covered (sets, spatial/temporal, decision, MNL/choice), \label{tab:notation} present
- model/problem-definition.tex: four subsections with labeled equations for objective (\ref{eq:objective}) and decision vector (\ref{eq:decvec}), M_r^P and M_r^D formally defined with distance filters, all five cost components written as aligned equations
- No symbol appears in problem-definition.tex that is absent from notation.tex
- Both files are LaTeX fragments (no \documentclass) ready for \input in model.tex
</success_criteria>

<output>
After completion, create .planning/phases/01-problem-formulation-model-structure/01-01-SUMMARY.md
with:
- Files created: model/notation.tex, model/problem-definition.tex
- Symbol count in notation table
- Any notation decisions made beyond what CONTEXT.md specified (e.g., specific domain choices)
- Key labels defined (eq:objective, eq:decvec, tab:notation)
- Blockers or open questions for downstream plans
</output>
