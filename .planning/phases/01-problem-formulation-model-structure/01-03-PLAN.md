---
phase: 01-problem-formulation-model-structure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - model/choice-model.tex
autonomous: true
requirements:
  - CHOICE-01
  - CHOICE-02
  - CHOICE-03
  - CHOICE-04
must_haves:
  truths:
    - "The MNL utility function with all four β parameters is written as a labeled equation"
    - "The outside option U_r0 is formally defined and included in the denominator of the choice probability"
    - "Three passenger types (price-sensitive, time-sensitive, walk-sensitive) are named and their β parameter profiles are tabulated"
    - "The choice probability formula P_rb is written with the outside option in the denominator"
    - "The acceptance probability (probability of choosing any offered bundle) is derived from the choice probabilities"
  artifacts:
    - path: "model/choice-model.tex"
      provides: "MNL utility, outside option, heterogeneity table, choice probability"
      contains: "\\label{eq:utility}"
  key_links:
    - from: "model/choice-model.tex"
      to: "model/model.tex"
      via: "\\input{model/choice-model}"
      pattern: "\\\\input\\{model/choice-model\\}"
    - from: "model/choice-model.tex"
      to: "Phase 2 simulation"
      via: "β parameter profiles used to simulate passenger type behavior"
      pattern: "beta_.*price\\|beta_.*time\\|beta_.*walk"
---

<objective>
Produce model/choice-model.tex, a LaTeX fragment formalizing the MNL passenger choice model
with outside option and passenger heterogeneity.

Purpose: The MNL model is the core dissertation thread (non-negotiable per CLAUDE.md and
REQUIREMENTS). This file must be precise enough that Phase 2 can implement the choice
probability computation directly from it, and Phase 3 can simulate three distinct passenger
types using the β parameter profiles defined here.

Output:
- model/choice-model.tex — utility function, outside option, three passenger types with
  β profiles, choice probability formula, and acceptance probability derivation
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-problem-formulation-model-structure/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write MNL utility, outside option, and choice probability (model/choice-model.tex)</name>

  <read_first>
    .planning/REQUIREMENTS.md — CHOICE-01 through CHOICE-04 specify exact formulas:
      CHOICE-01: U_rb = β1·Walk_rb + β2·Wait_rb + β3·IVT_rb + β4·p_r + ε
      CHOICE-02: outside option U_r0 must be included
      CHOICE-03: at least 2-3 passenger types
      CHOICE-04: P_rb = exp(U_rb) / (exp(U_r0) + Σ exp(U_rb))
    .planning/phases/01-problem-formulation-model-structure/01-CONTEXT.md — confirms
      type-indexed β^k parameters, three named types, and that β4 is negative (price disutility)
  </read_first>

  <files>model/choice-model.tex</files>

  <action>
Create model/choice-model.tex as a LaTeX fragment (no \documentclass). Add comment on line 1:
%% choice-model.tex — MNL passenger choice model with outside option and heterogeneity

Write a \section*{Passenger Choice Model} header, then four \subsection*{} blocks.

--- SUBSECTION 1: Utility Function ---
\subsection*{Utility Function}

"We model passenger choice using a Multinomial Logit (MNL) model. For request r of passenger
type k \in \mathcal{K}, the deterministic utility of service offer bundle b = (m_r^P, m_r^D,
\tau_r, p_r) is:

\begin{equation}\label{eq:utility}
  U_{rb}^k = \beta_1^k \cdot \text{Walk}_{rb} + \beta_2^k \cdot \text{Wait}_{rb}
           + \beta_3^k \cdot \text{IVT}_{rb} + \beta_4^k \cdot p_r
\end{equation}

where:
\begin{itemize}
  \item \text{Walk}_{rb} = d(o_r, m_r^P) + d(m_r^D, \delta_r) is the total walking distance
        (pickup walk + dropoff walk) in meters,
  \item \text{Wait}_{rb} = \tau_r - t_r^{\text{req}} is the waiting time from request
        submission to scheduled pickup (minutes),
  \item \text{IVT}_{rb} = s_{v_r,\, m_r^D} - s_{v_r,\, m_r^P} is the in-vehicle travel
        time from pickup to dropoff meeting point (minutes),
  \item p_r \geq 0 is the fare (CNY),
  \item \beta_1^k, \beta_2^k, \beta_3^k \leq 0 are disutility coefficients for walking,
        waiting, and in-vehicle time respectively (negative: more time/distance = lower utility),
  \item \beta_4^k \leq 0 is the price disutility coefficient (negative: higher fare = lower
        utility).
\end{itemize}
The random utility is U_{rb}^k + \varepsilon_{rb}$ where $\varepsilon_{rb}$ is i.i.d.\
Gumbel(0,1), yielding the standard MNL form."

--- SUBSECTION 2: Outside Option ---
\subsection*{Outside Option}

"The choice set for request r includes an outside option representing the passenger's
decision to reject all offered bundles (e.g., use a private car, walk, or cancel the trip).
The utility of the outside option is:

\begin{equation}\label{eq:utility-outside}
  U_{r0}^k = \beta_0^k
\end{equation}

where \beta_0^k is the alternative-specific constant (ASC) for the outside option of
passenger type k. Setting \beta_0^k = 0 normalizes the outside option as the reference
alternative; a positive \beta_0^k indicates a preference for the outside option (e.g.,
passengers with private car access).

The full choice set for request r is \mathcal{B}_r \cup \{0\}, where 0 denotes the outside
option."

--- SUBSECTION 3: Passenger Heterogeneity ---
\subsection*{Passenger Heterogeneity}

"We distinguish three passenger types \mathcal{K} = \{\text{price}, \text{time}, \text{walk}\}
with different sensitivity profiles. Table~\ref{tab:passenger-types} summarizes the
qualitative ordering of coefficients; exact values are calibrated from literature or
simulation in Phase~3.

\begin{table}[h]
\centering
\caption{Passenger type coefficient profiles (qualitative ordering)}
\label{tab:passenger-types}
\begin{tabular}{lcccc}
\toprule
Type & $\beta_1^k$ (Walk) & $\beta_2^k$ (Wait) & $\beta_3^k$ (IVT) & $\beta_4^k$ (Price) \\
\midrule
Price-sensitive  & moderate & moderate & moderate & high (most negative) \\
Time-sensitive   & moderate & high (most negative) & high (most negative) & low \\
Walk-sensitive   & high (most negative) & moderate & moderate & moderate \\
\bottomrule
\end{tabular}
\end{table}

Formally, the ordering constraints are:
\begin{align}
  |\beta_4^{\text{price}}| &> |\beta_4^{\text{time}}|,\quad |\beta_4^{\text{price}}| > |\beta_4^{\text{walk}}| \label{eq:type-price}\\
  |\beta_2^{\text{time}}| + |\beta_3^{\text{time}}| &> |\beta_2^{\text{price}}| + |\beta_3^{\text{price}}| \label{eq:type-time}\\
  |\beta_1^{\text{walk}}| &> |\beta_1^{\text{price}}|,\quad |\beta_1^{\text{walk}}| > |\beta_1^{\text{time}}| \label{eq:type-walk}
\end{align}

Baseline parameter values for simulation (to be confirmed against Work 1/2 calibration):
\begin{align}
  (\beta_1^{\text{price}},\, \beta_2^{\text{price}},\, \beta_3^{\text{price}},\, \beta_4^{\text{price}})
    &= (-0.005,\; -0.04,\; -0.02,\; -0.15) \label{eq:beta-price}\\
  (\beta_1^{\text{time}},\, \beta_2^{\text{time}},\, \beta_3^{\text{time}},\, \beta_4^{\text{time}})
    &= (-0.005,\; -0.10,\; -0.08,\; -0.03) \label{eq:beta-time}\\
  (\beta_1^{\text{walk}},\, \beta_2^{\text{walk}},\, \beta_3^{\text{walk}},\, \beta_4^{\text{walk}})
    &= (-0.020,\; -0.04,\; -0.02,\; -0.05) \label{eq:beta-walk}
\end{align}

Units: \beta_1 in utility/meter, \beta_2 and \beta_3 in utility/minute, \beta_4 in utility/CNY."

--- SUBSECTION 4: Choice Probability and Acceptance Rate ---
\subsection*{Choice Probability and Acceptance Rate}

"Given the choice set \mathcal{B}_r \cup \{0\} and utilities \{U_{rb}^k\}_{b \in \mathcal{B}_r}
and U_{r0}^k, the MNL choice probability that passenger r (of type k) selects bundle b is:

\begin{equation}\label{eq:choice-prob}
  P_{rb}^k = \frac{\exp(U_{rb}^k)}{\exp(U_{r0}^k) + \displaystyle\sum_{b' \in \mathcal{B}_r} \exp(U_{rb'}^k)}
  \quad \forall b \in \mathcal{B}_r
\end{equation}

The probability of choosing the outside option (rejecting all bundles) is:

\begin{equation}\label{eq:outside-prob}
  P_{r0}^k = \frac{\exp(U_{r0}^k)}{\exp(U_{r0}^k) + \displaystyle\sum_{b' \in \mathcal{B}_r} \exp(U_{rb'}^k)}
\end{equation}

The overall acceptance probability for request r is:

\begin{equation}\label{eq:acceptance-prob}
  P_r^{\text{acc},k} = 1 - P_{r0}^k = \frac{\displaystyle\sum_{b \in \mathcal{B}_r} \exp(U_{rb}^k)}
  {\exp(U_{r0}^k) + \displaystyle\sum_{b' \in \mathcal{B}_r} \exp(U_{rb'}^k)}
\end{equation}

Note: In the online decision process, the system selects the bundle b^* \in \mathcal{B}_r
that maximizes expected utility (or expected system objective contribution), then presents
it to the passenger. The passenger accepts with probability P_{rb^*}^k and rejects with
probability P_{r0}^k. The realized acceptance z_r is therefore a Bernoulli random variable
with parameter P_{rb^*}^k."
  </action>

  <verify>
    grep "label{eq:utility}" model/choice-model.tex
    grep "label{eq:utility-outside}" model/choice-model.tex
    grep "label{eq:choice-prob}" model/choice-model.tex
    grep "label{tab:passenger-types}" model/choice-model.tex
    grep "label{eq:acceptance-prob}" model/choice-model.tex
    grep "beta_4.*price\|beta_4^{\\\\text{price" model/choice-model.tex
  </verify>

  <acceptance_criteria>
    - model/choice-model.tex exists and is non-empty
    - \label{eq:utility} appears (MNL utility function labeled)
    - \label{eq:utility-outside} appears (outside option labeled)
    - \label{eq:choice-prob} appears (choice probability formula labeled)
    - \label{eq:outside-prob} appears (outside option probability labeled)
    - \label{eq:acceptance-prob} appears (acceptance probability labeled)
    - \label{tab:passenger-types} appears (heterogeneity table labeled)
    - The string \beta_4^k appears (price coefficient with type index)
    - The string \text{price} appears (price-sensitive type named)
    - The string \text{time} appears (time-sensitive type named)
    - The string \text{walk} appears (walk-sensitive type named)
    - The string \exp(U_{r0}^k) appears in the denominator of choice probability
    - The string P_{r0}^k appears (outside option probability defined)
    - Baseline β values appear as equations (eq:beta-price, eq:beta-time, eq:beta-walk)
    - No \documentclass or \begin{document} appears (fragment only)
    - Comment "%% choice-model.tex" appears on line 1
  </acceptance_criteria>

  <done>model/choice-model.tex exists as a LaTeX fragment with four subsections: MNL utility
  function (eq:utility with all four β^k parameters), outside option (eq:utility-outside with
  ASC β_0^k), passenger heterogeneity table (tab:passenger-types with three types and baseline
  β values), and choice probability formula (eq:choice-prob with outside option in denominator
  and acceptance probability eq:acceptance-prob derived).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Author → LaTeX files | Mathematical writing; no external data, no code execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | choice-model.tex | accept | Version-controlled in git; parameter values traceable to Work 1/2 calibration |
| T-01-02 | Information Disclosure | model/*.tex | accept | Academic content only; no sensitive data |
| T-01-03 | Tampering | β parameter values | mitigate | Baseline values labeled as provisional (eq:beta-*); STATE.md todo item to confirm against Work 1/2 calibration before Phase 3 |

Note: This is a mathematical writing phase. The primary integrity concern is that baseline
β parameter values are provisional — they must be confirmed against Work 1/2 calibration
before Phase 3 simulation. This is already tracked as a todo in STATE.md.
</threat_model>

<verification>
After task completes:
1. Run: ls -la model/choice-model.tex — file must exist
2. Run: grep -c "label{eq:" model/choice-model.tex — expect >= 8
3. Run: grep -c "label{" model/choice-model.tex — expect >= 9 (8 equations + 1 table)
4. Notation check: grep "beta_1\|beta_2\|beta_3\|beta_4" model/choice-model.tex
   — all four β parameters must appear with type superscript k
5. Outside option check: grep "U_{r0}" model/choice-model.tex
   — must appear in both the definition (eq:utility-outside) and the denominator of eq:choice-prob
6. Manual: confirm the three passenger types in tab:passenger-types match the names in
   \mathcal{K} = \{\text{price}, \text{time}, \text{walk}\}
</verification>

<success_criteria>
- model/choice-model.tex: four subsections, all required labels present
- MNL utility (eq:utility): four β^k terms, all four attribute variables defined in itemize
- Outside option (eq:utility-outside): ASC β_0^k defined, normalization explained
- Heterogeneity table (tab:passenger-types): three types, four β columns, qualitative ordering
  plus baseline numerical values in three labeled equations
- Choice probability (eq:choice-prob): outside option exp(U_r0^k) in denominator
- Acceptance probability (eq:acceptance-prob): derived as 1 - P_r0^k
- File is a clean LaTeX fragment ready for \input in model.tex
</success_criteria>

<output>
After completion, create .planning/phases/01-problem-formulation-model-structure/01-03-SUMMARY.md
with:
- Files created: model/choice-model.tex
- Equation label inventory (all eq:* and tab:* labels listed)
- Baseline β parameter values used (flag as provisional pending Work 1/2 calibration)
- Note whether β_0^k (outside option ASC) was set to 0 or given a non-zero default
- Any open questions for Phase 3 simulation setup
</output>
