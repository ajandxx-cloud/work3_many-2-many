---
phase: 01-problem-formulation-model-structure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - model/constraints.tex
autonomous: true
requirements:
  - PROB-04
must_haves:
  truths:
    - "Every operational constraint on vehicles and passengers is written as a numbered mathematical expression"
    - "Vehicle capacity, time windows, ride time, walking radius, route duration, and precedence are all present"
    - "Each constraint is uniquely labeled so Phase 2 (MILP formulation) can reference it by label"
    - "The constraints use exactly the notation established in notation.tex (no new symbols introduced)"
  artifacts:
    - path: "model/constraints.tex"
      provides: "Full constraint set for MILP and feasibility checker"
      contains: "\\label{con:capacity}"
  key_links:
    - from: "model/constraints.tex"
      to: "model/model.tex"
      via: "\\input{model/constraints}"
      pattern: "\\\\input\\{model/constraints\\}"
    - from: "model/constraints.tex"
      to: "Phase 2 MILP formulation"
      via: "constraint labels referenced in algorithm plan"
      pattern: "\\\\ref\\{con:"
---

<objective>
Produce model/constraints.tex, a LaTeX fragment containing every operational constraint of
the many-to-many DRT problem with bidirectional meeting points, written as numbered, labeled
mathematical expressions.

Purpose: The constraint set is the direct input to the Phase 2 MILP formulation (EXACT-01)
and the Phase 2 fast feasibility checker (HEUR-03). Writing them precisely here — with correct
labels — eliminates ambiguity in algorithm development. Every constraint must be implementable
directly as a linear inequality or equality.

Output:
- model/constraints.tex — seven constraint classes, each with LaTeX equation environments
  and \label{con:*} labels, plus a brief verbal description for each
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-problem-formulation-model-structure/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write all seven constraint classes in LaTeX (model/constraints.tex)</name>

  <read_first>
    .planning/REQUIREMENTS.md — PROB-04 lists the five required constraint classes:
    capacity, time windows, ride time, route duration, walking radius, precedence
    .planning/phases/01-problem-formulation-model-structure/01-CONTEXT.md — confirms
    continuous time, directed graph, and that precedence (pickup before dropoff) must be explicit
    model/notation.tex — if it exists, use its symbol definitions exactly; if not yet written,
    use the symbols listed in plan 01 (notation is fixed regardless of file existence)
  </read_first>

  <files>model/constraints.tex</files>

  <action>
Create model/constraints.tex as a LaTeX fragment (no \documentclass). Add comment on line 1:
%% constraints.tex — operational constraints for many-to-many DRT with bidirectional meeting points

Write a \section*{Operational Constraints} header, then present six constraint classes as
\subsection*{} blocks. Each constraint must use an equation or align environment and carry a
\label{con:*} tag. Each constraint must also have 1-2 sentences of verbal description.

All subscripts and superscripts must use { } for multi-character expressions.
Use \forall to quantify over index sets.

--- CONSTRAINT CLASS 1: Vehicle Capacity ---
\subsection*{Vehicle Capacity}
Description: At no point in any vehicle's route may the number of passengers on board exceed
the vehicle's capacity.

\begin{equation}\label{con:capacity}
  q_v(i) \leq Q_v \quad \forall v \in \mathcal{V},\; \forall i \in \sigma_v
\end{equation}

where q_v(i) is the passenger load on vehicle v when departing from node i, and Q_v is the
vehicle's maximum capacity.

--- CONSTRAINT CLASS 2: Time Windows ---
\subsection*{Time Windows}
Description: Each accepted request must be picked up within its specified time window at the
assigned pickup meeting point.

\begin{equation}\label{con:tw-early}
  s_{v_r,\, m_r^P} \geq e_r \quad \forall r \in \mathcal{R},\; z_r = 1
\end{equation}

\begin{equation}\label{con:tw-late}
  s_{v_r,\, m_r^P} \leq l_r \quad \forall r \in \mathcal{R},\; z_r = 1
\end{equation}

where s_{v_r,\, m_r^P} is the scheduled service time at pickup meeting point m_r^P of vehicle
v_r, and [e_r, l_r] is the pickup time window for request r.

--- CONSTRAINT CLASS 3: In-Vehicle Ride Time ---
\subsection*{In-Vehicle Ride Time}
Description: The time a passenger spends riding in the vehicle (from pickup meeting point to
dropoff meeting point) must not exceed the maximum allowable ride time.

\begin{equation}\label{con:ridetime}
  s_{v_r,\, m_r^D} - s_{v_r,\, m_r^P} \leq L^{\max} \quad \forall r \in \mathcal{R},\; z_r = 1
\end{equation}

where s_{v_r,\, m_r^D} is the scheduled service time at dropoff meeting point m_r^D, and
L^{\max} is the maximum allowable in-vehicle ride time.

--- CONSTRAINT CLASS 4: Walking Radius ---
\subsection*{Walking Radius}
Description: The assigned pickup and dropoff meeting points must lie within the maximum walking
radius of the passenger's origin and destination, respectively.

\begin{equation}\label{con:walk-pickup}
  d(o_r,\, m_r^P) \leq \rho^P \quad \forall r \in \mathcal{R},\; z_r = 1
\end{equation}

\begin{equation}\label{con:walk-dropoff}
  d(\delta_r,\, m_r^D) \leq \rho^D \quad \forall r \in \mathcal{R},\; z_r = 1
\end{equation}

where d(o_r, m_r^P) is the Euclidean distance from origin o_r to pickup meeting point m_r^P,
\rho^P is the maximum pickup walking radius, and \rho^D is the maximum dropoff walking radius.

Note: These constraints are equivalent to the candidate set filters M_r^P and M_r^D defined
in the problem setup; they are stated here explicitly to make the MILP formulation complete.

--- CONSTRAINT CLASS 5: Precedence (Pickup Before Dropoff) ---
\subsection*{Precedence: Pickup Before Dropoff}
Description: For each accepted request, the pickup meeting point node must appear before the
dropoff meeting point node in the assigned vehicle's route, and the vehicle must serve the
pickup before the dropoff in time.

\begin{equation}\label{con:precedence-pos}
  \pi_r^P < \pi_r^D \quad \forall r \in \mathcal{R},\; z_r = 1
\end{equation}

\begin{equation}\label{con:precedence-time}
  s_{v_r,\, m_r^P} + t(m_r^P,\, m_r^D) \leq s_{v_r,\, m_r^D} \quad \forall r \in \mathcal{R},\; z_r = 1
\end{equation}

where \pi_r^P and \pi_r^D are the route insertion positions of the pickup and dropoff nodes
(both on vehicle v_r), and t(m_r^P, m_r^D) is the travel time from pickup to dropoff meeting
point (a lower bound on the in-vehicle segment).

--- CONSTRAINT CLASS 6: Route Time Consistency ---
\subsection*{Route Time Consistency}
Description: Scheduled service times must be consistent with travel times along each vehicle's
route — the vehicle cannot arrive at a node before it has had time to travel from the previous
node.

\begin{equation}\label{con:time-consistency}
  s_{v,\, i+1} \geq s_{v,\, i} + t(\sigma_v(i),\, \sigma_v(i+1))
  \quad \forall v \in \mathcal{V},\; \forall i < |\sigma_v|
\end{equation}

where \sigma_v(i) is the i-th node in vehicle v's route, and t(\sigma_v(i), \sigma_v(i+1))
is the travel time from the i-th to the (i+1)-th node.

--- CONSTRAINT CLASS 7: Maximum Route Duration ---
\subsection*{Maximum Route Duration}
Description: The total operating time of each vehicle from its first service node to its last
must not exceed the vehicle's maximum shift duration.

\begin{equation}\label{con:route-duration}
  s_{v,\, |\sigma_v|} - s_{v,\, 1} \leq T_v^{\max} \quad \forall v \in \mathcal{V}
\end{equation}

where s_{v, 1} is the scheduled time at the first node in vehicle v's route, s_{v, |\sigma_v|}
is the scheduled time at the last node, and T_v^{\max} is the maximum allowable route duration
(shift length) for vehicle v.

--- DOMAIN AND BINARY CONSTRAINTS ---
After all seven classes, add a final \subsection*{Domain Constraints} with:

\begin{align}
  z_r &\in \{0,1\} \quad &&\forall r \in \mathcal{R} \label{con:binary-z}\\
  v_r &\in \mathcal{V} \quad &&\forall r \in \mathcal{R},\; z_r = 1 \label{con:domain-v}\\
  m_r^P &\in M_r^P \quad &&\forall r \in \mathcal{R},\; z_r = 1 \label{con:domain-mp}\\
  m_r^D &\in M_r^D \quad &&\forall r \in \mathcal{R},\; z_r = 1 \label{con:domain-md}
\end{align}

End file with a summary table using tabular{lll} with columns:
  Label | Class | Variables Involved
listing one row per constraint label (con:capacity through con:domain-md).
  </action>

  <verify>
    grep -c "label{con:" model/constraints.tex
    grep "label{con:capacity}" model/constraints.tex
    grep "label{con:tw-early}" model/constraints.tex
    grep "label{con:ridetime}" model/constraints.tex
    grep "label{con:walk-pickup}" model/constraints.tex
    grep "label{con:precedence-pos}" model/constraints.tex
    grep "label{con:time-consistency}" model/constraints.tex
  </verify>

  <acceptance_criteria>
    - model/constraints.tex exists and is non-empty
    - File contains at least 11 \label{con:*} entries (one per constraint expression)
    - All required labels present: con:capacity, con:tw-early, con:tw-late,
      con:ridetime, con:walk-pickup, con:walk-dropoff, con:precedence-pos,
      con:precedence-time, con:time-consistency, con:route-duration, con:binary-z
    - The string T_v^{\max} appears (maximum route duration bound)
    - The string L^{\max} appears (ride time bound)
    - The string \rho^P appears (pickup walking radius)
    - The string \rho^D appears (dropoff walking radius)
    - The string z_r \in \{0,1\} appears (binary domain)
    - The string \pi_r^P < \pi_r^D appears (positional precedence)
    - No \documentclass or \begin{document} appears (fragment only)
    - Comment "%% constraints.tex" appears on line 1
    - Summary table with tabular{lll} environment appears at end of file
  </acceptance_criteria>

  <done>model/constraints.tex exists as a LaTeX fragment with seven constraint classes (capacity,
  time windows, ride time, walking radius, precedence, route time consistency, maximum route duration),
  domain constraints, and a summary table — all with unique \label{con:*} labels that Phase 2 can
  reference directly in the MILP formulation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Author → LaTeX files | Mathematical writing; no external data, no code execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | constraints.tex | accept | Version-controlled in git; notation drift detectable via diff against notation.tex |
| T-01-02 | Information Disclosure | model/*.tex | accept | Academic content only; no sensitive data or credentials |
| T-01-03 | Tampering | Constraint labels | mitigate | Acceptance criteria verify every required label is present; Phase 2 plan will grep for these labels before use |

Note: This is a mathematical writing phase with no code execution, no external APIs, and
no user data. The primary integrity risk is notation drift between files — mitigated by
the accept criteria requiring exact symbol matching with notation.tex.
</threat_model>

<verification>
After task completes:
1. Run: ls -la model/constraints.tex — file must exist
2. Run: grep -c "label{con:" model/constraints.tex — expect >= 11
3. Run: grep "subsection" model/constraints.tex — expect 8 entries (7 classes + domain)
4. Notation cross-check: grep for symbols in constraints.tex against notation.tex
   — specifically: Q_v, L^{\max}, \rho^P, \rho^D, \pi_r^P, \pi_r^D, s_{v_r, T_v^{\max}
5. Manual: confirm each constraint has a verbal description sentence before the equation
</verification>

<success_criteria>
- model/constraints.tex: seven constraint classes + domain block, each with labeled equation(s)
- All 11 required \label{con:*} labels present and unique (including con:route-duration)
- Every symbol in constraints.tex matches a symbol defined in model/notation.tex
- Summary table at end maps all labels to constraint class and variables
- File is a clean LaTeX fragment ready for \input in model.tex
</success_criteria>

<output>
After completion, create .planning/phases/01-problem-formulation-model-structure/01-02-SUMMARY.md
with:
- Files created: model/constraints.tex
- Constraint label inventory (all con:* labels listed)
- Any notation additions required beyond notation.tex (should be zero — flag if any)
- Note any constraint that required linearization assumptions for MILP Phase 2
</output>
