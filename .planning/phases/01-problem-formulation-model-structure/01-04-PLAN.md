---
phase: 01-problem-formulation-model-structure
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - model/model.tex
  - model/three-layer.tex
autonomous: true
requirements:
  - PROB-01
  - PROB-02
  - PROB-03
  - PROB-04
  - PROB-05
  - CHOICE-01
  - CHOICE-02
  - CHOICE-03
  - CHOICE-04
must_haves:
  truths:
    - "A single compilable LaTeX document model.tex assembles all Phase 1 fragments"
    - "The three-layer coupled model (service generation, passenger response, dispatch) is described with formal notation in three-layer.tex"
    - "The rolling horizon feedback loop is described with the re-optimization trigger condition"
    - "model.tex compiles without LaTeX errors when run with pdflatex"
    - "The assembled document can be handed directly to Phase 2 as the algorithm specification"
  artifacts:
    - path: "model/model.tex"
      provides: "Self-contained compilable model document assembling all fragments"
      contains: "\\input{model/notation}"
    - path: "model/three-layer.tex"
      provides: "Three-layer coupled model description with formal notation"
      contains: "\\label{sec:three-layer}"
  key_links:
    - from: "model/model.tex"
      to: "model/notation.tex"
      via: "\\input{model/notation}"
      pattern: "\\\\input\\{model/notation\\}"
    - from: "model/model.tex"
      to: "model/three-layer.tex"
      via: "\\input{model/three-layer}"
      pattern: "\\\\input\\{model/three-layer\\}"
    - from: "model/three-layer.tex"
      to: "Phase 2 algorithm"
      via: "Layer 1 candidate generation maps to HEUR-01; Layer 3 rolling horizon maps to HEUR-04"
      pattern: "Layer 1\\|Layer 2\\|Layer 3"
---

<objective>
Assemble all Phase 1 LaTeX fragments into a single self-contained model document (model.tex)
and write the three-layer coupled model description (three-layer.tex).

Purpose: model.tex is the deliverable handed to Phase 2. It must compile cleanly, contain
all notation, constraints, and choice model, and include the three-layer architecture
description that maps directly to the algorithm structure (HEUR-01 through HEUR-04).

Output:
- model/three-layer.tex — formal description of the three-layer coupled model with
  rolling horizon feedback, using notation from plans 01-03
- model/model.tex — compilable LaTeX document that \input's all four fragments and
  produces a complete, readable model writeup
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-problem-formulation-model-structure/01-CONTEXT.md
@.planning/phases/01-problem-formulation-model-structure/01-01-SUMMARY.md
@.planning/phases/01-problem-formulation-model-structure/01-02-SUMMARY.md
@.planning/phases/01-problem-formulation-model-structure/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write three-layer coupled model description (model/three-layer.tex)</name>

  <read_first>
    model/notation.tex — use only symbols defined there; do not introduce new symbols
    model/problem-definition.tex — Layer 1 references M_r^P, M_r^D, k^{top}; Layer 3 references H, Δ
    model/choice-model.tex — Layer 2 references P_{rb}^k, U_{rb}^k, U_{r0}^k
    .planning/phases/01-problem-formulation-model-structure/01-CONTEXT.md — confirms sequential
    coupling with feedback: service generation → passenger response → dispatch with rolling horizon
  </read_first>

  <files>model/three-layer.tex</files>

  <action>
Create model/three-layer.tex as a LaTeX fragment (no \documentclass). Add comment on line 1:
%% three-layer.tex — three-layer coupled model: service generation, passenger response, dispatch

Write \section*{Three-Layer Coupled Model}\label{sec:three-layer} as the opening.

Write an introductory paragraph:
"The many-to-many DRT system with bidirectional meeting points operates through three
sequentially coupled layers, executed online upon each request arrival and periodically
re-optimized via a rolling horizon mechanism. Figure~\ref{fig:three-layer} (Phase~6)
illustrates the architecture. The three layers are: (1) service offer generation,
(2) passenger response, and (3) dynamic dispatch and re-optimization."

--- LAYER 1: Service Offer Generation ---
\subsection*{Layer 1: Service Offer Generation}\label{sec:layer1}

"Upon arrival of request r at time t_r^{\text{req}}, the system generates a set of
candidate service offer bundles \hat{\mathcal{B}}_r \subseteq \mathcal{B}_r as follows:

\begin{enumerate}
  \item \textbf{Candidate pickup set:} Compute M_r^P = \{ m \in \mathcal{M} \mid d(o_r, m) \leq \rho^P \}.
        Retain the top-$k^{\text{top}}$ candidates ranked by proximity to o_r.
  \item \textbf{Candidate dropoff set:} Compute M_r^D = \{ m \in \mathcal{M} \mid d(\delta_r, m) \leq \rho^D \}.
        Retain the top-$k^{\text{top}}$ candidates ranked by proximity to \delta_r.
  \item \textbf{Bundle enumeration:} For each pair (m^P, m^D) \in M_r^P \times M_r^D,
        evaluate feasibility against constraints~\ref{con:capacity}--\ref{con:time-consistency}
        for each vehicle v \in \mathcal{V} and each insertion position pair (\pi^P, \pi^D).
  \item \textbf{Bundle selection:} Select the bundle b^* = \arg\max_{b \in \hat{\mathcal{B}}_r}
        \mathbb{E}[\text{system benefit} \mid b, \mathbf{x}_r] that maximizes expected
        system objective contribution, subject to feasibility.
\end{enumerate}

The output of Layer 1 is the online decision vector \mathbf{x}_r = (z_r, m_r^P, m_r^D, v_r,
\pi_r^P, \pi_r^D) (Equation~\ref{eq:decvec}) and the service offer bundle b^* presented
to the passenger."

--- LAYER 2: Passenger Response ---
\subsection*{Layer 2: Passenger Response}\label{sec:layer2}

"Given the offered bundle b^*, passenger r of type k accepts with probability
P_{rb^*}^k (Equation~\ref{eq:choice-prob}) and rejects (chooses outside option) with
probability P_{r0}^k (Equation~\ref{eq:outside-prob}).

The realized acceptance indicator is:
\begin{equation}\label{eq:acceptance-indicator}
  z_r \sim \text{Bernoulli}(P_{rb^*}^k)
\end{equation}

If z_r = 1, the vehicle route \sigma_{v_r} is updated by inserting the pickup node m_r^P
at position \pi_r^P and the dropoff node m_r^D at position \pi_r^D. If z_r = 0, the
decision vector is discarded and the vehicle routes remain unchanged.

The coupling between Layer 1 and Layer 2 is \emph{anticipatory}: the system selects b^*
to maximize expected system benefit, accounting for the probability that the passenger
will accept. This distinguishes the model from a deterministic assignment approach."

--- LAYER 3: Dynamic Dispatch and Rolling Horizon Re-optimization ---
\subsection*{Layer 3: Dynamic Dispatch and Rolling Horizon Re-optimization}\label{sec:layer3}

"Vehicle dispatch follows the committed route schedules \{\sigma_v, \mathbf{s}_v\}_{v \in \mathcal{V}}.
Every \Delta minutes, a rolling horizon re-optimization is triggered over the active request
window [t^{\text{now}}, t^{\text{now}} + H]:

\begin{enumerate}
  \item \textbf{Active set:} Identify \mathcal{R}^{\text{act}}(t^{\text{now}}) — all accepted
        requests not yet completed at time t^{\text{now}}.
  \item \textbf{Re-optimization:} Apply ALNS (Adaptive Large Neighborhood Search) to
        reassign and reorder requests in \mathcal{R}^{\text{act}}(t^{\text{now}}) across
        vehicles, subject to constraints~\ref{con:capacity}--\ref{con:time-consistency}.
  \item \textbf{Commitment:} Commit the updated routes for the next \Delta minutes;
        nodes already served are locked and cannot be reassigned.
\end{enumerate}

The rolling horizon creates a feedback loop from Layer 3 back to Layer 1: the updated
vehicle schedules after re-optimization change the feasibility and cost of future bundle
insertions, influencing the next Layer 1 evaluation.

\begin{equation}\label{eq:rh-trigger}
  \text{Re-optimize at times } t^{\text{now}} = 0,\; \Delta,\; 2\Delta,\; 3\Delta,\; \ldots
\end{equation}

Typical parameter values: H = 30 minutes (horizon window), \Delta = 5 minutes (re-optimization
frequency). These are configurable and subject to sensitivity analysis in Phase~4."

--- COUPLING SUMMARY TABLE ---
After the three subsections, add:

\subsection*{Layer Coupling Summary}

\begin{table}[h]
\centering
\caption{Three-layer model coupling structure}
\label{tab:layer-coupling}
\begin{tabular}{llll}
\toprule
Layer & Input & Output & Coupling \\
\midrule
1: Service Generation & Request r, routes \{\sigma_v\} & Bundle b^*, decision \mathbf{x}_r & Feeds Layer 2 \\
2: Passenger Response & Bundle b^*, type k & Acceptance z_r & Updates routes if z_r=1 \\
3: Rolling Horizon & Active set \mathcal{R}^{\text{act}} & Updated routes \{\sigma_v\} & Feeds back to Layer 1 \\
\bottomrule
\end{tabular}
\end{table}
  </action>

  <verify>
    grep "label{sec:three-layer}" model/three-layer.tex
    grep "label{sec:layer1}" model/three-layer.tex
    grep "label{sec:layer2}" model/three-layer.tex
    grep "label{sec:layer3}" model/three-layer.tex
    grep "label{eq:rh-trigger}" model/three-layer.tex
    grep "label{tab:layer-coupling}" model/three-layer.tex
  </verify>

  <acceptance_criteria>
    - model/three-layer.tex exists and is non-empty
    - \label{sec:three-layer} appears (section labeled for cross-reference)
    - \label{sec:layer1}, \label{sec:layer2}, \label{sec:layer3} all appear
    - \label{eq:acceptance-indicator} appears (Bernoulli acceptance)
    - \label{eq:rh-trigger} appears (rolling horizon trigger equation)
    - \label{tab:layer-coupling} appears (coupling summary table)
    - The string k^{\text{top}} appears (candidate count parameter)
    - The string \Delta appears (re-optimization frequency)
    - The string H = 30 appears (horizon window default value)
    - The string \Delta = 5 appears (re-optimization frequency default value)
    - References to constraint labels appear: \ref{con:capacity} and \ref{con:time-consistency}
    - References to equation labels from other fragments: \ref{eq:decvec}, \ref{eq:choice-prob}
    - No \documentclass or \begin{document} appears (fragment only)
    - Comment "%% three-layer.tex" appears on line 1
  </acceptance_criteria>

  <done>model/three-layer.tex exists as a LaTeX fragment with three subsections (Layer 1
  service generation with k^top candidate enumeration, Layer 2 passenger response with
  Bernoulli acceptance, Layer 3 rolling horizon with H and Δ parameters), a coupling summary
  table, and cross-references to all constraint and equation labels from plans 01-03.</done>
</task>

<task type="auto">
  <name>Task 2: Assemble model.tex and verify compilation (model/model.tex)</name>

  <read_first>
    model/notation.tex — must exist (from plan 01); check file exists before writing \input
    model/problem-definition.tex — must exist (from plan 01)
    model/constraints.tex — must exist (from plan 02)
    model/choice-model.tex — must exist (from plan 03)
    model/three-layer.tex — must exist (from task 1 above)
  </read_first>

  <files>model/model.tex</files>

  <action>
Create model/model.tex as a complete, compilable LaTeX document. This is the ONLY file in
the model/ directory with \documentclass.

Use the following exact structure:

```latex
%% model.tex — Work 3: Many-to-Many DRT with Bidirectional Meeting Points
%% Self-contained model document for Phase 1 handoff to Phase 2 (Algorithm Development)
%% Compile with: pdflatex model.tex

\documentclass[12pt, a4paper]{article}

% Packages
\usepackage{amsmath, amssymb, amsthm}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}
\usepackage{geometry}
\usepackage{hyperref}
\usepackage{cleveref}
\geometry{margin=2.5cm}

% Title
\title{Work 3: Many-to-Many DRT with Bidirectional Meeting Points\\
       \large Mathematical Model — Phase 1 Formulation}
\author{[Author]}
\date{2026}

\begin{document}
\maketitle
\tableofcontents
\newpage

% -------------------------------------------------------
\section{Notation}\label{sec:notation}
\input{notation}

% -------------------------------------------------------
\section{Problem Definition}\label{sec:problem}
\input{problem-definition}

% -------------------------------------------------------
\section{Operational Constraints}\label{sec:constraints}
\input{constraints}

% -------------------------------------------------------
\section{Passenger Choice Model}\label{sec:choice}
\input{choice-model}

% -------------------------------------------------------
\section{Three-Layer Coupled Model}\label{sec:model}
\input{three-layer}

% -------------------------------------------------------
\section{Summary and Phase 2 Handoff}\label{sec:handoff}

This document constitutes the complete mathematical foundation for Work 3.
Table~\ref{tab:handoff} maps each model component to the corresponding Phase~2
implementation requirement.

\begin{table}[h]
\centering
\caption{Phase 1 to Phase 2 handoff map}
\label{tab:handoff}
\begin{tabular}{lll}
\toprule
Model Component & Section & Phase 2 Requirement \\
\midrule
Notation table & \S\ref{sec:notation} & All (symbol reference) \\
Network + meeting point sets & \S\ref{sec:problem} & HEUR-01 (candidate generation) \\
Service offer bundle & \S\ref{sec:problem} & EXACT-01, HEUR-02 \\
System objective & \S\ref{sec:problem} & EXACT-01 (MILP objective) \\
Online decision vector & \S\ref{sec:problem} & HEUR-02 (insertion evaluator) \\
Capacity constraint & \S\ref{sec:constraints} & EXACT-01, HEUR-03 \\
Time window constraints & \S\ref{sec:constraints} & EXACT-01, HEUR-03 \\
Ride time constraint & \S\ref{sec:constraints} & EXACT-01, HEUR-03 \\
Walking radius constraints & \S\ref{sec:constraints} & HEUR-01 (filter) \\
Precedence constraints & \S\ref{sec:constraints} & EXACT-01, HEUR-03 \\
Route time consistency & \S\ref{sec:constraints} & EXACT-01, HEUR-03 \\
MNL utility function & \S\ref{sec:choice} & Phase 3 simulation \\
Passenger type profiles & \S\ref{sec:choice} & Phase 3 simulation \\
Choice probability & \S\ref{sec:choice} & HEUR-02 (expected benefit) \\
Layer 1: Service generation & \S\ref{sec:model} & HEUR-01, HEUR-02 \\
Layer 2: Passenger response & \S\ref{sec:model} & HEUR-02 \\
Layer 3: Rolling horizon & \S\ref{sec:model} & HEUR-04, HEUR-05 \\
\bottomrule
\end{tabular}
\end{table}

\end{document}
```

Note: The \input{} commands use filenames without the model/ prefix because model.tex
is compiled from within the model/ directory. When compiling, run:
  cd model && pdflatex model.tex

After writing model.tex, attempt compilation:
  cd model && pdflatex -interaction=nonstopmode model.tex 2>&1 | tail -20

If compilation fails due to missing packages, note the error in the SUMMARY but do not
block — the file structure is the deliverable, not the PDF. Common issues:
  - longtable not installed: add \usepackage{longtable} (already included above)
  - booktabs not installed: add \usepackage{booktabs} (already included above)
  - cleveref conflicts: remove \usepackage{cleveref} if it causes errors

If compilation succeeds, note "model.pdf generated successfully" in the SUMMARY.
  </action>

  <verify>
    grep "input{notation}" model/model.tex
    grep "input{problem-definition}" model/model.tex
    grep "input{constraints}" model/model.tex
    grep "input{choice-model}" model/model.tex
    grep "input{three-layer}" model/model.tex
    grep "label{tab:handoff}" model/model.tex
  </verify>

  <acceptance_criteria>
    - model/model.tex exists and is non-empty
    - \documentclass appears exactly once (this is the only file with it)
    - \input{notation} appears (notation fragment included)
    - \input{problem-definition} appears (problem definition included)
    - \input{constraints} appears (constraints included)
    - \input{choice-model} appears (choice model included)
    - \input{three-layer} appears (three-layer model included)
    - \label{tab:handoff} appears (Phase 2 handoff table labeled)
    - \tableofcontents appears (document has TOC)
    - All five required packages appear: amsmath, booktabs, longtable, geometry, hyperref
    - The handoff table contains at least 15 rows mapping model components to Phase 2 requirements
    - Comment "%% model.tex" appears on line 1
  </acceptance_criteria>

  <done>model/model.tex exists as a compilable LaTeX document that \input's all five fragments
  (notation, problem-definition, constraints, choice-model, three-layer) and includes a
  Phase 2 handoff table mapping every model component to its corresponding algorithm
  requirement (EXACT-01 through HEUR-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Author → LaTeX files | Mathematical writing; no external data, no code execution |
| model.tex → fragment files | \input{} file inclusion; fragments must exist before compilation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | model.tex | accept | Version-controlled; any change to \input order is visible in git diff |
| T-01-02 | Denial of Service | pdflatex compilation | accept | Compilation failure does not block deliverable; model.tex structure is the artifact |
| T-01-03 | Tampering | Cross-reference labels | mitigate | Acceptance criteria verify all \ref{} targets exist in their source files; broken refs produce LaTeX warnings detectable in compilation log |
| T-01-04 | Information Disclosure | model/*.tex | accept | Academic content only; no sensitive data |

Note: This is a documentation assembly phase. The primary risk is broken cross-references
between fragments — mitigated by acceptance criteria that verify all \label{} targets exist.
</threat_model>

<verification>
After both tasks complete:
1. Run: ls -la model/ — expect: notation.tex, problem-definition.tex, constraints.tex,
   choice-model.tex, three-layer.tex, model.tex (6 files total)
2. Run: grep -c "input{" model/model.tex — expect exactly 5
3. Run: grep "label{tab:handoff}" model/model.tex — must be present
4. Cross-reference integrity: grep all \ref{} targets in model.tex and verify each
   \label{} exists in the corresponding fragment file
5. Attempt compilation: cd model && pdflatex -interaction=nonstopmode model.tex
   — note result (success or specific errors) in SUMMARY
6. Manual: open model.tex and confirm the handoff table covers all 17 rows listed in the action
</verification>

<success_criteria>
- model/three-layer.tex: three subsections with formal notation, rolling horizon trigger
  equation, coupling summary table, cross-references to constraint and equation labels
- model/model.tex: compilable document with \tableofcontents, all five \input{} calls,
  and Phase 2 handoff table with 15+ rows
- All six model/ files exist: notation.tex, problem-definition.tex, constraints.tex,
  choice-model.tex, three-layer.tex, model.tex
- pdflatex compilation produces no fatal errors (warnings acceptable)
- The assembled document is self-contained: a reader can understand the full model
  without consulting any other Phase 1 file
</success_criteria>

<output>
After completion, create .planning/phases/01-problem-formulation-model-structure/01-04-SUMMARY.md
with:
- Files created: model/three-layer.tex, model/model.tex
- Compilation result (success / errors encountered)
- Complete file inventory for model/ directory
- Cross-reference integrity status (any broken \ref{} targets)
- Phase 2 readiness statement: confirm model.tex is ready for algorithm development handoff
- Any open items (e.g., β parameter confirmation, figure placeholder for FIG-02)
</output>
