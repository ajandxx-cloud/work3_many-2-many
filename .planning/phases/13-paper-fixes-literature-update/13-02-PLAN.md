---
phase: 13-paper-fixes-literature-update
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - paper/main.tex
  - paper/sections/model.tex
  - paper/sections/algorithm.tex
autonomous: true
requirements: [BEHAV-01, BEHAV-02, BEHAV-03]
must_haves:
  truths:
    - "A notation/units table (tab:notation) exists in the paper appendix listing all symbols with units"
    - "A worked utility example with explicit numbers appears at the end of Section 4.2 in model.tex"
    - "A commitment assumption paragraph appears in algorithm.tex after the committed-nodes sentence"
  artifacts:
    - path: paper/main.tex
      provides: "Appendix section with notation table"
      contains: "tab:notation"
    - path: paper/sections/model.tex
      provides: "Worked utility example with beta values"
      contains: "beta_1"
    - path: paper/sections/algorithm.tex
      provides: "Commitment assumption paragraph"
      contains: "commitment"
  key_links:
    - from: paper/sections/model.tex
      to: "tab:notation"
      via: "Table~\\ref{tab:notation}"
      pattern: "tab:notation"
    - from: paper/sections/algorithm.tex
      to: "committed nodes sentence"
      via: "lines 174-177"
      pattern: "commitment"
---

<objective>
Add three behavioral consistency materials to the paper:
1. A notation/units table in the appendix (already referenced at model.tex:465 as tab:notation)
2. A worked utility example with explicit numbers at the end of Section 4.2 in model.tex
3. A commitment assumption paragraph in algorithm.tex after the committed-nodes sentence

Purpose: Address GPT-5 reviewer concern that the behavioral model lacks concrete grounding
and that the commitment assumption is implicit rather than stated.
Output: Three modified LaTeX files; notation table, worked example, and commitment paragraph
all present and internally consistent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-paper-fixes-literature-update/13-CONTEXT.md

<interfaces>
<!-- Key values from model.tex for the worked example -->
<!-- Beta values (walk-sensitive type, from model.tex lines 275-276): -->
<!--   beta_walk = -0.020 (utility/meter) -->
<!--   beta_wait = -0.04  (utility/minute) -->
<!--   beta_ivt  = -0.02  (utility/minute) -->
<!--   beta_price = -0.05 (utility/CNY) -->
<!-- Outside option penalty: 5.0 -->
<!-- Walking speed: 80 m/min -->
<!-- Notation table already referenced: Table~\ref{tab:notation} at model.tex line 465 -->
<!-- Committed nodes sentence location: algorithm.tex lines 174-177 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add notation/units table to appendix in main.tex</name>
  <files>paper/main.tex</files>
  <read_first>paper/main.tex</read_first>
  <action>
Read paper/main.tex. The file currently ends at line 46 with \end{document}. Insert an
appendix section with the notation table BEFORE \end{document}. Add after
\bibliography{references} and before \end{document}:

\appendix

\section{Notation and Units Reference}
\label{sec:appendix-notation}

\begin{table}[h]
\centering
\caption{Notation and units reference}
\label{tab:notation}
\begin{tabular}{lll}
\toprule
Symbol & Description & Unit \\
\midrule
$r$ & Passenger request index & --- \\
$v$ & Vehicle index & --- \\
$m_r^P$ & Pickup meeting point for request $r$ & --- \\
$m_r^D$ & Dropoff meeting point for request $r$ & --- \\
$M_r^P$ & Candidate pickup meeting point set & --- \\
$M_r^D$ & Candidate dropoff meeting point set & --- \\
$b_r$ & Service offer bundle $(m_r^P, m_r^D, \tau_r, p_r)$ & --- \\
$\tau_r$ & Scheduled pickup time & seconds \\
$p_r$ & Fare offered to passenger $r$ & CNY \\
$d_{\text{walk}}$ & Walking distance to/from meeting point & meters \\
$t_{\text{wait}}$ & Waiting time at meeting point & minutes \\
$t_{\text{ivt}}$ & In-vehicle travel time & minutes \\
$\rho$ & Maximum walking radius & meters \\
$H$ & Rolling horizon window length & minutes \\
$\Delta$ & Re-optimisation interval & minutes \\
$\Gamma$ & Rejection penalty in ALNS objective & CNY \\
$U_r(b)$ & Deterministic utility of bundle $b$ for request $r$ & utils \\
$P_{\text{accept}}(b)$ & MNL acceptance probability & [0,1] \\
$z_r$ & Acceptance indicator ($z_r \sim \text{Bernoulli}(P_{\text{accept}})$) & \{0,1\} \\
$\beta_1^k$ & Walk disutility coefficient for type $k$ & utils/meter \\
$\beta_2^k$ & Wait disutility coefficient for type $k$ & utils/minute \\
$\beta_3^k$ & IVT disutility coefficient for type $k$ & utils/minute \\
$\beta_4^k$ & Price disutility coefficient for type $k$ & utils/CNY \\
$\mathcal{R}^{\text{acc}}$ & Set of accepted requests & --- \\
$\mathcal{R}^{\text{act}}(t)$ & Active request set at time $t$ & --- \\
$\sigma_v$ & Route sequence for vehicle $v$ & --- \\
\bottomrule
\end{tabular}
\end{table}

The full file should end: \bibliography{references} ... \appendix ... \end{document}
  </action>
  <verify>
    <automated>grep -n "tab:notation" paper/main.tex && grep -n "appendix" paper/main.tex && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "tab:notation" paper/main.tex returns a match (the label definition)
    - grep "\\appendix" paper/main.tex returns a match
    - grep "Unit" paper/main.tex returns a match (table header)
    - The \end{document} line is still the last line of the file
  </acceptance_criteria>
  <done>Appendix with tab:notation table present in main.tex; table lists symbols with units (seconds/minutes/meters/CNY).</done>
</task>

<task type="auto">
  <name>Task 2: Add worked utility example to model.tex Section 4.2</name>
  <files>paper/sections/model.tex</files>
  <read_first>paper/sections/model.tex</read_first>
  <action>
Read paper/sections/model.tex. Locate the end of the beta parameter block (around lines
275-295, after the footnote about parameter plausibility). Find the closing of the
footnote (the line with just `}` closing the \footnote). After that closing brace,
insert the following worked example paragraph:

\paragraph{Worked utility example.}
Consider a walk-sensitive passenger ($k = \text{walk}$) with
$\beta_1^{\text{walk}} = -0.020$\,utils/m,
$\beta_2^{\text{walk}} = -0.04$\,utils/min,
$\beta_3^{\text{walk}} = -0.02$\,utils/min, and
$\beta_4^{\text{walk}} = -0.05$\,utils/CNY.
Suppose the system offers a bundle with walking distance $d_{\text{walk}} = 300$\,m,
waiting time $t_{\text{wait}} = 5$\,min, in-vehicle time $t_{\text{ivt}} = 20$\,min,
and fare $p = 8$\,CNY.
The deterministic utility is:
\begin{align}
  U &= \beta_1 \cdot d_{\text{walk}} + \beta_2 \cdot t_{\text{wait}}
       + \beta_3 \cdot t_{\text{ivt}} + \beta_4 \cdot p \notag \\
    &= (-0.020)(300) + (-0.04)(5) + (-0.02)(20) + (-0.05)(8) \notag \\
    &= -6.0 - 0.2 - 0.4 - 0.4 = -7.0\text{ utils.} \notag
\end{align}
With an outside option penalty of $\mu_0 = 5.0$, the acceptance probability is:
\begin{equation}
  P_{\text{accept}} = \frac{e^{-7.0}}{e^{-7.0} + e^{-5.0}}
  = \frac{0.000912}{0.000912 + 0.006738} \approx 0.119.
\end{equation}
This low acceptance probability (11.9\%) reflects the 300\,m walking requirement,
which is highly penalised for walk-sensitive passengers. Reducing walking to 100\,m
would raise $U$ to $-3.0$ and acceptance to approximately 47\%, illustrating the
sensitivity of demand to meeting point placement.

Insert this block immediately after the closing `}` of the \footnote block (the footnote
about parameter plausibility that ends around line 294). Do not modify any other content.
  </action>
  <verify>
    <automated>grep -n "Worked utility example" paper/sections/model.tex && grep -n "0\.119" paper/sections/model.tex && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "Worked utility example" paper/sections/model.tex returns a match
    - grep "0\.119" paper/sections/model.tex returns a match (the computed acceptance probability)
    - grep "beta_1" paper/sections/model.tex OR grep "\\\\beta_1" paper/sections/model.tex returns a match in the worked example
    - The beta parameter equations (eq:beta-walk etc.) are unchanged
  </acceptance_criteria>
  <done>Worked utility example with explicit numbers (walk=300m, wait=5min, ivt=20min, fare=8CNY, P_accept≈0.119) present in model.tex after the beta parameter block.</done>
</task>

<task type="auto">
  <name>Task 3: Add commitment assumption paragraph to algorithm.tex</name>
  <files>paper/sections/algorithm.tex</files>
  <read_first>paper/sections/algorithm.tex</read_first>
  <action>
Read paper/sections/algorithm.tex. Locate the committed-nodes sentence at lines 174-177:
  "Committed nodes (already served or within the commitment horizon) are locked and cannot
   be reassigned. The ALNS runs for 100 iterations..."

After the sentence "Committed nodes (already served or within the commitment horizon) are
locked and cannot be reassigned." and BEFORE "The ALNS runs for 100 iterations...",
insert the following paragraph:

\paragraph{Commitment assumption.}
Once a service offer bundle $b_r^* = (m_r^P, m_r^D, \tau_r, p_r)$ is communicated to
passenger $r$ and accepted ($z_r = 1$), the assigned meeting points and scheduled pickup
time are \emph{committed}: they are not subject to re-optimisation in subsequent rolling
horizon cycles. This commitment is operationally necessary — passengers who have begun
walking to a meeting point cannot be redirected — and is enforced by locking the
corresponding nodes in the route graph before each ALNS re-optimisation trigger.
Formally, a node $i \in \sigma_v$ is committed if its scheduled service time satisfies
$\tau_i \leq t^{\text{now}} + \delta_{\text{commit}}$, where $\delta_{\text{commit}} = 5$
minutes is the commitment horizon (equal to the re-optimisation interval $\Delta$).
Uncommitted accepted nodes may be re-sequenced within the vehicle route to improve
efficiency, but their assigned meeting points remain fixed.

Do not modify any other content.
  </action>
  <verify>
    <automated>grep -n "Commitment assumption" paper/sections/algorithm.tex && grep -n "delta_.*commit\|delta_{\\\\text{commit}}" paper/sections/algorithm.tex && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "Commitment assumption" paper/sections/algorithm.tex returns a match
    - grep "commitment horizon" paper/sections/algorithm.tex returns a match
    - grep "operationally necessary" paper/sections/algorithm.tex returns a match
    - The original "Committed nodes" sentence is still present (not replaced, only supplemented)
  </acceptance_criteria>
  <done>Commitment assumption paragraph present in algorithm.tex; explains that accepted offers are committed and not re-optimized after acceptance, with formal definition of commitment horizon.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor→LaTeX source | All edits are to local .tex files; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | Tampering | model.tex beta block | mitigate | Read file first; insert after footnote closing brace; verify original equations unchanged |
| T-13-05 | Tampering | algorithm.tex committed-nodes | mitigate | Read file first; insert paragraph between two specific sentences; verify original sentence still present |
| T-13-06 | Information Disclosure | notation table units | accept | Units are derived from model.tex lines 279-280 (already public in paper) |
</threat_model>

<verification>
After all three tasks:
1. `grep -n "tab:notation" paper/main.tex` returns the label definition in the appendix
2. `grep -n "Worked utility example" paper/sections/model.tex` returns a match
3. `grep -n "Commitment assumption" paper/sections/algorithm.tex` returns a match
4. `grep -n "0\.119" paper/sections/model.tex` returns the computed acceptance probability
5. `grep -n "commitment horizon" paper/sections/algorithm.tex` returns a match
</verification>

<success_criteria>
- Notation table (tab:notation) exists in paper appendix with time in seconds/minutes, walk in meters, fare in CNY
- Worked utility example present in model.tex with explicit beta values, computed utility (-7.0), and acceptance probability (≈0.119)
- Commitment assumption paragraph present in algorithm.tex explaining that accepted offers are locked and not re-optimized
</success_criteria>

<output>
After completion, create `.planning/phases/13-paper-fixes-literature-update/13-02-SUMMARY.md`
</output>
