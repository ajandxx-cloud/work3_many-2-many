---
phase: 13-paper-fixes-literature-update
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - paper/sections/intro.tex
  - paper/sections/conclusion.tex
  - paper/sections/abstract.tex
autonomous: true
requirements: [TEXT-01, TEXT-02, TEXT-03]
must_haves:
  truths:
    - "The strings '2383.85', '3662.33', '34.9' do not appear anywhere under paper/"
    - "intro.tex contribution item 3 cites 35.0% endogenous and 29.2% unconstrained with correct vkm/trip values"
    - "conclusion.tex paragraph 1 and contribution item 3 use v3.0 numbers (11.1 vs 17.1, 35.0%; 15.1 vs 21.3, 29.2%)"
    - "abstract.tex leads with endogenous 35.0% as primary claim; post-hoc 74.3% is parenthetical"
  artifacts:
    - path: paper/sections/intro.tex
      provides: "Corrected contribution item 3 with v3.0 numbers"
      contains: "35.0"
    - path: paper/sections/conclusion.tex
      provides: "Corrected efficiency numbers throughout"
      contains: "35.0"
    - path: paper/sections/abstract.tex
      provides: "Abstract leading with endogenous 35.0% as primary claim"
      contains: "35.0"
  key_links:
    - from: paper/sections/intro.tex
      to: "contribution item 3"
      via: "\\item Empirical demonstration"
      pattern: "35\\.0"
    - from: paper/sections/conclusion.tex
      to: "opening paragraph"
      via: "demonstrated that"
      pattern: "35\\.0"
---

<objective>
Fix all stale numbers in intro.tex, conclusion.tex, and abstract.tex. Replace the old
"2383.85 vs 3662.33 / -34.9%" figures with the v3.0 corrected values, and reorder the
abstract so the endogenous 35.0% result is the primary claim.

Purpose: Eliminate internally inconsistent numbers that contradict the Section 5.2
endogenous comparison already in the paper.
Output: Three corrected LaTeX files; grep confirms no old numbers remain.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-paper-fixes-literature-update/13-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix intro.tex contribution item 3</name>
  <files>paper/sections/intro.tex</files>
  <read_first>paper/sections/intro.tex</read_first>
  <action>
Read paper/sections/intro.tex. Locate the \begin{enumerate} contributions block (around
lines 78-87). Find item 3 which currently reads:

  \item Empirical demonstration of a $-34.9\%$ vehicle efficiency gain (vkm per
        accepted trip: 2383.85 vs.\ 3662.33) over the door-to-door baseline.

Replace it with:

  \item Empirical demonstration of a \textbf{35.0\%} vehicle efficiency gain in
        per-trip vehicle utilisation at equal served share (FullModel 11.1 vs.\
        DoorToDoor\,(capped) 17.1\,vkm/trip, endogenous matched-coverage comparison),
        and a 29.2\% gain under the unconstrained comparison (15.1 vs.\ 21.3\,vkm/trip).

Do not change any other lines.
  </action>
  <verify>
    <automated>grep -n "35\.0" paper/sections/intro.tex && ! grep -q "2383" paper/sections/intro.tex && ! grep -q "3662" paper/sections/intro.tex && ! grep -q "34\.9" paper/sections/intro.tex && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "35\.0" paper/sections/intro.tex returns a match
    - grep "2383" paper/sections/intro.tex returns no match
    - grep "3662" paper/sections/intro.tex returns no match
    - grep "34\.9" paper/sections/intro.tex returns no match
  </acceptance_criteria>
  <done>intro.tex contribution item 3 uses v3.0 numbers; old numbers absent.</done>
</task>

<task type="auto">
  <name>Task 2: Fix conclusion.tex old numbers</name>
  <files>paper/sections/conclusion.tex</files>
  <read_first>paper/sections/conclusion.tex</read_first>
  <action>
Read paper/sections/conclusion.tex. There are two locations with old numbers:

LOCATION A — opening paragraph (around lines 13-15):
  "demonstrated that the full bidirectional model achieves a vehicle efficiency of
   2383.85 vehicle-km per accepted trip, compared to 3662.33 for the door-to-door baseline,
   a reduction of 34.9\%."

Replace with:
  "demonstrated that the full bidirectional model achieves \textbf{35.0\%} lower
   per-trip vehicle utilisation at equal served share (FullModel 11.1 vs.\
   DoorToDoor\,(capped) 17.1\,vkm/trip, endogenous matched-coverage comparison at
   $\approx$22.8\% served share). Under the unconstrained comparison, FullModel achieves
   15.1\,vkm/trip versus 21.3\,vkm/trip for DoorToDoor (29.2\% improvement)."

LOCATION B — contribution item 3 (around line 28):
  "Third, we empirically demonstrated the 34.9\% vehicle efficiency gain"

Replace with:
  "Third, we empirically demonstrated the 35.0\% vehicle efficiency gain (endogenous
   matched-coverage comparison) and 29.2\% gain (unconstrained comparison)"

Do not change any other lines.
  </action>
  <verify>
    <automated>grep -n "35\.0" paper/sections/conclusion.tex && ! grep -q "2383" paper/sections/conclusion.tex && ! grep -q "3662" paper/sections/conclusion.tex && ! grep -q "34\.9" paper/sections/conclusion.tex && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "35\.0" paper/sections/conclusion.tex returns at least 2 matches
    - grep "2383" paper/sections/conclusion.tex returns no match
    - grep "3662" paper/sections/conclusion.tex returns no match
    - grep "34\.9" paper/sections/conclusion.tex returns no match
  </acceptance_criteria>
  <done>conclusion.tex uses v3.0 numbers in both the opening paragraph and contribution item 3.</done>
</task>

<task type="auto">
  <name>Task 3: Update abstract.tex primary claim + grep-verify paper-wide</name>
  <files>paper/sections/abstract.tex</files>
  <read_first>paper/sections/abstract.tex</read_first>
  <action>
Read paper/sections/abstract.tex. The current abstract leads with the post-hoc 74.3%
figure as the primary claim (lines 15-21). Rewrite the results sentence block to lead
with the endogenous 35.0% result:

CURRENT (lines 15-21):
  "Numerical experiments on synthetic scenarios calibrated to Chinese suburban conditions
   show that, at equal served share ($\approx$23.5\%), the full bidirectional model
   achieves \textbf{10.9\,vehicle-km per accepted trip} compared to
   \textbf{42.3\,vkm/trip} for the door-to-door baseline at the same coverage
   level, a reduction of \textbf{74.3\%}. Under the unconstrained
   baseline comparison (FullModel 20.8\% vs.\ DoorToDoor 61.0\% served share),
   FullModel achieves 15.1\,vkm/trip versus 21.3\,vkm/trip for DoorToDoor
   (29.2\% improvement)."

REPLACE WITH:
  "Numerical experiments on synthetic scenarios calibrated to Chinese suburban conditions
   show that, at equal served share ($\approx$22.8\%), the full bidirectional model
   achieves \textbf{11.1\,vkm/trip} compared to \textbf{17.1\,vkm/trip} for the
   DoorToDoor\,(capped) baseline — a \textbf{35.0\%} improvement in per-trip vehicle
   utilisation that cannot be attributed to coverage differences (endogenous
   matched-coverage comparison). Under the unconstrained comparison (FullModel 20.8\%
   vs.\ DoorToDoor 61.0\% served share), FullModel achieves 15.1\,vkm/trip versus
   21.3\,vkm/trip (29.2\% improvement). For reference, a post-hoc matched-coverage
   estimate yields 10.9 vs.\ 42.3\,vkm/trip (74.3\% improvement), providing a
   conservative lower bound."

After editing, run a paper-wide grep to confirm no old numbers remain anywhere:
  grep -rn "2383\|3662\|34\.9" paper/

If any matches are found outside the files already fixed, report them in the summary.
  </action>
  <verify>
    <automated>grep -n "35\.0" paper/sections/abstract.tex && ! grep -rq "2383\|3662" paper/ && echo "PAPER-WIDE CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - grep "35\.0" paper/sections/abstract.tex returns a match
    - grep -rn "2383" paper/ returns no matches
    - grep -rn "3662" paper/ returns no matches
    - grep -rn "34\.9" paper/ returns no matches (note: "34.9" as a standalone number; "134.9" or similar are fine)
  </acceptance_criteria>
  <done>Abstract leads with endogenous 35.0%; paper-wide grep confirms "2383.85", "3662.33", "34.9%" are absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor→LaTeX source | All edits are to local .tex files; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | intro.tex / conclusion.tex | accept | Pure text replacement in local files; no external data |
| T-13-02 | Information Disclosure | old numbers persisting | mitigate | grep-verify after all edits confirms elimination |
</threat_model>

<verification>
After all three tasks:
1. `grep -rn "2383\|3662\|34\.9" paper/` returns zero matches
2. `grep -n "35\.0" paper/sections/intro.tex` returns a match in contribution item 3
3. `grep -n "35\.0" paper/sections/conclusion.tex` returns matches in opening paragraph and contribution item 3
4. `grep -n "35\.0" paper/sections/abstract.tex` returns a match as the primary claim
</verification>

<success_criteria>
- Old numbers "2383.85", "3662.33", "-34.9%" absent from all paper/ files (grep confirms)
- intro.tex, conclusion.tex, abstract.tex all use v3.0 numbers (35.0% endogenous primary, 29.2% unconstrained secondary)
- Abstract leads with endogenous result; post-hoc 74.3% is a parenthetical reference
</success_criteria>

<output>
After completion, create `.planning/phases/13-paper-fixes-literature-update/13-01-SUMMARY.md`
</output>
