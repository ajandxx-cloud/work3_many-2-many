---
phase: 13-paper-fixes-literature-update
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - paper/sections/literature.tex
  - paper/sections/experiments.tex
  - paper/response_to_reviewers.tex
autonomous: true
requirements: [LIT-01, LIT-02, ROB-01, ROB-02]
must_haves:
  truths:
    - "Fielbaum et al. (2021) is cited in Section 2.2 of literature.tex with a positioning sentence"
    - "Table 1 (tab:main-results) in experiments.tex has ± notation on acceptance rate, vkm, and vkm/trip columns"
    - "Section 5.1 of experiments.tex contains a 3-seed justification sentence"
    - "response_to_reviewers.tex notes the addition of Fielbaum et al. (2021)"
  artifacts:
    - path: paper/sections/literature.tex
      provides: "Fielbaum 2021 citation in Section 2.2"
      contains: "fielbaum2021"
    - path: paper/sections/experiments.tex
      provides: "Table 1 with ± notation; 3-seed note in Section 5.1"
      contains: "pm"
    - path: paper/response_to_reviewers.tex
      provides: "Note about Fielbaum addition"
      contains: "Fielbaum"
  key_links:
    - from: paper/sections/literature.tex
      to: "Section 2.2 after Cortenbach discussion"
      via: "\\citet{fielbaum2021}"
      pattern: "fielbaum2021"
    - from: paper/sections/experiments.tex
      to: "Table 1 acceptance rate column"
      via: "\\pm"
      pattern: "\\\\pm"
---

<objective>
Integrate Fielbaum et al. (2021) into the literature review (Section 2.2), add ± std
notation to Table 1 in experiments.tex, add a 3-seed justification note in Section 5.1,
and note the Fielbaum addition in response_to_reviewers.tex.

Purpose: Address GPT-5 MAJOR issue (missing prior art on bidirectional walking flexibility)
and strengthen robustness reporting with confidence intervals.
Output: Four modified LaTeX files; Fielbaum cited in Section 2.2; Table 1 has ± notation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-paper-fixes-literature-update/13-CONTEXT.md

<interfaces>
<!-- Fielbaum bib key: fielbaum2021 (confirmed in paper/references.bib line 309) -->
<!-- Citation style: \citet{key} for inline, \citep{key} for parenthetical -->
<!-- Table 1 location: experiments.tex lines 86-99 (tab:main-results) -->
<!-- ± format used in paper: $X \pm Y$ (see experiments.tex line 122-123 for MILP table) -->
<!-- Section 5.1 seed mention: experiments.tex around line 32 ("seeds 42, 43, 44") -->
<!-- Per-seed data for std: results/synthetic_results.csv, scale=200, seeds 42/43/44 -->
<!-- Table 1 current values (scale=200, mean across seeds): -->
<!--   DoorToDoor: accept=0.610, vkm=2603.7, vkm/trip=21.3 -->
<!--   FullModel:  accept=0.208, vkm=628.5,  vkm/trip=15.1 -->
<!-- NOTE: Table values were set in prior phases; compute std from CSV for same scale=200 -->
<!-- CSV columns: variant, scale, seed, acceptance_rate, vehicle_km, ... -->
<!-- vkm/trip formula: vehicle_km / (acceptance_rate * scale) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Fielbaum et al. (2021) citation to literature.tex Section 2.2</name>
  <files>paper/sections/literature.tex</files>
  <read_first>paper/sections/literature.tex</read_first>
  <action>
Read paper/sections/literature.tex. Locate Section 2.2 "Meeting Points and Virtual Stops"
(starts around line 12). Find the paragraph about Cortenbach et al. (2024) — specifically
the paragraph beginning "However, the DARPmp of \citet{cortenbach2024} has two important
limitations..." (around line 18). This paragraph ends the section.

After the final sentence of that paragraph (ending "...and couples this with an explicit
multinomial logit (MNL) passenger choice model."), add the following new paragraph:

\citet{fielbaum2021} studied bidirectional walking flexibility in ridepooling, showing
that allowing passengers to walk to both pickup and dropoff points reduces vehicle detours
and improves system efficiency. Their analysis demonstrates that the efficiency gains from
dropoff-side flexibility are comparable in magnitude to those from pickup-side flexibility,
providing empirical motivation for the bidirectional design adopted in the present paper.
Our work extends this insight to the many-to-many DRT setting with explicit MNL passenger
choice and online re-optimisation: unlike \citet{fielbaum2021}, who study a static
ridepooling context, we address the online assignment problem where meeting points must be
selected in real time as requests arrive, and passenger acceptance is modelled
probabilistically rather than assumed.

Do not modify any other content.
  </action>
  <verify>
    <automated>grep -n "fielbaum2021" paper/sections/literature.tex && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "fielbaum2021" paper/sections/literature.tex returns a match
    - grep "bidirectional walking flexibility" paper/sections/literature.tex returns a match
    - grep "online re-optimisation" paper/sections/literature.tex returns a match in the new paragraph
    - The Cortenbach paragraph is unchanged (grep "DARPmp of" still returns a match)
  </acceptance_criteria>
  <done>Fielbaum et al. (2021) cited in Section 2.2 with a positioning sentence explaining how the present paper extends their work.</done>
</task>

<task type="auto">
  <name>Task 2: Add ± std to Table 1 and 3-seed note to Section 5.1 in experiments.tex</name>
  <files>paper/sections/experiments.tex</files>
  <read_first>
    paper/sections/experiments.tex
    results/synthetic_results.csv
  </read_first>
  <action>
STEP A — Compute std values from results/synthetic_results.csv.

Read results/synthetic_results.csv. Filter rows where scale=200 and seeds in {42,43,44}.
For each variant, compute std of acceptance_rate, vehicle_km, and vkm/trip
(vkm/trip = vehicle_km / (acceptance_rate * scale)).

Key values to compute (scale=200, seeds 42/43/44):
  DoorToDoor:
    acceptance_rate: seeds 42=0.58, 43=0.72, 44=0.55 → std
    vehicle_km: seeds 42=1693.75, 43=2152.46, 44=1765.44 → std
    vkm/trip: 1693.75/(0.58*200), 2152.46/(0.72*200), 1765.44/(0.55*200) → std
  SingleSidedPickup:
    acceptance_rate: seeds 42=0.53, 43=0.675, 44=0.565 → std
    vehicle_km: seeds 42=1465.39, 43=1789.28, 44=1446.13 → std
    vkm/trip: compute → std
  BidirectionalNoChoice:
    acceptance_rate: seeds 42=0.565, 43=0.70, 44=0.545 → std
    vehicle_km: seeds 42=1491.67, 43=1717.65, 44=1396.50 → std
    vkm/trip: compute → std
  FullModel:
    acceptance_rate: seeds 42=0.23, 43=0.185, 44=0.23 → std
    vehicle_km: seeds 42=521.91, 43=330.02, 44=405.72 → std
    vkm/trip: compute → std
  AblationNoChoice:
    acceptance_rate: seeds 42=0.53, 43=0.56, 44=0.47 → std
    vehicle_km: seeds 42=1276.75, 43=1116.76, 44=1298.34 → std
    vkm/trip: compute → std
  AblationNoRollingHorizon:
    acceptance_rate: seeds 42=0.21, 43=0.265, 44=0.27 → std
    vehicle_km: seeds 42=473.83, 43=537.89, 44=663.76 → std
    vkm/trip: compute → std

Use Python or manual calculation. Round std to 1 decimal for vkm/vehicle_km,
3 decimal places for acceptance_rate.

STEP B — Update Table 1 in experiments.tex.

Read experiments.tex. Locate Table 1 (tab:main-results, lines 84-100). The current table
has columns: Variant, Accept., vkm, vkm/trip, Avg Wait (s), Avg Walk (m), Detour, CPU (s).

Update the Accept., vkm, and vkm/trip columns to show "mean ± std" format using
$X \pm Y$ notation. Keep Avg Wait, Avg Walk, Detour, CPU columns as-is (means only).

The table caption already says "mean $\pm$ std" — update the data rows to match.

Example format for FullModel row:
  FullModel & $0.208 \pm 0.026$ & $419.2 \pm 79.1$ & $9.7 \pm 1.4$ & 450.3 & 95.9 & 1.00 & 1.4 \\

IMPORTANT: Use the std values computed from the CSV in Step A. The mean values in the
existing table (e.g., FullModel vkm/trip=15.1) were set in prior phases and may differ
from the CSV-computed means. Use the existing mean values from the table as-is; only
ADD the ± std computed from the CSV. If the CSV-computed mean differs significantly from
the table value, note this discrepancy in the summary but do not change the mean — only
add the std.

STEP C — Add 3-seed justification note to Section 5.1.

In experiments.tex, locate the paragraph about the synthetic scenario in Section 5.1
(around lines 26-32, the paragraph ending "...with full reproducibility (seeds 42, 43, 44).").

After the sentence "This scenario allows controlled parameter sweeps with full
reproducibility (seeds 42, 43, 44).", add:
  " Results are averaged over three random seeds (42, 43, 44); standard deviations
    are reported in Table~\ref{tab:main-results}."

(Append to the same sentence or add as a new sentence in the same paragraph.)
  </action>
  <verify>
    <automated>grep -n "\\\\pm" paper/sections/experiments.tex | head -5 && grep -n "standard deviations" paper/sections/experiments.tex && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "\\pm" paper/sections/experiments.tex returns at least 3 matches in the Table 1 data rows (one per updated column)
    - grep "standard deviations" paper/sections/experiments.tex returns a match in Section 5.1
    - grep "tab:main-results" paper/sections/experiments.tex returns a match in the 3-seed note
    - The table still has 6 data rows (DoorToDoor, SingleSidedPickup, BidirectionalNoChoice, FullModel, AblationNoChoice, AblationNoRollingHorizon)
    - The table caption still contains "mean \$\\pm\$ std"
  </acceptance_criteria>
  <done>Table 1 has ± std on acceptance rate, vkm, and vkm/trip columns; Section 5.1 has 3-seed justification sentence referencing Table 1.</done>
</task>

<task type="auto">
  <name>Task 3: Add Fielbaum note to response_to_reviewers.tex</name>
  <files>paper/response_to_reviewers.tex</files>
  <read_first>paper/response_to_reviewers.tex</read_first>
  <action>
Read paper/response_to_reviewers.tex. Find the most recent reviewer response section
(likely a GPT-5 review section or a "v4.0 Revisions" section). If a v4.0 section exists,
add the Fielbaum note there. If not, append a new section at the end of the document
(before \end{document} if present, or at the end of the file).

Add the following text in the appropriate location:

\subsection*{Literature Update: Fielbaum et al.\ (2021)}

In response to the GPT-5 review (2026-04-13), we have added \citet{fielbaum2021} to
Section~2.2 (Meeting Points and Virtual Stops). \citet{fielbaum2021} studied bidirectional
walking flexibility in ridepooling and demonstrated that allowing passengers to walk to
both pickup and dropoff points reduces vehicle detours. We now explicitly position our
work relative to this prior art: our contribution extends the bidirectional flexibility
concept to the many-to-many DRT setting with online re-optimisation and probabilistic
passenger choice, which \citet{fielbaum2021} do not address.

If the file uses a different section structure (e.g., itemized responses), adapt the
format to match the existing style while preserving the substance above.
  </action>
  <verify>
    <automated>grep -n "fielbaum2021\|Fielbaum" paper/response_to_reviewers.tex && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "fielbaum2021" paper/response_to_reviewers.tex returns a match
    - grep "Fielbaum" paper/response_to_reviewers.tex returns a match
    - grep "Section.*2.2\|Section~2.2" paper/response_to_reviewers.tex returns a match in the new note
  </acceptance_criteria>
  <done>response_to_reviewers.tex notes the addition of Fielbaum et al. (2021) to Section 2.2 in response to GPT-5 review.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor→LaTeX source | All edits are to local .tex files; no external input |
| CSV→table | Std values computed from local results/synthetic_results.csv |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-07 | Tampering | literature.tex Section 2.2 | mitigate | Read file first; append after Cortenbach paragraph; verify original paragraph unchanged |
| T-13-08 | Information Disclosure | std values from CSV | accept | CSV is local simulation data; no PII or sensitive information |
| T-13-09 | Tampering | Table 1 mean values | mitigate | Instruction explicitly says: keep existing means, only add ± std; discrepancies noted in summary |
</threat_model>

<verification>
After all three tasks:
1. `grep -n "fielbaum2021" paper/sections/literature.tex` returns a match in Section 2.2
2. `grep -n "\\\\pm" paper/sections/experiments.tex` returns matches in Table 1 data rows
3. `grep -n "standard deviations" paper/sections/experiments.tex` returns a match in Section 5.1
4. `grep -n "Fielbaum" paper/response_to_reviewers.tex` returns a match
5. `grep -n "fielbaum2021" paper/references.bib` returns a match (confirming bib entry exists)
</verification>

<success_criteria>
- Fielbaum et al. (2021) cited in Section 2.2 with positioning sentence explaining extension to many-to-many DRT with online re-optimization and passenger choice
- Table 1 has ± notation on acceptance rate, vkm, and vkm/trip (std computed from seeds 42/43/44 at scale=200)
- Section 5.1 has 3-seed justification sentence referencing Table 1
- response_to_reviewers.tex notes the Fielbaum addition
</success_criteria>

<output>
After completion, create `.planning/phases/13-paper-fixes-literature-update/13-03-SUMMARY.md`
</output>
