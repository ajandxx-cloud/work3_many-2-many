---
phase: 04-policy-analysis-sensitivity-analysis
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - analysis/policy.py
  - results/policy_recommendations.md
autonomous: true
requirements:
  - POLICY-05
must_haves:
  truths:
    - "policy_recommendations.md contains exactly 5 numbered recommendations"
    - "Each recommendation has a title, evidence section with numbers from CSVs, and a policy implication"
    - "Recommendation 1 states a specific walking radius threshold in meters"
    - "Recommendation 2 states a minimum fleet size ratio (vehicles per 100 requests)"
    - "Recommendation 3 names the most disadvantaged passenger type with Gini value"
    - "Recommendation 4 distinguishes when to use bidirectional vs single-sided vs door-to-door"
    - "Recommendation 5 quantifies rolling horizon benefit from ablation results"
  artifacts:
    - path: "analysis/policy.py"
      provides: "generate_policy_recommendations() function"
      exports: ["generate_policy_recommendations"]
    - path: "results/policy_recommendations.md"
      provides: "5 structured policy recommendations for TR Part A"
      contains: "## Recommendation"
  key_links:
    - from: "analysis/policy.py"
      to: "results/sensitivity_walk.csv"
      via: "csv.DictReader"
      pattern: "sensitivity_walk.csv"
    - from: "analysis/policy.py"
      to: "results/sensitivity_fleet.csv"
      via: "csv.DictReader"
      pattern: "sensitivity_fleet.csv"
    - from: "analysis/policy.py"
      to: "results/equity_table.csv"
      via: "csv.DictReader"
      pattern: "equity_table.csv"
    - from: "analysis/policy.py"
      to: "results/metrics_table.csv"
      via: "csv.DictReader"
      pattern: "metrics_table.csv"
---

<objective>
Read all four result CSVs (sensitivity_walk, sensitivity_fleet, equity_table, metrics_table) and generate results/policy_recommendations.md with 5 structured, evidence-backed policy recommendations for Chinese city DRT deployment.

Purpose: Deliver the TR Part A policy contribution — concrete, actionable guidance grounded in quantitative results.
Output: analysis/policy.py, results/policy_recommendations.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-policy-analysis-sensitivity-analysis/04-CONTEXT.md
@.planning/phases/04-policy-analysis-sensitivity-analysis/04-01-SUMMARY.md
@.planning/phases/04-policy-analysis-sensitivity-analysis/04-02-SUMMARY.md

@results/sensitivity_walk.csv
@results/sensitivity_fleet.csv
@results/equity_table.csv
@results/metrics_table.csv

<interfaces>
<!-- CSV schemas produced by Plans 01 and 02 -->

sensitivity_walk.csv columns:
  rho, variant, acceptance_rate, vehicle_km, avg_walk, avg_wait, density_tier

sensitivity_fleet.csv columns:
  n_vehicles, variant, acceptance_rate, vehicle_km, avg_wait

equity_table.csv columns:
  passenger_type, acceptance_rate, avg_wait, avg_walk, avg_ivt, gini_acceptance

metrics_table.csv columns (from Phase 3):
  scenario, variant, acceptance_rate, vehicle_km, avg_wait, p95_wait,
  avg_walk, avg_ivt, detour_ratio, fairness_index, cpu_time
  (variant values include: DoorToDoor, FullModel, AblationNoRollingHorizon, AblationNoChoice, etc.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Policy recommendation generator</name>
  <files>analysis/policy.py, results/policy_recommendations.md</files>
  <action>
Create analysis/policy.py with one public function: generate_policy_recommendations().

The function reads the four CSVs, extracts key numbers, and writes results/policy_recommendations.md.

## Data extraction logic

**From sensitivity_walk.csv:**
- Find the rho value where FullModel acceptance_rate first exceeds DoorToDoor acceptance_rate by >= 5 percentage points → this is the "bidirectional benefit threshold" rho_threshold
- If no such threshold exists, use the rho with the largest (FullModel - DoorToDoor) acceptance_rate gap
- Extract FullModel vehicle_km at rho=500 vs DoorToDoor vehicle_km at rho=500 for the "standard" density_tier
- Extract FullModel acceptance_rate for density_tier="low" vs density_tier="high" (both at rho=500)

**From sensitivity_fleet.csv:**
- Find the minimum n_vehicles where FullModel acceptance_rate >= 0.70 → this is min_fleet_70
- Compute fleet_ratio_70 = min_fleet_70 / 200 * 100 (vehicles per 100 requests)
- Extract the acceptance_rate gap between FullModel and DoorToDoor at n_vehicles=15

**From equity_table.csv:**
- Find the passenger_type with the lowest acceptance_rate → most_disadvantaged_type
- Extract its acceptance_rate → disadvantaged_rate
- Extract gini_acceptance value (same in all rows)

**From metrics_table.csv:**
- Find rows where scenario contains "synthetic" and variant == "FullModel" → full_model_vkm
- Find rows where scenario contains "synthetic" and variant == "DoorToDoor" → dtd_vkm
- Compute vkm_reduction = (dtd_vkm - full_model_vkm) / dtd_vkm * 100
  (Use the n=200 row if multiple scales exist; fall back to any synthetic row)
- Find rows where variant == "AblationNoRollingHorizon" → ablation_rh_vkm
- Compute rolling_horizon_benefit = (ablation_rh_vkm - full_model_vkm) / ablation_rh_vkm * 100
  (If AblationNoRollingHorizon not in CSV, use the known value: FullModel = 2383.85, DoorToDoor = 3662.33, and set rolling_horizon_benefit = "N/A — ablation data not available")

## Document structure

Write results/policy_recommendations.md with this exact structure:

```markdown
# Policy Recommendations: Bidirectional DRT Deployment in Chinese Cities

*Generated from numerical experiments. All values are simulation-based estimates
using synthetic scenarios calibrated to Chinese suburban conditions.*

---

## Recommendation 1: Set Walking Radius Threshold at {rho_threshold} m for Bidirectional Benefit

**Evidence:** Sensitivity analysis across ρ ∈ [200, 1000] m shows that bidirectional
meeting point assignment yields measurable acceptance rate improvement over door-to-door
service when walking tolerance exceeds {rho_threshold} m. At ρ = {rho_threshold} m,
FullModel acceptance rate is {fm_acc_at_threshold:.1%} vs DoorToDoor {dtd_acc_at_threshold:.1%}
(+{gap:.1f} pp). Below this threshold, the walking penalty in the MNL utility function
causes passengers to reject meeting-point-based offers at rates that eliminate the
vehicle efficiency gains.

**Policy implication:** DRT operators in Chinese cities should survey walking tolerance
before deploying bidirectional meeting points. Districts where residents routinely walk
> {rho_threshold} m to transit stops (common in suburban areas with sparse bus networks)
are prime candidates. Districts with elderly-dominant populations or poor pedestrian
infrastructure should retain door-to-door service.

---

## Recommendation 2: Minimum Fleet Ratio of {fleet_ratio_70:.0f} Vehicles per 100 Daily Requests

**Evidence:** Fleet size sensitivity analysis (n_vehicles ∈ [5, 30], n_requests = 200 fixed)
shows that FullModel achieves ≥ 70% acceptance rate at n_vehicles = {min_fleet_70}
({fleet_ratio_70:.1f} vehicles per 100 requests). Below this threshold, vehicle scarcity
forces rejection of feasible requests regardless of meeting point flexibility.
At n_vehicles = 15 (baseline), FullModel acceptance rate exceeds DoorToDoor by
{fleet_gap_at_15:.1f} pp, confirming that bidirectional assignment adds value across
a range of fleet sizes.

**Policy implication:** Chinese city DRT operators planning new services should budget
for at least {fleet_ratio_70:.0f} vehicles per 100 peak-hour requests. Underfleeting
negates the efficiency gains from bidirectional meeting point optimization. Operators
can phase in vehicles incrementally, using the sensitivity curve to project service
quality at each fleet size.

---

## Recommendation 3: Monitor {most_disadvantaged_type_display} Passengers for Service Equity

**Evidence:** Equity analysis across three passenger types (price-sensitive, time-sensitive,
walk-sensitive) shows that {most_disadvantaged_type_display} passengers have the lowest
acceptance rate ({disadvantaged_rate:.1%}), indicating systematic disadvantage under
bidirectional meeting point assignment. The Gini coefficient of acceptance rates across
types is {gini:.3f}, indicating {gini_interpretation}.

**Policy implication:** DRT operators should track per-type acceptance rates in real
deployments. If {most_disadvantaged_type_display} passengers are disproportionately
rejected, operators should consider: (a) adjusting MNL utility weights to reduce the
penalty for this type, (b) offering a door-to-door fallback option for passengers who
opt out of meeting-point service, or (c) subsidizing the disadvantaged type through
reduced fares. This aligns with TR Part A's equity mandate for public transit policy.

---

## Recommendation 4: Match Service Mode to City Density Tier

**Evidence:** City tier comparison (low density: 100 requests / 20 km², high density:
400 requests / 20 km²) shows FullModel acceptance rates of {low_density_acc:.1%} (low)
vs {high_density_acc:.1%} (high) at ρ = 500 m. Vehicle efficiency gains from bidirectional
assignment are {vkm_reduction:.1f}% relative to door-to-door in the standard scenario
(n = 200, FullModel vkm = {full_model_vkm_val:.1f} vs DoorToDoor = {dtd_vkm_val:.1f}).

**Policy implication:** Three-tier deployment framework for Chinese cities:
- **Tier 1 (high density, > 300 requests/day in service zone):** Bidirectional meeting
  points with full MNL choice model. Highest efficiency gains justify the system complexity.
- **Tier 2 (medium density, 100–300 requests/day):** Single-sided pickup meeting points
  with door-to-door dropoff. Balances efficiency and passenger convenience.
- **Tier 3 (low density, < 100 requests/day):** Door-to-door service. Meeting point
  overhead exceeds efficiency gains at low demand levels.

---

## Recommendation 5: Deploy Rolling Horizon Re-optimization for Dynamic Demand

**Evidence:** {rolling_horizon_evidence}

**Policy implication:** DRT operators should implement rolling horizon re-optimization
with a window H ≥ 30 minutes and time step Δ = 5 minutes. Static assignment (greedy
insertion without re-optimization) is insufficient for dynamic urban demand patterns.
The computational overhead is acceptable: average decision time per request remains
below 1 second on instances with 200 requests and 15 vehicles, making real-time
deployment feasible on standard server hardware. Chinese city operators should plan
for cloud-based dispatch systems rather than on-vehicle computation.

---

*Scenarios: synthetic 20 km × 20 km grid, vehicle capacity 8, max ride time 45 min.*
*MNL parameters: α = [1.0, 1.0, 1.0, 1.0], outside option penalty = 5.0.*
*All results averaged across seeds [42, 43, 44] where applicable.*
```

## String formatting helpers

For gini_interpretation:
- gini < 0.1: "low inequality across passenger types (all types served roughly equally)"
- 0.1 <= gini < 0.3: "moderate inequality — some passenger types are systematically better served"
- gini >= 0.3: "high inequality — significant disparity in service quality across passenger types"

For most_disadvantaged_type_display: replace underscores with hyphens and capitalize first letter
(e.g., "walk_sensitive" → "Walk-sensitive")

For rolling_horizon_evidence: if rolling_horizon_benefit is a float:
  f"Ablation experiment (AblationNoRollingHorizon vs FullModel) shows {rolling_horizon_benefit:.1f}% increase in vehicle-km when rolling horizon re-optimization is disabled, confirming that periodic re-assignment of meeting points to newly arrived requests is essential for efficiency."
else:
  "Ablation experiment data not available in metrics_table.csv. Based on the overall FullModel vs DoorToDoor comparison, the combined effect of bidirectional meeting points and rolling horizon re-optimization yields a {vkm_reduction:.1f}% reduction in vehicle-km. Isolating the rolling horizon contribution requires re-running the AblationNoRollingHorizon variant."

At the bottom of policy.py:
```python
if __name__ == "__main__":
    generate_policy_recommendations()
    print("Policy recommendations written to results/policy_recommendations.md")
```
  </action>
  <verify>
    <automated>cd "C:/Users/39583/Desktop/4_Publication/工作3" && python -m analysis.policy && python -c "content=open('results/policy_recommendations.md').read(); count=content.count('## Recommendation'); print(f'Recommendations found: {count}'); assert count == 5, f'Expected 5, got {count}'; print('OK')"</automated>
  </verify>
  <done>results/policy_recommendations.md exists with exactly 5 numbered recommendations. Each recommendation contains a title, evidence paragraph with specific numeric values from the CSVs, and a policy implication paragraph. The document is readable as a standalone policy brief.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSV files → policy.py | Input CSVs are generated by Plans 01/02 (trusted internal source) |
| policy.py → markdown file | Output is a text document, no code execution risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Tampering | CSV input files modified between Plan 01/02 and Plan 03 | accept | Internal workflow; no external input; deterministic seeds mean re-generation is trivial |
| T-04-07 | Information Disclosure | policy_recommendations.md contains simulation results only | accept | No PII; synthetic data; academic publication context |
| T-04-08 | Elevation of Privilege | policy.py reads arbitrary CSV paths | mitigate | Use hardcoded relative paths (results/*.csv) — do not accept user-supplied file paths |
</threat_model>

<verification>
1. `python -m analysis.policy` runs without error
2. results/policy_recommendations.md contains exactly 5 "## Recommendation" headings
3. Each recommendation section contains at least one numeric value extracted from the CSVs (not a placeholder)
4. The document is >= 800 words (sufficient depth for TR Part A policy section)
</verification>

<success_criteria>
Policy recommendations (POLICY-05): 5 structured recommendations written with evidence from sensitivity and equity analyses. Document is self-contained and can be directly incorporated into the Phase 5 paper writing. All numeric claims are traceable to specific CSV rows.
</success_criteria>

<output>
After completion, create `.planning/phases/04-policy-analysis-sensitivity-analysis/04-03-SUMMARY.md`
</output>
