---
phase: 14-paper-response-letter-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - paper/sections/policy.tex
  - paper/response_to_reviewers.tex
autonomous: true
requirements:
  - NUM-01
  - NUM-02
  - NUM-03

must_haves:
  truths:
    - "No occurrence of 0.122 (without trailing digit) remains in policy.tex"
    - "policy.tex Gini value reads 0.1216"
    - "response_to_reviewers.tex FIX-02 cap target reads 22.8%, not 23.5%"
    - "Weight-sensitivity table column headers already correct — no change needed"
  artifacts:
    - path: "paper/sections/policy.tex"
      provides: "Gini coefficient corrected to 0.1216"
      contains: "0.1216"
    - path: "paper/response_to_reviewers.tex"
      provides: "Cap target corrected to 22.8% in FIX-02"
      contains: "22.8"
  key_links:
    - from: "paper/sections/policy.tex"
      to: "paper/sections/experiments.tex"
      via: "Gini value must match across both files"
      pattern: "0\\.1216"
---

<objective>
Fix two numeric inconsistencies in the paper and response letter: (1) Gini coefficient in policy.tex still reads 0.122 instead of 0.1216, and (2) the FIX-02 section of the response letter cites 23.5% cap target instead of the actual experiment mean of 22.8%. Also verify the weight-sensitivity table column headers (NUM-03) — pre-scouted context confirms they are already correct, so this task is a read-and-confirm only.

Purpose: Ensure every number in the submission package matches actual experiment output before final submission.
Output: policy.tex with corrected Gini; response_to_reviewers.tex with corrected cap target in FIX-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Gini coefficient in policy.tex (NUM-01)</name>
  <files>paper/sections/policy.tex</files>
  <read_first>
    - paper/sections/policy.tex (locate the exact line with \textbf{0.122})
    - paper/sections/experiments.tex (confirm 0.1216 is already present — source of truth)
    - paper/sections/abstract.tex (confirm 0.1216 is already present — source of truth)
  </read_first>
  <action>
In paper/sections/policy.tex at line 112, change:

  \textbf{0.122}

to:

  \textbf{0.1216}

This is the only occurrence that needs changing. experiments.tex line 272 and abstract.tex line 24 already have 0.1216 and must NOT be touched.

After editing, run: grep -n "0\.122[^0-9]" paper/sections/policy.tex
Expected result: no matches (the old value is gone).
Also run: grep -n "0\.1216" paper/sections/policy.tex
Expected result: at least one match confirming the new value is present.
  </action>
  <verify>
    <automated>grep -n "0\.122[^0-9]" paper/sections/policy.tex && echo "FAIL: old value still present" || echo "PASS: old value removed"</automated>
    <automated>grep -n "0\.1216" paper/sections/policy.tex</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "0\.122[^0-9]" paper/sections/policy.tex returns zero matches
    - grep -n "0\.1216" paper/sections/policy.tex returns at least one match
    - grep -n "0\.1216" paper/sections/experiments.tex still returns a match (unchanged)
    - grep -n "0\.1216" paper/sections/abstract.tex still returns a match (unchanged)
  </acceptance_criteria>
  <done>policy.tex Gini reads \textbf{0.1216}; experiments.tex and abstract.tex are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Fix cap target in response letter FIX-02 (NUM-02) and verify weight-sensitivity table (NUM-03)</name>
  <files>paper/response_to_reviewers.tex</files>
  <read_first>
    - paper/response_to_reviewers.tex (read lines 150-170 to see the full FIX-02 block before editing)
    - paper/sections/experiments.tex (lines 370-385 — weight-sensitivity table to confirm NUM-03 is already correct)
  </read_first>
  <action>
PART A — Fix cap target in response_to_reviewers.tex (NUM-02):

At line 159 of response_to_reviewers.tex, change:

  $\approx$23.5\%

to:

  $\approx$22.8\%

This is the only occurrence of 23.5 in the FIX-02 block. The paper text (experiments.tex line 148) and table caption (experiments.tex line 174) already use 22.8% and must NOT be touched.

PART B — Verify weight-sensitivity table (NUM-03):

Read paper/sections/experiments.tex lines 370-385. Confirm the column headers for the weight-sensitivity table contain "vkm/trip" (not raw "vkm"). Pre-scouted context confirms headers already read "FullModel vkm/trip" and "DoorToDoor vkm/trip". If confirmed, no edit is needed — record the confirmation in the task output.

After editing PART A, run:
  grep -n "23\.5" paper/response_to_reviewers.tex
Expected: no matches in the FIX-02 block (lines 153-167).
  grep -n "22\.8" paper/response_to_reviewers.tex
Expected: at least one match in the FIX-02 block.
  </action>
  <verify>
    <automated>grep -n "23\.5" paper/response_to_reviewers.tex</automated>
    <automated>grep -n "22\.8" paper/response_to_reviewers.tex</automated>
    <automated>grep -n "vkm/trip" paper/sections/experiments.tex</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "23\.5" paper/response_to_reviewers.tex returns zero matches (or only matches outside lines 153-167 if 23.5 appears elsewhere in the letter for unrelated reasons — verify by line number)
    - grep -n "22\.8" paper/response_to_reviewers.tex returns at least one match in the FIX-02 block (lines 153-167)
    - grep -n "vkm/trip" paper/sections/experiments.tex returns matches for both "FullModel vkm/trip" and "DoorToDoor vkm/trip" column headers — confirming NUM-03 is already satisfied
    - experiments.tex lines 148 and 174 still contain 22.8 (unchanged)
  </acceptance_criteria>
  <done>response_to_reviewers.tex FIX-02 cap target reads 22.8%; weight-sensitivity table headers confirmed correct (no edit needed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor → LaTeX source | All edits are to static .tex files; no code execution or external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | policy.tex | accept | Pure text substitution in a local file; no integrity risk beyond accidental wrong edit — grep verification catches errors |
| T-14-02 | Tampering | response_to_reviewers.tex | accept | Same as above; acceptance criteria verify exact strings before and after |
</threat_model>

<verification>
After both tasks complete:
1. grep -rn "0\.122[^0-9]" paper/sections/ — must return zero matches
2. grep -rn "0\.1216" paper/sections/ — must return matches in policy.tex, experiments.tex, abstract.tex
3. grep -n "23\.5" paper/response_to_reviewers.tex — must return zero matches in FIX-02 block
4. grep -n "22\.8" paper/response_to_reviewers.tex — must return at least one match
5. grep -n "vkm/trip" paper/sections/experiments.tex — must confirm weight-sensitivity table headers
</verification>

<success_criteria>
- policy.tex: \textbf{0.1216} (not 0.122)
- response_to_reviewers.tex FIX-02: $\approx$22.8\% (not 23.5%)
- experiments.tex weight-sensitivity table: column headers confirmed as "vkm/trip" (NUM-03 already satisfied, no edit)
- No regressions in experiments.tex or abstract.tex Gini values
</success_criteria>

<output>
After completion, create .planning/phases/14-paper-response-letter-fixes/14-01-SUMMARY.md
</output>
