---
phase: 14-paper-response-letter-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - paper/response_to_reviewers.tex
  - paper/sections/experiments.tex
  - paper/sections/model.tex
autonomous: true
requirements:
  - RESP-01
  - RESP-02
  - CLEAN-01
  - CLEAN-02
  - CLEAN-03

must_haves:
  truths:
    - "FIX-02 section cites 11.1 vs 17.1 vkm/trip (35.0%) as primary efficiency claim"
    - "74.3% figure is demoted to footnote reference in FIX-02"
    - "R1 body reads 15.1 vkm/trip and 21.3 vkm/trip (not 3022/4268)"
    - "METRIC AUDIT comment block is gone from experiments.tex"
    - "model.tex no longer contains the word provisional"
    - "model.tex cross-reference uses sec:policy not subsec:vot-mapping"
  artifacts:
    - path: "paper/response_to_reviewers.tex"
      provides: "Updated FIX-02 and R1 body with correct endogenous numbers"
      contains: "11.1"
    - path: "paper/sections/experiments.tex"
      provides: "Clean file without METRIC AUDIT comment block"
    - path: "paper/sections/model.tex"
      provides: "No provisional annotation; correct cross-reference"
      contains: "sec:policy"
  key_links:
    - from: "paper/response_to_reviewers.tex FIX-02"
      to: "paper/sections/experiments.tex endogenous results"
      via: "11.1 vs 17.1 vkm/trip (35.0%) must match experiment output"
      pattern: "11\\.1.*17\\.1.*35\\.0"
---

<objective>
Five cleanup and correction tasks across three files: (1) rewrite the FIX-02 section of the response letter to cite endogenous matched-coverage results as primary evidence and demote the post-hoc 74.3% to a footnote; (2) fix the R1 body to use correct vkm/trip values instead of the old 3022/4268 raw numbers; (3) remove the METRIC AUDIT comment block from experiments.tex; (4) remove the "(provisional)" annotation from model.tex; (5) replace the subsec:vot-mapping cross-reference with sec:policy in model.tex.

Purpose: Produce a clean, internally consistent submission package with no development artifacts and no stale numbers in the response letter.
Output: Updated response_to_reviewers.tex, experiments.tex, model.tex.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite FIX-02 section and fix R1 body in response letter (RESP-01, RESP-02)</name>
  <files>paper/response_to_reviewers.tex</files>
  <read_first>
    - paper/response_to_reviewers.tex (read lines 55-75 for R1 body; read lines 150-175 for full FIX-02 block)
  </read_first>
  <action>
PART A — Fix R1 body (RESP-02), around line 63:

Find the sentence that currently reads:
  "FullModel achieves 3{,}022\,vkm per unit acceptance rate, compared to 4{,}268 for DoorToDoor --- a \textbf{29.2\% improvement}"

Replace with:
  "FullModel achieves 15.1\,vkm/trip, compared to 21.3\,vkm/trip for DoorToDoor --- a \textbf{29.2\% improvement}"

PART B — Rewrite FIX-02 block (RESP-01), lines 153-167:

The FIX-02 block currently contains:
  - $\approx$23.5\% (already fixed in Plan 01, Task 2 — confirm it now reads 22.8%)
  - \textbf{10.9}\,vkm/trip for FullModel
  - \textbf{42.3}\,vkm/trip for DoorToDoor matched
  - \textbf{74.3\%} improvement
  - "This is now the primary efficiency claim" framing around the 74.3% figure

Make these changes:
  1. Change \textbf{10.9}\,vkm/trip → \textbf{11.1}\,vkm/trip
  2. Change \textbf{42.3}\,vkm/trip → \textbf{17.1}\,vkm/trip
  3. Change \textbf{74.3\%} → \textbf{35.0\%}
  4. Update the surrounding sentence so 11.1 vs 17.1 (35.0%) is framed as the PRIMARY efficiency claim (endogenous matched-coverage result at ~22.8% served share)
  5. The unconstrained comparison (15.1 vs 21.3, 29.2%) should remain as a secondary reference sentence
  6. The post-hoc result (10.9 vs 42.3, 74.3%) should be moved to a footnote or parenthetical note — it must NOT appear as a primary claim. If a \footnote{} already exists for it, update the numbers there too. If not, add a sentence like: "(For reference, a post-hoc lower bound gives 10.9 vs 42.3\,vkm/trip, 74.3\%; see footnote.)" or demote it to a \footnote{} at the end of the primary claim sentence.

After editing, verify:
  grep -n "3{,}022\|4{,}268" paper/response_to_reviewers.tex → zero matches
  grep -n "11\.1.*vkm\|vkm.*11\.1" paper/response_to_reviewers.tex → at least one match
  grep -n "17\.1.*vkm\|vkm.*17\.1" paper/response_to_reviewers.tex → at least one match
  grep -n "35\.0" paper/response_to_reviewers.tex → at least one match
  grep -n "15\.1.*vkm\|vkm.*15\.1" paper/response_to_reviewers.tex → at least one match (R1 body)
  </action>
  <verify>
    <automated>grep -n "3{,}022\|4{,}268" paper/response_to_reviewers.tex && echo "FAIL: old values present" || echo "PASS"</automated>
    <automated>grep -n "11\.1" paper/response_to_reviewers.tex</automated>
    <automated>grep -n "17\.1" paper/response_to_reviewers.tex</automated>
    <automated>grep -n "35\.0" paper/response_to_reviewers.tex</automated>
    <automated>grep -n "15\.1" paper/response_to_reviewers.tex</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "3{,}022" paper/response_to_reviewers.tex returns zero matches
    - grep -n "4{,}268" paper/response_to_reviewers.tex returns zero matches
    - grep -n "11\.1" paper/response_to_reviewers.tex returns at least one match in the FIX-02 block
    - grep -n "17\.1" paper/response_to_reviewers.tex returns at least one match in the FIX-02 block
    - grep -n "35\.0" paper/response_to_reviewers.tex returns at least one match in the FIX-02 block
    - grep -n "15\.1" paper/response_to_reviewers.tex returns at least one match in the R1 body (around line 63)
    - grep -n "21\.3" paper/response_to_reviewers.tex returns at least one match in the R1 body
    - The 74.3% figure appears only in a footnote or parenthetical — NOT as a standalone primary claim sentence
  </acceptance_criteria>
  <done>R1 body uses 15.1/21.3 vkm/trip; FIX-02 primary claim is 11.1 vs 17.1 vkm/trip (35.0%); 74.3% is demoted to footnote.</done>
</task>

<task type="auto">
  <name>Task 2: Remove METRIC AUDIT block from experiments.tex (CLEAN-01)</name>
  <files>paper/sections/experiments.tex</files>
  <read_first>
    - paper/sections/experiments.tex (read lines 1-20 to see the full comment block before deleting)
  </read_first>
  <action>
Read paper/sections/experiments.tex lines 1-20. The file begins with a comment block that starts with:
  % Section 5: Numerical Experiments

and ends before the line:
  \section{Numerical Experiments}\label{sec:experiments}

Delete lines 1 through the last comment line (line 13 or 14 — whichever is the blank line immediately before \section{...}). The file after editing must start with:
  \section{Numerical Experiments}\label{sec:experiments}

Do NOT delete the \section line itself or any content below it.

After editing, verify:
  head -1 paper/sections/experiments.tex
Expected output: \section{Numerical Experiments}\label{sec:experiments}
  grep -n "METRIC AUDIT" paper/sections/experiments.tex → zero matches
  </action>
  <verify>
    <automated>grep -n "METRIC AUDIT" paper/sections/experiments.tex && echo "FAIL: comment block still present" || echo "PASS"</automated>
    <automated>head -3 paper/sections/experiments.tex</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "METRIC AUDIT" paper/sections/experiments.tex returns zero matches
    - The first non-blank line of experiments.tex is \section{Numerical Experiments}\label{sec:experiments}
    - grep -n "\\\\section\{Numerical Experiments\}" paper/sections/experiments.tex returns a match at line 1 or 2 (allowing for one blank line at most)
  </acceptance_criteria>
  <done>experiments.tex starts directly with \section{Numerical Experiments}; no comment block above it.</done>
</task>

<task type="auto">
  <name>Task 3: Remove provisional annotation and fix cross-reference in model.tex (CLEAN-02, CLEAN-03)</name>
  <files>paper/sections/model.tex</files>
  <read_first>
    - paper/sections/model.tex (read lines 260-310 to see both the provisional annotation around line 268 and the cross-reference around line 301)
    - paper/sections/policy.tex (confirm label sec:policy exists — grep -n "\\label{sec:policy}" paper/sections/policy.tex)
  </read_first>
  <action>
PART A — Remove provisional annotation (CLEAN-02), around line 268:

Find the text:
  Baseline parameter values for simulation (provisional --- to be confirmed against
  Work~1/2 calibration before Phase~3):

Replace with:
  Baseline parameter values for simulation:

This removes the parenthetical "(provisional --- to be confirmed against Work~1/2 calibration before Phase~3)" entirely. The colon at the end must be retained.

PART B — Fix cross-reference (CLEAN-03), around line 301:

Find the text:
  Table~\ref{tab:vot-mapping} (Section~\ref{subsec:vot-mapping}).

Replace with:
  Table~\ref{tab:vot-mapping} (Section~\ref{sec:policy}).

After editing, verify:
  grep -n "provisional" paper/sections/model.tex → zero matches
  grep -n "subsec:vot-mapping" paper/sections/model.tex → zero matches
  grep -n "sec:policy" paper/sections/model.tex → at least one match
  grep -n "Baseline parameter values for simulation:" paper/sections/model.tex → at least one match
  </action>
  <verify>
    <automated>grep -n "provisional" paper/sections/model.tex && echo "FAIL: provisional still present" || echo "PASS"</automated>
    <automated>grep -n "subsec:vot-mapping" paper/sections/model.tex && echo "FAIL: old ref still present" || echo "PASS"</automated>
    <automated>grep -n "sec:policy" paper/sections/model.tex</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "provisional" paper/sections/model.tex returns zero matches
    - grep -n "subsec:vot-mapping" paper/sections/model.tex returns zero matches
    - grep -n "sec:policy" paper/sections/model.tex returns at least one match (the updated cross-reference)
    - grep -n "Baseline parameter values for simulation:" paper/sections/model.tex returns a match (colon retained, no parenthetical)
    - grep -n "\\\\label{sec:policy}" paper/sections/policy.tex returns a match (confirming the target label exists)
  </acceptance_criteria>
  <done>model.tex has no "(provisional)" text; cross-reference uses \ref{sec:policy} instead of \ref{subsec:vot-mapping}.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor → LaTeX source | All edits are to static .tex files; no code execution or external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Tampering | response_to_reviewers.tex | accept | Text substitution verified by grep on exact strings before and after; acceptance criteria catch wrong values |
| T-14-04 | Tampering | experiments.tex | accept | Deleting comment lines only; \section line preserved; head -1 check confirms correct first line |
| T-14-05 | Tampering | model.tex | accept | Two independent substitutions; grep confirms both old strings gone and new strings present |
</threat_model>

<verification>
After all three tasks complete:
1. grep -rn "3{,}022\|4{,}268" paper/response_to_reviewers.tex — zero matches
2. grep -n "11\.1" paper/response_to_reviewers.tex — match in FIX-02 block
3. grep -n "35\.0" paper/response_to_reviewers.tex — match in FIX-02 block
4. grep -n "METRIC AUDIT" paper/sections/experiments.tex — zero matches
5. head -2 paper/sections/experiments.tex — first line is \section{Numerical Experiments}
6. grep -n "provisional" paper/sections/model.tex — zero matches
7. grep -n "subsec:vot-mapping" paper/sections/model.tex — zero matches
8. grep -n "sec:policy" paper/sections/model.tex — at least one match
</verification>

<success_criteria>
- response_to_reviewers.tex: R1 body uses 15.1/21.3 vkm/trip; FIX-02 primary claim is 11.1 vs 17.1 vkm/trip (35.0%); 74.3% in footnote only
- experiments.tex: starts with \section{Numerical Experiments}; no METRIC AUDIT comment block
- model.tex: no "(provisional)" text; cross-reference is \ref{sec:policy}
</success_criteria>

<output>
After completion, create .planning/phases/14-paper-response-letter-fixes/14-02-SUMMARY.md
</output>
