# Technology Stack

**Analysis Date:** 2026-06-16

## Languages

**Primary:**
- Python >=3.10 - Core DRT package, experiment orchestration, analysis scripts, tests, and figure generation. Declared in `pyproject.toml`; package code lives in `src/drt/`, experiments in `experiments/`, analysis scripts in `analysis/`, root runner in `run_experiments.py`, and tests in `tests/`.

**Secondary:**
- LaTeX - Manuscript source in `manuscript/main.tex`, section files under `manuscript/sections/`, bibliography in `manuscript/references.bib`, and submission/support files `manuscript/cover_letter.tex` and `manuscript/response_to_reviewers.tex`.
- Markdown / text - Project documentation in `README.md`, `CLAUDE.md`, and `docs/`; generated policy output in `results/policy_recommendations.md`.
- CSV / JSON - Experiment outputs and manifests under `results/`, including `results/synthetic_results.csv`, `results/beijing_results.csv`, `results/metrics_table.csv`, `results/formal/phase06/**/*.csv`, and `results/formal/phase06/**/*.json`.
- PDF / PNG - Publication artifacts under `manuscript/figures/` and `results/formal/phase06/plots/`; generated by scripts under `manuscript/figures/scripts/` and `experiments/formal_statistics.py`.

## Runtime

**Environment:**
- Python 3.12.4 observed in the active shell.
- Project supports Python >=3.10 via `pyproject.toml`.
- Gurobi runtime is required only for exact MILP solve paths. `src/drt/milp.py` defers `gurobipy` imports inside `DRTModel.build()` and `DRTModel.solve()`, and `experiments/milp_gap.py` treats unavailable or unlicensed Gurobi as `milp_status="no_gurobi"`.
- LaTeX compilation requires a local TeX distribution with `pdflatex`, `bibtex`, and `elsarticle.cls`; commands are documented in `README.md`.

**Package Manager:**
- pip 26.0.1 observed in the active shell via `python -m pip --version`.
- Install package with `pip install -e .` and development dependencies with `pip install -e ".[dev]"`, as documented in `README.md`.
- Lockfile: missing. No `requirements.txt`, `poetry.lock`, `uv.lock`, `Pipfile.lock`, `environment.yml`, or Conda environment file detected.

## Frameworks

**Core:**
- setuptools >=42 - Build backend declared in `pyproject.toml`; package discovery uses `[tool.setuptools.packages.find]` with `where = ["src"]`.
- gurobipy, unpinned - Exact static MILP solver client for `src/drt/milp.py`; diagnostic gap experiments use it through `experiments/milp_gap.py`.
- numpy, unpinned - Declared runtime dependency in `pyproject.toml`; used for numeric/statistical work in `experiments/metrics.py`, `experiments/formal_statistics.py`, `experiments/phase06_robustness.py`, and figure scripts under `manuscript/figures/scripts/`.

**Testing:**
- pytest, unpinned - Declared in `[project.optional-dependencies].dev` in `pyproject.toml`; tests live in `tests/` plus `analysis/test_sensitivity.py`.
- pytest-benchmark, unpinned - Declared in `[project.optional-dependencies].dev`; timing/benchmark checks appear in `tests/test_alns.py` and `tests/test_insertion.py`.

**Build/Dev:**
- pandas, undeclared runtime import - Required by `run_experiments.py`, `experiments/runner.py`, `experiments/formal_validation.py`, `experiments/formal_statistics.py`, `experiments/phase06_coverage_controls.py`, `experiments/phase06_robustness.py`, tests such as `tests/test_runner.py`, and figure scripts such as `manuscript/figures/scripts/fig04_baseline_comparison.py`.
- matplotlib, undeclared figure dependency - Required by figure generation scripts under `manuscript/figures/scripts/`, including `fig01_system_overview.py`, `fig02_three_layer.py`, `fig03_algorithm.py`, `fig04_baseline_comparison.py`, `fig05_sensitivity.py`, `fig06_policy_map.py`, and `fig07_pareto.py`.
- subprocess/git CLI, undeclared external command - Experiment metadata helpers call `git rev-parse --short HEAD` in `experiments/runner.py`, `experiments/formal_statistics.py`, `experiments/phase06_formal.py`, `experiments/phase06_coverage_controls.py`, and `experiments/phase06_robustness.py`; failures fall back to `"unknown"` in several paths.
- No linting or formatting tool configuration detected. No `.eslintrc*`, `.prettierrc*`, `eslint.config.*`, `biome.json`, `ruff.toml`, `mypy.ini`, `pytest.ini`, or `tox.ini` files were detected at the repository root.

## Key Dependencies

**Critical:**
- `gurobipy` - Required for exact MILP modeling and solves in `src/drt/milp.py`; tests in `tests/test_milp.py` skip Gurobi-dependent cases when it is unavailable or unlicensed.
- `numpy` - Required for numeric metrics and statistical summaries in experiment modules such as `experiments/metrics.py` and `experiments/formal_statistics.py`.
- `pandas` - Required by experiment runners, validation/closeout scripts, and tests, but not declared in `pyproject.toml`. Add it before relying on reproducible fresh installs.
- `matplotlib` - Required for manuscript and phase plot generation, but not declared in `pyproject.toml`. Add it before relying on reproducible figure builds.

**Infrastructure:**
- Python standard library `csv`, `json`, `pathlib`, `argparse`, `subprocess`, `concurrent.futures`, `datetime`, and `dataclasses` - Used broadly for local experiment orchestration and artifact generation in `experiments/runner.py`, `experiments/formal_statistics.py`, and `src/drt/types.py`.
- `pytest` - Test runner for `tests/` and `analysis/test_sensitivity.py`.
- Local TeX toolchain - Required to compile `manuscript/main.tex` into `manuscript/main.pdf`.

## Configuration

**Environment:**
- Runtime configuration is code-based. Shared experiment constants live in `experiments/config.py`, including seeds, scale grids, vehicle counts, walking radii, rolling-horizon parameters, ALNS iterations, choice-model parameters, and vehicle capacity/duration constants.
- Package metadata and dependency declarations live in `pyproject.toml`.
- No `.env*` files detected at repository root. Do not add required secrets or environment values to committed files.
- Gurobi licensing is configured outside the repository through the local Gurobi installation/license mechanism; no repo environment variable is read by `src/drt/milp.py`.

**Build:**
- `pyproject.toml` declares `setuptools.build_meta`, project metadata, Python version support, package discovery, runtime dependencies, and optional dev dependencies.
- `README.md` documents installation, experiment execution, pytest invocation, and LaTeX manuscript compilation.
- `src/drt.egg-info/` is present with generated package metadata, including `src/drt.egg-info/requires.txt`; treat it as build metadata, not source of truth.

## Platform Requirements

**Development:**
- Python >=3.10 with pip.
- Install core package using `pip install -e .`; install tests with `pip install -e ".[dev]"`.
- Install undeclared-but-used packages `pandas` and `matplotlib` for full experiment, validation, and figure workflows.
- Install Gurobi plus a valid local license for exact MILP solves in `src/drt/milp.py`, `experiments/milp_gap.py`, and Gurobi-enabled tests in `tests/test_milp.py`.
- Set `PYTHONPATH=src` or use editable install so `drt` imports resolve in tests and scripts; several test files insert local paths manually, but editable install is the expected workflow in `README.md`.
- Local `git` executable is useful for artifact metadata; code paths fall back to `"unknown"` when Git metadata is unavailable.
- TeX distribution with `pdflatex`, `bibtex`, and `elsarticle.cls` for manuscript builds from `manuscript/`.

**Production:**
- Not applicable. This is a local research/academic simulation repository, not a deployed service.
- Runtime outputs are local files under `results/` and publication artifacts under `manuscript/`.

---

*Stack analysis: 2026-06-16*
